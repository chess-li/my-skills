# interview 触发测试集

修改 description 后必跑。断言：干净会话中是否加载了 interview（`skill` 工具调用 name=interview）。
语料优先真实会话原样摘录；跑法：干净会话发输入，查该 harness 的 skill 加载记录（opencode：`opencode run --dir <空目录> --title trigger-interview-<ID> "<输入>"` 后查会话 DB；Codex：子代理干净上下文探针，rollout 取证）。

## 应触发

- **P1**（同一描述多种合理解读）：

  ```
  现在skill的配置项有哪些
  ```

  来源：2026-09-02 ey-timp-lab ses_f9dff28fbfferh37TojOJTEyK5 首条原话，当时零 question、自选一种作答（返工事件）

## 不应触发

- **N1**（一种解读的能力变更）：`将该配置的prefix修改为ey.skill.script.dependency`
  来源：同会话第四条原话；应走 spec，不因多义加载 interview

## 结果记录

| 日期 | 版本 | P1 | N1 | 备注 |
|---|---|---|---|---|
| 2026-09-07 | 负向「术语表维护」改为「概念定义、命名选择与词条沉淀」 | 待跑 | 待跑 | 对照：用于段未改，P1 仍应触发、N1 仍不应；与 domains 负向对偶（domains 不用于多种合理解读的点选）。干净会话未跑 |
| 2026-09-12 | 同上（干净会话补跑） | ✓ | ✗ | P1 加载 interview 列三种解读停下问。N1 加载 interview、未加载 spec——原话是同会话第四条，干净会话无「该配置」前序，语境饥渴过触发 |
