# implement 触发测试集

修改 description 后必跑。断言：干净会话中是否加载了 implement（`skill` 工具调用 name=implement）。
语料优先真实会话原样摘录；跑法：干净会话发输入，查该 harness 的 skill 加载记录（opencode：`opencode run --dir <目录> --title trigger-<ID> "<输入>"` 后查会话 DB；Codex：子代理干净上下文探针，rollout 取证）。
前置：implement 语义含"spec 就绪"，跑前应触发用例前先在目录内预置最小 spec（如 `docs/contexts/智能体节点/智能体节点-spec.md` 含一条验收标准），否则欠触发属环境伪影不属 description 缺陷。
工作流冒烟：前端对接产物见 `冒烟集.md`（其余工作流语料仍欠）；本文件只覆盖触发面。

## 应触发

- **P1**（已有能力上追加新能力）：`我想要为智能体节点增加试运行功能`
  来源：2026-08-05 智能体节点试运行会话（ses_02e537f28）首条原话，旧版 description 下未触发（返工事件）
- **P2**（中断续写）：`刚刚发生了一些中断,继续执行任务`
  来源：2026-08-05 智能体节点设计会话（ses_02f456f7d）原话，实际触发正确（读任务文件恢复）
- **P3**（spec 就绪进入实现）：`spec 就绪了，开始实现企业下单`
  来源：implement 设计会话示例语料

- **P4**（合并同一 spec 的任务归档）：`我的意思是整合本地的task archive到一个文件`
  来源：2026-08-28 ey-timp-lab ses_fb91594feffe 用户纠正原话（6 份归档合并为一份现场；08-28 description 加归档合并锚点时未补本用例）

## 不应触发

- **N1**（需求未定 → spec）：`需求还没定，先讨论下验收标准`
- **N2**（写测试 → tdd）：`给这个函数写单元测试`
- **N3**（技术决策修订 → design）：`XDubboService是历史兼容名称,实际已将其实现迁移到feign,所以不要使用@DubboReference注解,而是使用@Autowired`

## 边界观察（不做硬性断言）

- **B1**（implement vs tdd）：P3 输入两者同触发且分工不冲突（tdd 管编码纪律，implement 管验收闭环）；记录实际行为

## 结果记录

| 日期 | 版本 | P1 | P2 | P3 | P4 | N1 | N2 | N3 | B1 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|
| 2026-08-05 | 旧版（无"追加新能力"锚点） | ✗ | ✓ | ✓ | — | — | — | — | — | 真实会话 ses_02e537f28 未触发，用户问"为什么没有git提交"（返工事件） |
| 2026-08-05 | 新版（加锚点） | ◐ | — | ✓ | — | ✓ | — | — | — | P1（预置 spec）：流程正确以 spec 先行并发起澄清访谈，implement 接续与否未观测——单发会话止于提问、续跑被权限环境阻断，待真实使用验证；P3（预置 spec）加载 implement；N1 加载 spec 未加载 implement；P2/N2/N3 未跑（不受本次改动影响，成本控制） |
| 2026-09-24 | issue 化改写（+frontier 锚点，任务文件→issue） | ✓ | ◐ | ✓ | ✗ | ✓ | ✓ | ✓ | — | 干净会话实跑（opencode run 探针，样本量各 1–2 次如实报）：P1 spec→implement→interview 全链路 ✓（08-05 ◐ 清偿）；P3 ✓；N1→spec、N2/N3 零加载，implement 均未过触发 ✓；P2 续写 1/2——P2b 预置 doing issue 仍零加载（直接读文件作答），P2c ✓；P4 归档合并 0/2——两次均未加载且直接把 archive 散文件合并删目录，绕过核对/备查纪律；P2/P4 欠触发入观察名单（证据日志 2026-09-24） |
