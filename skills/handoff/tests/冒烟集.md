# handoff 冒烟集

工作流行为面。断言：加载 handoff 之后的正文分支行为。
语料均为真实会话原样摘录。对照改后正文即可。

## 用例

- **S1**（临时目录取本会话环境，不写死 harness）：

  ```
  不同的harness有不同的临时文件夹,你不应该固定opencode的临时目录作为目标
  ```

  硬性：交接文档落 `{本会话临时目录}/handoff/<yyyyMMdd>-<topic>.md`；`path.py` 接收临时目录参数，不拼接 harness 名。
  来源：2026-09-13 用户原话。

- **S2**（交出后提示词含绝对路径）：

  ```
  把当前任务移交给新会话
  ```

  硬性：写出交接文档后交给用户一条可粘贴提示词，内含该文档绝对路径。
  来源：2026-09-13 用户指令创建 handoff。

- **S3**（接手不查源会话）：

  ```
  按照任务ses_f66b32d4dffepvqllLJ0aR6Wto的建议修改结论执行
  ```

  前置：消息含交接文档路径或交接提示词。
  硬性：只读交接文档，按「下一步」走；不查源会话 id、不翻会话库。
  来源：2026-09-13 ses_f66842526ffe 首条原话。

## 结果记录

| 日期 | 版本 | S1 | S2 | S3 | 备注 |
|---|---|---|---|---|---|
| 2026-09-13 | 临时目录改本会话环境 | ✓ | ✓ | ✓ | 对照改后正文：旧稿 `{tmpdir}/opencode/handoff` 且 path.py 写死 `opencode`；新稿参数传入本会话临时目录。S2/S3 未触仍成立。冷启动 |
