# debug 触发测试集

修改 description 后必跑。断言：干净会话中是否加载了 debug（`skill` 工具调用 name=debug）。
语料优先真实会话原样摘录；跑法：干净会话发输入，查该 harness 的 skill 加载记录（opencode：`opencode run --dir <空目录> --title trigger-debug-<ID> "<输入>"` 后查会话 DB；Codex：子代理干净上下文探针，rollout 取证）。

## 应触发

- **P1**（调试给出本地访问地址的接口）：

  ```
  本地服务已启动,帮我调试/etl/exec/agent/recommend接口,本地访问域名为http://localhost:10003
  ```

  来源：2026-09-08 ses_f811722e 首条原话

- **P2**（测试给出本地访问地址的接口）：

  ```
  帮我测试recommand接口,本地地址为localhost:10003,userPrompt为"根据上传的excel生成OCED GIR XML,如有错误,生成对应的错误报告",目标推荐agent为PillarTwoAgent,目标推荐skill为generate-oecd-gir-xml,目标输出契约包含1.生成的OCED GIR XML 2.错误报告文件
  ```

  来源：2026-09-08 ses_f7f18c4c 首条原话

- **P3**（用户报告 bug）：

  ```
  修一下这个 bug，订单打折后总金额差一分钱
  ```

  来源：tdd 冒烟 T02 语料（bug 请求入口归 debug）

- **P4**（按已有结论执行修复）：

  ```
  按照任务ses_f66b32d4dffepvqllLJ0aR6Wto的建议修改结论执行
  ```

  来源：2026-09-13 ses_f66842526ffe 首条原话（零加载 debug）

- **P5**（本地验证接口，未给访问地址）：

  ```
  还未验证智能体节点解析前置节点结果的能力,设计一个场景,在本地环境中验证,可以使用etl的testrun接口
  ```

  来源：2026-09-13 ses_f697be333ffe 首条原话（零加载 debug）

- **P6**（验证并修复连体命令）：

  ```
  自动使用本地环境验证,自动修复

  ## 需要覆盖的核心闭环场景:

  1.上传skill->2.更新skill->3.ETL智能体配置推荐->4.ETL智能体试运行->5.ETL智能体试运行错误分析->6.含有智能体配置的ETL试运行.
  其中必须完整覆盖已提供的智能体配置功能.

  ## 你需要自己生成对应的场景并测试修复

  ## 除了你自己生成的场景之外还必须覆盖

  1. ORCD XML SKILL的场景(已在skill列表中提供)
  2. 所有内置skill的使用场景
  ```

  来源：2026-09-13 Codex 线程 01a09a63-73ad 首条原话（全程仅加载 local-env，debug/tdd 零加载）

## 不应触发

- **N1**（spec 就绪进入实现）：`spec 就绪了，开始实现企业下单`
  来源：tdd 冒烟 T01；应走 implement，不因「实现」加载 debug

- **N2**（能力变更含「接口」、无本地访问地址）：`我觉得试运行接口的依赖gap/错误等报告等应该通过一个report字段传送md文档给前端,这样可以直接展示`
  来源：2026-09-11 ses_f72091874ffe 首条原话；应走 spec，不因「接口」加载 debug

- **N3**（建立 `.local-env`）：`没有该目录或没有能够使用的start和url时应该自动引导用户创建`
  来源：2026-09-13 用户原话；应走 local-env，不因启动目录加载 debug

- **N4**（任务中断续写 → implement）：`刚刚发生了一些中断,继续执行任务`
  来源：implement 触发 P2；应走 implement，不因「执行任务」加载 debug

- **N5**（会话交接 → handoff）：`把当前任务移交给新会话`
  来源：2026-09-13 用户指令创建 handoff；应走 handoff，不因「移交」加载 debug

## 结果记录

| 日期 | 版本 | P1 | P2 | P3 | P4 | P5 | P6 | N1 | N2 | N3 | N4 | N5 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-09-12 | 用于段加本地访问地址的接口/服务 | 待跑 | 待跑 | 待跑 | — | — | 待跑 | 待跑 | — | — | — | 对照改后 description：P1/P2 含本地地址+调试/测试应触发；P3 仍为 bug 报告应触发；N1 实现入口、N2 加字段无本地地址不应触发 |
| 2026-09-13 | 负向补 `.local-env` | 待跑 | 待跑 | 待跑 | — | — | 待跑 | 待跑 | 待跑 | — | — | 对照改后 description：N3 建立/补齐 `.local-env` 不应触发 debug；P1–P3/N1/N2 锚点未改 |
| 2026-09-13 | 接手结论/无地址本地验证 | 对照 | 对照 | 对照 | 对照 | 对照 | 对照 | 对照 | 对照 | 对照 | — | P4/P5 对照用于段（按已有结论执行修复；本地验证不要求地址）；N4 对照负向 implement。干净会话 `opencode run` 连不上 api.x.ai，未实跑 |
| 2026-09-13 | 负向补会话交接 | 对照 | 对照 | 对照 | 对照 | 对照 | 对照 | 对照 | 对照 | 对照 | 对照 | N5 对照负向 handoff；P/N1–N4 锚点未改。干净会话未跑 |
| 2026-09-14 | 连体命令锚点 | — | — | — | — | — | ✓ | — | ✓ | — | — | — | 干净会话探针（Codex 子代理，rollout 取证，n=1，skills 仓库 cwd 有噪声）：P6 于 3 分钟探索后 cat debug+local-env SKILL.md（锚点生效）；N2 加载 spec+interview、未加载 debug（负向未过触发）|
