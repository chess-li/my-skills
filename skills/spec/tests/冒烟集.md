# spec 冒烟集

工作流行为面测试（触发面见触发测试集.md）。断言：加载 spec 之后的正文分支行为与产物。
语料优先真实会话原样摘录；跑法同触发测试集（干净会话发输入，查该 harness 的 skill 加载记录；opencode：`opencode run --dir <目录> --title smoke-<ID> "<输入>"`），另观测会话产物（写了哪些文件、走了哪个分支、告知语是否发出）。

## 用例

- **S1**（愿景级输入 → 讨论出口）：

  ```
  我想要自建一个harness,拥有dsh的架构可塑性及可插拔性, 并拥有cindy和multica的多harness/agent编排沟通
  ```

  预期：spec 加载；告知「目标轮廓未成形：先讨论，不落文档」；产物为会话内决策清单（未决决策整批列出、每条附推荐方案）；零断言文档写入（无 docs/ 任何文件落盘）。
  来源：2026-09-01 opensource/harness 会话（ses_fa3ba5c7dffegxKznNY0MXaDbz）第二条原话，当日真实会话被 spec 文档流程吸收、用户 92 秒后纠正「我觉得可以先不从spec开始，而是先从讨论开始」（返工事件）；2026-09-01 讨论出口条款落地后建此用例。
  环境保真：原现场前序有 7 仓源码阅读与分析上下文。**单发跑法复现失败两次**（2026-09-02：空目录一次、目录含七仓符号链接+真实总结文件一次）——语境饥渴使开场分类被事实追问接管，spec 未加载、出口未到达（观察名单「干净会话回归覆盖不了依赖累积上下文的退化」又一实例）。有效跑法=两轮预置：目录预置七仓符号链接 + 真实分析总结文件（原会话产物），第一轮令其读总结，第二轮发原话输入。断言只钉分支判别（spec 加载 + 告知语 + 决策清单 + 零断言文档），不钉讨论内容质量。

- **S2**（部分已明不得整批跳过访谈）：

  ```
  生成的skillCode应该是30位以内且以字母开头,只包含数字字母下划线
  ```

  预期：spec 加载；澄清访谈若已列出「登记是否同样校验」未决，不得因生成格式已明确整批跳过；须问 add 是否同样校验后再落断言。
  来源：2026-09-06 ey-timp-lab ses_f8b781b8fffe 首条原话（返工事件：写出登记未定后「访谈跳过」）。

- **S3**（失败面枚举）：

  前置：写含多个失败入口的能力的验收标准（如试运行：配置校验失败、预检失败、超时、中断/取消、外部写存储失败）。
  语料（非会话原话，git 取证）：2026-09-10 ey-timp-etl 智能体节点评审循环——试运行失败分支语义在评审中逐条补写：超时关 MCP（09-09 21:10/23:00）、快照落盘失败不改判（09-09 23:47）、取消与配置校验失败写过程日志（09-10 14:36/14:56）、中断分支携带缺口报告（09-10 19:31），spec 当天 5 个 docs 补丁提交。

  预期：动笔前列出失败入口全集并逐个给出断言；语义未定的失败入口进第 3 步访谈未决清单，不静默留白只写主路径与点名场景。
  来源：2026-09-10 返工事件。

## 结果记录

| 日期 | 版本 | S1 | S2 | S3 | 备注 |
|---|---|---|---|---|---|
| 2026-09-02 | 讨论出口版（豁免判断三路判断） | ✓ | — | — | S2 当时未建 |
| 2026-09-06 | 禁整批跳过访谈 | ✓ | ✓ | — | 对照改后正文：旧稿「决策都已有明确答案 → 跳过」可把清单剩余未决一起吞掉；新稿「任一条未决 → 不得因其余已明整批跳过」；S1 讨论出口未触不受影响；description 未改，触发集不跑 |
| 2026-09-10 | 写验收标准前先枚举失败面 | ✓ | ✓ | ✓ | 对照改后第 5 步：旧稿只要求每条用户故事至少一条可验证标准（采样式，失败面合法留白，评审循环替需求阶段补写 5 批断言）；新稿失败入口全集逐个断言、未定进访谈；S1/S2 未触（对照仍成立）；description 未改，触发集不跑 |
| 2026-09-24 | 收口发布 issue（第 7 步） | — | — | — | 对照改后第 7 步：新需求/能力收口走 issues 发布或更新（验收标准抄入验收清单、status: open 入队列）、执行中偏差不新建——与 issues 模板/frontmatter 状态机对照一致；S1–S3 未触（对照仍成立）；description 未改，触发集不跑 |
