# spec 触发测试集

修改 description 后必跑。断言：干净会话中是否加载了 spec（`skill` 工具调用 name=spec）。
语料优先真实会话原样摘录；跑法：`opencode run --dir <空目录> --title trigger-<ID> "<输入>"`，再查会话 DB 的 skill 加载记录。
工作流冒烟集欠账（等真实语料偿还）；本文件只覆盖触发面。

## 应触发

- **P1**（完整详细需求文档，新能力未落纸面）：

  ```
  # 增加一个智能体节点,使用AgentScopeJava2来完成基于智能体的任务

  ## 节点配置: 在formData中需要配置
  1. agentCode(必填).通过IAgentDubboService.getAgentConfigByCode(新增)获取agentConfig(新增),agentConfig中包含modelCode和systemPrompt.通过IAiModelDubboService.getModelConfigByCode(新增)来获取modelConfig. modelConfig中包含用于模型连接的modeName,baseUrl,apiKey
  2. skillCodeList(可选,如果没有填写则只加载内置的skillConfig).这是在本节点中自定义需要使用的skill.通过ISkillDubboService.getSkillConfigListByCodes(新增)和IAgentDubboService.getBuildInSkillConfigList(新增)来获取自定义和内置skillConfigList.skillConfig中包含skillFileCode.需要使用IPlatformStorage来下载对应skill-zip
  3. userPrompt(必填).自定义如何处理本次任务,userPrompt允许完整的el解析.
  4. inputContract(可选,如果没有填写,那么本次执行无输入执行参数).输入参数合约table.输入合约中包含(key(输入名),description(输入参数描述)).通过getRequestParam(key)(这是一个需要新增的agent tool)获取对应的参数值
  5. outputContract(可选,如果没有填写,那么固定输出List.of()到下一节点). 输出参数合约table.输入合约中包含(key(输入名),description(输入参数描述)). 在调用模型时需要依据outputContract让模型返回结构化json数据(map结构).在获取到结构化json-map后,需要包装成List.of(output)输出到下一节点.
  6. fileBusId(可选,如果没有填写,则禁用writeFileToCode,该节点无法输出文件到系统中)

  ## 全局配置:
  ### 增加etl.agent.sandbox配置
  model: NONE(不使用sandbox)/DOCKER(使用docker)/K8S(使用k8s-agentsandbox)
  properties: 用来配置不同sandbox需要的配置信息

  ### 增加etl.agent.workspace.busId配置
  1. 通过com.ey.cn.ti.datamart.util.HybridFileableUtil#path(java.lang.String, java.lang.String)来获取workspace的parentPath,该path已经包含租户信息
  2. 实际的workspace为<parentPath>/<etl-code>/<etl-request-id>/<agent节点code>/

  ## 内置agentTool
  getRequestParam(key),用于从请求参数中获取对应值
  getPrevNodeList(),用于获取当前节点前的所有节点名
  getNodeResultByNode(nodeName),用于根据节点名来获取节点执行结果
  getFileByCode(fileCode),根据文件code获取文件到workspace中
  writeFileToCode(file),通过IPlatformStorage将文件写入对应的fileCode中

  ## 通用规则
  1. 智能体节点只能操作自己的workspace
  2. 智能体节点不能直接输出文件地址/文件blob,必须通过writeFileToCode将文件写入文件服务,只能向下输出fileCode
  3. 智能体节点的错误都不能隐藏,必须抛出异常中断ETL执行
  4. 智能体节点的过程日志中必须包含所有的思考/工具调用/mcp/skill等及对应的token usage.还必须包含总的token usage.智能体节点的异常也应该写入过程日志中
  5. 智能体节点只能获取inputContract中配置的参数或者前序节点的执行结果.
  6. 智能体节点只能向下输出outputContract中描述的结构
  ```

  来源：2026-08-05 智能体节点设计会话（ses_02f456f7d）首条原话，旧版 description 下未触发（返工事件）
- **P2**（更新/迁移已有 spec）：`将docs/requirements/和docs/specs/ 下的文档按照spec-skill的标准进行迁移`
  来源：2026-08-05 文档迁移会话（ses_02fbe7635）原话，实际触发正确
- **P3**（删除 spec 已覆盖的已有能力，简短命令）：`删除 ey-timp-etl 中的 inputContract`
  来源：2026-08-24 ey-timp-etl 会话（ses_fcd2f114cffexa70IBK19koUlp）首条原话（绝对路径脱敏为项目名，防测试误改真实仓库），旧版 description 下未触发（返工事件）；跑时预置 `docs/智能体节点/智能体节点-spec.md`（含输入合约章节），工作目录命名 ey-timp-etl（目录名与输入指称不一致会制造找项目干扰，属测试假象）
- **P4**（已有能力迁移+扩展，完整验收细节在会话中给出）：`com.ey.cn.timp.etl.structure.nodes.PropertiesAgentConfig#outputContract 需要移动到com.ey.cn.timp.etl.structure.nodes.EtlNodeAgentFormData的outputConfig中,并增加"字段类型"字段,目前支持"文本" "数值" "JSON" "文件"四种类型. 智能体输出后需要校验,"文本"类型的值不能blank,允许程序自动将"大模型输出的数值"自动转换成"文本";"数值"类型的值不能为null,允许程序自动将"大模型输出地文本"自动转换成类型,如果无法转换,则视为本次智能体节点执行失败,需要记录其失败日志;"JSON"类型的值实质指的是"JSON文本",所以既不能为blank,又要能被json解析器解析成功,且不能是"空json";"文件"类型实质指的是"文件编码",需要根据模型输出的文件编码校验文件不能为空. 所有的输出key都必须存在,其值必须符合对应的类型要求,如果校验失败则视为智能体节点执行失败,需要记录其失败日志`
  来源：同会话第二条原话，旧版 description 下未触发（同一返工事件）；跑时预置同上

- **P6**（命令形态的已有能力行为变更：类全名+改为必填）：`com.ey.cn.timp.etl.structure.nodes.PropertiesAgentConfig#skillCodeList需要修改为必填`
  来源：2026-08-25 ey-timp-etl 会话（ses_fc7d3316）首条原话，当日真实会话 tdd 先加载、spec 补中、implement 缺席（返工事件）；跑时预置 `docs/contexts/智能体节点/智能体节点-spec.md`（含 skillCodeList 可选断言），P6b 起加 PropertiesAgentConfig.java 源码桩以观测 spec 后链路
- **P7**（命令形态新能力追加：一句话给出接口与返回构成）：`智能体节点需要增加一个用于预览所有提示词的接口,该接口的请求参数为智能体formdata,返回结果为agent对应的系统提示词(systemPrompt)+智能体节点配置的用户提示词(userPrompt)+生成的上游节点及输出契约提示词(programPrompt)`
  来源：2026-08-25 ey-timp-etl 会话（ses_fc6c1074）首条原话，当日真实会话零 skill 加载（返工事件）；跑时预置真实项目 AGENTS.md 与 DOMAINS.md、`docs/contexts/智能体节点/智能体节点-spec.md`
- **P8**（完整需求一句话新接口，**无 docs 无 spec 项目**）：`我觉得现在需要增加一个接口,该接口接受skill-zip-file-code,根据fileCode获取skill的zip包,并解析出其中的skill name和skill description,还需要自动根据skill-name生成skill code`
  来源：2026-08-26 ey-timp-lab 会话（ses_fc27145adffeI5ngT22EDuJns2）首条原话，当日真实会话门禁存在性条件假阴性、零 spec 加载直出 8 write+25 edit（返工事件）；跑法：git 检出 141b80a（squash 提交 4ad1821 之父，2026-09-01 起取代手工删产物，更保真）还原改前状态；**P1 因 datamart 工程不在本机无法保真复跑后，本用例是无 docs 象限唯一防线**；2026-09-01 起预期扩展：spec 判到无 DOMAINS.md → 路由 domains 冷启动判管辖（建最小 DOMAINS.md + AGENTS.md 引用行，或按 domains 正文豁免出口返回）

**环境保真约定（2026-08-25 P7 取证后立）**：①跑时预置项目真实 AGENTS.md——常备层竞争模板（计划优先等）是真实会话触发失败的复现必要条件，无竞争的干净目录会假通过（P6 旧版测试通过而真实会话失败）；②类全名形态输入（P6）须预置对应源码桩且为请求所求变更的改前版本（git show 取历史版），否则模型离目录找真实仓库、触发窗口被污染；桩为改后版本时请求本身已被满足，零加载是正确行为而非触发失败。

## 不应触发

- **N2**（变更评审 → code-review）：`进行独立评审`
- **N3**（bug 修复 → debug）：`修一下这个 bug，订单打折后总金额差一分钱`
- **N4**（纯重构，能力行为不变）：`我觉得com.ey.cn.timp.etl.structure.nodes.PropertiesAgentOutputConfig.OutputField需要提升到com.ey.cn.timp.etl.structure.nodes.PropertiesAgentOutputField`
  来源：同会话第三条原话，当日实际零 skill 加载；断言仅 spec 不触发（加载 tdd 等不判失败）

## 边界观察（不做硬性断言）

- **B1**（spec vs design）：`XDubboService是历史兼容名称,实际已将其实现迁移到feign,所以不要使用@DubboReference注解,而是使用@Autowired`
  来源：同上设计会话原话。技术决策修订属 design；记录实际行为供后续判断
- **B2**（追加新能力：spec 更新先行，implement 随后）：`我想要为智能体节点增加试运行功能`（跑时预置 `docs/contexts/智能体节点/智能体节点-spec.md`）
  来源：2026-08-05 试运行会话原话。追加新能力需先补 spec 章节——加载 spec 是正确行为，不属过触发；implement 是否随后接续由 implement 触发测试集跟踪

## 结果记录

| 日期 | 版本 | P1 | P2 | N2 | N3 | B1 | B2 | 备注 |
|---|---|---|---|---|---|---|---|---|
| 2026-08-05 | 旧版（无"会话中给出完整需求"澄清） | ✗ | ✓ | — | — | — | — | 真实会话 ses_02f456f7d 未触发，用户问"为什么不先建立spec?"（返工事件） |
| 2026-08-05 | 新版（加澄清） | ✓ | — | ✓ | ✓ | — | ✓ | 干净目录跑；P1 加载 spec（8s）；N2 加载 code-review、N3 加载 debug（spec 未过触发）；B2 加载 spec 并发起澄清访谈（正确先行），implement 接续与否未观测（单发会话止于提问） |

2026-08-24 扩集（P3/P4/N4 入场）后另表记录：

| 日期 | 版本 | P1 | P3 | P4 | N2 | N3 | N4 | 备注 |
|---|---|---|---|---|---|---|---|---|
| 2026-08-24 | 旧版（"改动改变了 spec 已写过的断言"） | — | ✗ | ✗ | — | — | — | 基线复现当日失败：零 skill 加载（跑时目录名 trigger-etl 与输入指称不一致，模型找项目被外部目录权限拦截，但触发决策窗口已过，零加载成立） |
| 2026-08-24 | 新版（产物管辖锚点） | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | P3 加载 spec 并正确执行（删章节+变更历史、非 git 目录不提交）；P4 加载 spec 进澄清访谈（一次一问附推荐）；P1 无回退；N2→code-review、N3→debug、N4 零加载，spec 均无过触发 |
| 2026-08-24 | 迁移版（+迁移提议锚点） | — | ✓ | ✓ | ✓ | — | — | ✓ | P2 ✓ 且迁移正确执行（直挂→contexts/、变更历史追加、非 git 不提交）；P4 ✓ 且主动提议迁移老布局文件（「检测到就提议」当场生效）；N4 零加载无过触发；四跑共用 mock 目录并行存在读写竞态，触发断言不受影响 |
| 2026-08-25 | 路由版（tdd 入口路由 + spec 落盘步 implement 接续） | — | — | — | — | — | ✓(N4r) | P6 ✓ spec 先行、全程无 tdd 抢跑（干净会话下 spec description 直接承接，tdd 入口路由条款未被行使——该条款防御的真实会话 tdd-first 路径干净会话复现不了，属观察名单 #12 已知局限）；P6b（含源码桩）✓ 全链路 spec→implement→tdd→code-review 按时序加载 + `docs/tasks/skillCodeList必填.md` 任务文件创建（code-review 裁决「修复后重审」并正确把预置 spec 的既有失真断言路由为范围外问题）；N4r ✓ 零加载无过触发 |
| 2026-08-25 | 编码请求拦截版（首条锚点+负向边界按阶段划界） | — | — | — | — | — | ✓(N4r-newdesc) | 环境首次预置真实 AGENTS.md 竞争模板：P7-old ✗ 零加载（基线复现 22:07 真实失败）→ P7-new ✓ spec 加载判管辖进澄清访谈；P6（改前源码桩+改前 spec）✓ spec 先行（先改 spec 章节再改代码）；N4r ✓ 零加载无过触发 |
| 2026-08-26 | 类别全覆盖版（v1 锚点+功能/配置例示+debug 划界） | — | — | — | — | ✓(N3-v21) | ✓(N4-v21 零加载) | v2「所有代码变更请求」全称字面版 P7 0/2 退化（「变更」词汇缺口：新增类请求不自归类为变更；全称断言无分类力）→弃用；v2.1 补例示但挤掉「校验」→ P6 ✗（零加载直改代码）；v3 恢复 v1 锚点并扩例示（功能/配置）：P6-v3 ✓ spec 先行（澄清确认后输出 spec 与代码）、P7 继承 v1 结果（锚点结构未变）、N3-v21 ✓ debug 加载 spec 未过触发、N4-v21 零加载 |
| 2026-08-26 | 收窄定稿版（「任意系统能力变更」命令式+锚点、bug 让位 debug、负向补 prototype；正文入口判断→豁免判断） | — | — | — | — | ✓(N3-final) | ✓(N4-final 零加载) | 同日晚间 A/B 链：旧门禁（存在性条件）真实会话 ✗ → 删门禁 v3 独扛 P8 t1✗/t2✓ →「任意变更都要使用本skill」P8 2/2✓ N3✗ N4✗ →「先使用+加载前不自判」+豁免判断 P8✓ P7✓ N3 1/2 N4 0/2 → 架构收窄（bug 归 debug、重构属豁免面）后定稿回归：**P8-final ✓ spec 加载、P7-final ✓、N3-final2 ✓ debug 单独加载、N4-final ✓ 零加载**；附带发现：N4-final 中 tdd 被加载前自判豁免（reasoning 取证，观察名单） |
| 2026-09-01 | domains 冷启动路由版（正文「分解与读现状」无 DOMAINS.md → 走 domains 冷启动判管辖，取代「按直觉命名」） | — | — | — | — | — | — | **P8 ✓**：spec→domains 依序加载；冷启动建 DOMAINS.md（5 关键概念，含技能编码/技能文件编码反例辨析——正是原请求内的一处一词多义陷阱）+ AGENTS.md 引用行（不存在则创建）；零生产代码写入；术语沿用代码事实（ai_agent_skills 字段）。落盘提交步未完成属 opencode run 非交互截断假象，非触发层问题 |
