# domains 触发测试集

修改 description 后必跑。断言：干净会话中是否加载了 domains（`skill` 工具调用 name=domains）。
语料优先真实会话原样摘录；跑法：`opencode run --dir <空目录> --title trigger-<ID> "<输入>"`，再查会话 DB 的 skill 加载记录。

## 应触发

- **P1**（概念单元裁定）：`如果一次需求包含了多个领域的特性,会有多少个spec文档?`
  来源：2026-08-04 spec-skill 设计会话（ses_034155）原话，旧版 description 下未触发（返工事件）
- **P2**（概念定义）：`我觉得我想要的更近似于定义 what/why——用户故事、验收标准->从 spec 的约束推导出 how->agent 自主执行 ->按照spec进行验证`
  来源：同上会话原话
- **P3**（歧义信号，轻度补上下文）：`我们在讨论 spec 文档的设计：关于"目标已明确不触发"似乎很奇怪,这样的话spec文档不就和项目事实脱节了吗`
  来源：同上会话原话 + 一句场景前缀

## 不应触发

- **N1**（邻近 skill：skill-creator）：`我想从零创建一个新 skill,用于自动生成提交信息`
- **N2**（机械执行）：`帮我跑一下 scripts/install.sh,告诉我结果`
- **N3**（任务执行，无概念讨论）：`给登录表单加个校验,邮箱必须包含 @`

## 路由触发（非用户输入直触）

- **R1**（spec/design 路由冷启动）：项目无 DOMAINS.md 时由 spec 正文「分解与读现状」/ design 正文「读现状」路由过来冷启动；用户输入直触面无锚点，验收走端到端——见 spec 测试集 P8（2026-09-01 ✓：spec→domains 依序加载，建 DOMAINS.md+AGENTS.md）

## 边界观察（不做硬性断言）

- **B1**（skill 创建 + 概念关系混合）：`我觉得似乎还需要一个用于记录描述任务目标/如何验证任务目标的skill,这个想法和规格驱动开发是一致的吗`
  来源：同上会话首条原话。skill-creator 应触发；domains 是否并行触发均可，记录实际行为供后续判断

## 结果记录

| 日期 | 版本 | P1 | P2 | P3 | N1 | N2 | N3 | B1 | 备注 |
|---|---|---|---|---|---|---|---|---|---|
| 2026-08-04 | 旧版（不用于业务功能开发本身） | ✗ | ✗ | ✗ | — | — | — | ✗ | 真实会话 ses_034155 全程未触发（返工事件） |
| 2026-08-04 | 新版（任何讨论中…概念定义、命名选择） | ✗ | ✗ | ✓ | ✓ | ✓ | ✓ | 无skill | 干净目录跑，无工作区 AGENTS.md，只测 description 本身；N1 实际加载了 customize-opencode（环境伪影，非 domains 过触发）；P1/P2 reasoning 显示模型把裸概念问题判为"可直接回答"，此类靠 AGENTS.md 常备层覆盖 |
| 2026-09-01 | +可达性条款（spec/design 判到无 DOMAINS.md 路由冷启动） | — | — | 见备注 | ✓ | ✓ | ✓ | — | 过触发防线 N1（加载 skill-creator）/N2（零加载）/N3（零加载）domains 均未误触；P3 行为变化（spec 加载、domains 未触发）——对照组 P3-ctrl（改前 description）加载 skill-creator 亦未触发 domains，归因=08-26 spec description 定稿后竞争场漂移（P3 自 08-04 后未复跑），非本次追加条款（纯追加未删锚点）；P3 会话中疑问经解释消解、无术语需沉淀，行为结果可接受；meta 讨论形态触发竞争入证据日志观察名单 |
