# skill 证据日志

追加式，一行一条。写入者：agent（执行后告知级）；升格须用户一字批准。

## 观察名单

- 归档后 worktree/任务分支残留直到用户发起合入：2026-09-09 把拆树/squash 从第 7 步迁到合入节 | 已结案：同日用户裁定完整评审可合入后必须问合入/保留 worktree/丢弃，不再静默保留
- implement description 未加「合入任务分支到基线」锚点：合入口径只在已加载 implement 时可达。ses_f7ad71bd「合并智能体节点到 feature-2.5.0.10」当时零加载 implement。拉动条件：下一次独立合入会话仍不加载 implement
- 「探讨 skill」候选（2026-09-01 用户发起评估，证据=同日返工事件 ses_fa3ba5c7）：机制诊断=病灶在路由不在探讨能力——用户纠正后开放探讨质量无需 skill 守护（决策点附推荐、逐块细化、末尾给拍板项均自发完成），假想正文大部分过不了审计四问第 1 问；独立 skill 若 model-invoked 则与 spec 在同一输入形态（愿景级能力请求）双声称→触发竞争（spec 触发面系 08-26 精细调定），若 user-invoked 则入口仍被 spec 触发必达先行拦截——两路均须先补 spec 探讨出口 | 修法候选=spec 加「探讨出口」：目标轮廓未成形或用户明确要先探讨 → 不落断言文档、产出决策清单、收敛后回分解与读现状（升格须用户一字批准） | 拉动条件：第二次「愿景级输入被 spec 吸收后用户纠正要探讨」现场，或探讨出口落地后仍出现探讨质量/产物去向返工 → 再裁独立 skill | 2026-09-01 用户裁定：不建独立 skill，「讨论出口」条款一字批准落地 spec（见升格区）；独立 skill 拉动条件保留待验证
- meta 讨论形态（关于 spec/skill 设计的概念讨论）下 domains description 层被相邻 skill 竞争吸收：2026-09-01 回归 P3（「我们在讨论 spec 文档的设计…」原话）两轮分别加载 spec、skill-creator，domains 均未触发——对照组（改前 description）同样未触发，归因=08-26 spec description 定稿后竞争场漂移，非当日追加条款；设计中此类讨论由常备层（项目 AGENTS.md 指针）覆盖，而该指针恰是 09-01 冷启动改动后才会被创建——无指针项目的 meta 讨论仍是 domains 盲区 | 2026-09-01 回归取证 | 已结案：2026-09-07 用户指令 fix 3——description 加加载前不自判、去掉与 interview 重叠的「一词多义/理解偏移」（见升格区）

- S1 常备层门禁正确版与 S4 hook 延后：双层概率网（spec 请求级 description + tdd 动作级 description/B 条款）已覆盖全部三场真实翻车路径（tdd 在 fc7d3316、fc6c1074 待核、18:12 ses_fc27145a 中均先于写码加载，旧 B 条款存在性条件静默、修复后会截回 spec）；S1 残面=两层同漏（暂无实例），S4 拦截面与 tdd 覆盖面重叠 | 2026-08-26 用户裁定延后 | 拉动条件：真实会话出现「spec 与 tdd 双双未加载」的写码翻车
- tdd 动作级触发对「小重构」形态概率性 + 加载前自判豁免：trigger-N4-final 中模型 reasoning 显示判断事件发生但自判跳过（"this is a pure refactoring, so tdd might not strictly apply"，随后按项目 AGENTS.md 计划优先模板走）——与 spec 已废止的「加载前自判豁免」同构缺口，tdd 无对应条款（其无行为变化分支要求前后全绿，并未豁免加载本身）| 2026-08-26 取证（该 mock 无测试设施，模型判断有局部合理性）| 已结案：2026-09-07 用户指令 fix 3——description 加「是否免新失败测试由正文判定，加载前不自判」，正文无行为变化分支钉死仍加载（见升格区）

- 锚点写盘版本指纹：客户端载入页面时计算内容 hash，保存/包盒/删除请求携带，服务端对当前磁盘文件校验不一致即拒绝并提示「页面已过期，请刷新」 | 来源：2026-08-23 cake prototype.html 双文档污染事件（见同日返工事件）；正文已先加「写盘防护」提示条（改文件后先刷新再保存），机制化防护待用户裁定 | 待验证：用户是否拉动建指纹校验

- 锚点可视化 tooling 候选（2026-08-20 cake 会话）：术语→DOM 映射只保 agent 可 grep，人没有视图——用户报告「不知道哪个功能对应哪个锚点，无所适从」。原型内已落地锚点视图模式（A 覆盖标签/B 锚点清单/C 层级条 + 会话内手动添加与片段导出）供挑选 | 2026-08-23 补注：机制化由用户指令拉动——落成 `packages/prototype-anchor`（Node 无依赖，npx 分发：默认 stdio MCP 供 agent、serve 子命令 HTTP 供页面；写盘按绝对路径+白名单 ~/workspace+AM_ALLOW；项目根=向上找 docs/contexts，术语表自动同步）；页面探测链 同源→127.0.0.1:8420，file:// 传绝对路径；opencode 全局 MCP 配置已写入（npm link 本地等效，publish 待定）；HTTP/MCP/安全面均实测通过 | 待验证：变体 A/B/C 用户挑选后收编（2026-08-27 补注：C 层级条经用户指令移出、A/B 留存，挑选部分闭环，见升格区同日条）；npm publish 决策；prototype skill 正文是否引用此工具（一字批准）；cake 旧 serve.py 已被覆盖（同源兜底保留，去留待用户）

- 差值原则假设模型先验恒定，跨模型/版本差值静默漂移 | 2026-08-04 审查讨论 | 待验证：换模型或模型升级后原测试通过的 skill 退化（harness 已记 model.change 事件行，证据会自动到场） | 2026-08-06 补注：model.change 通道当日实际接通（观测基础设施落地，见升格区）；此前"harness 已记"名存实亡——harness 当日才真正存在 | 2026-08-26 补注：model.change 取证通道随 hook 能力删除关闭（见同日移出条），该项退化为纯人工观察——换模型后人工重跑触发测试集
- 干净会话回归覆盖不了依赖累积上下文的退化（上下文臃肿、skill 间冲突类病灶） | 2026-08-04 审查讨论 | 待验证：测试集全绿但真实长会话翻车
- 观察名单只进不出，无移出规则 | 2026-08-04 审查讨论 | 待验证：名单膨胀到取证困难
- "证据闸"一词本身挂在选词双闸·闸二（"闸"夹带二元开合的副作用），5 处引用，改名是跨文件改动 | 2026-08-04 双闸落地时发现 | 待验证：出现因"闸"的二元联想而漏记观察名单项的实际案例
- spec skill 冷启动创建（skills/spec/）：无返工证据，经设计讨论由用户批准落地；冒烟集按用户决定推迟到有真实反馈再建 | 2026-08-04 创建会话 | 待验证：触发边界过/欠触发、"spec 保持薄"护栏、BC 划分跟随 domains 规则在真实任务中的表现；冒烟集欠账需真实语料到场后偿还 | 2026-08-05 真实使用：欠触发发生一次（见同日返工事件）；薄度保持与 BC 划分跟随 domains 正常
- design skill 冷启动创建（skills/design/）：无返工证据，经设计讨论由用户批准落地；冒烟集按用户决定推迟到有真实反馈再建 | 2026-08-04 创建会话 | 待验证：决策归属路由（design vs ARCHITECTURE.md）误判、"design 保持薄"护栏、与 spec 的边界配合；冒烟集欠账需真实语料到场后偿还 | 2026-08-05 真实使用：归属路由（节点内决策→BC design、运行边界→ARCHITECTURE.md）与偏差路由（Feign 修订）均正确触发，未见误判
- Tasks 层 skill（任务拆解与执行顺序）：本次会话用户明确推迟，不展开 | 2026-08-04 design skill 讨论 | 已解决：2026-08-04 implement 设计会话（见升格区）
- implement 与任务契约（验收卡）词条形态高度相似（均为一次性、记目标与验证），仅"是否驱动执行"分界 | 2026-08-04 implement 设计讨论 | 已结案：2026-08-04 术语表消费者审计，任务契约词条零消费者被删，混用不再有发生面
- spec/design 的 description 未加指向 implement 的负向边界条款 | 2026-08-04 implement 设计讨论 | 待验证："开始实现"类输入出现误触发/欠触发再补 | 2026-08-05 真实使用："提交并进入design/进入实现"类转场触发正确；但 implement 本体在追加功能场景欠触发（见返工事件） | 已结案：2026-08-25 欠触发证据到场（同日 ey-timp-etl fc7d3316：spec 补完后 implement 全程缺席），修法落定为正文体接续路由而非 description 负向边界（见升格区）
- git 时机约定（2026-08-04 落地）：无返工证据，经设计讨论由用户裁定三个决策点（隔离单位=任务文件、提交粒度=每条验收标准、落点=各 skill 落盘步骤）批准 | 待验证：每条验收标准一提交在真实项目中是否过碎；"合回主干 vs 开 PR"默认是否误判；冒烟集欠账随真实语料偿还 | 2026-08-05 真实使用：每条验收标准一提交（13 切片）执行无过碎投诉、归档合入成功；implement 未触发时默认"未经要求不提交"生效引发用户疑问（见返工事件）
- implement skill 质量缺口（2026-08-05 用户质疑+社区调研）：现有验收闭环只保功能正确，不保代码质量；对照 obra/superpowers、mattpocock/skills、genkovich/sdd 等社区 skill 集，共性纪律六道中 implement 缺四道半——测试先行、新鲜证据（本轮实跑）、断言质量、生成者≠评审者、项目级全量门禁、熔断均无强制 | 已结案：2026-08-05 用户裁定创建 tdd/code-review 两个 skill 并改 implement 接入（见升格区）；项目级全量门禁与熔断未落地，留待真实证据 | 2026-08-05 lint/typecheck 门禁随范围与整洁护栏落地 implement 完成步骤（见升格区）；熔断仍待证据 | 2026-08-18 熔断证据到场（CE脚本会话同类失败连犯 3 次才机制化），熔断规则落地为 tdd 验证信道条款（见升格区），熔断项结案
- BDD 独立成 skill 之问（2026-08-05 用户提问）：结论暂不成立——测行为已在 tdd、验收可执行化已在 implement、仅剩 Given/When/Then 书写格式差值极小，且与 tdd 同时刻触发、无独立触发场景；社区亦无独立 BDD skill 先例 | 待验证：验收标准含糊导致返工、或结构化格式（Given/When/Then）实际降低歧义的真实案例；证据到场优先考虑并入 spec 验收标准书写约定，而非独立 skill
- 过度设计缺口（2026-08-05 用户提问，未附真实案例）：现有护栏只拦范围外加法（implement 范围护栏、code-review「无未请求的额外功能」），验收标准之内的多余抽象、投机泛化、冗余配置/扩展点无人拦截；code-review「复杂度」仅清单词无判据 | 已结案：同日用户报告证据到场（多层代码/插入入口），用户批准 tdd+code-review 双落点（见升格区）
- 追加型需求会话中 how 层执行计划先于 spec 更新发出（试运行会话），"spec 是变更第一现场"的顺序倒置，用户未指出 | 2026-08-05 真实会话观察 | 待验证：是否复发、是否值得立规则
- 触发测试用例设计教训：正/负例分类隐含"上游产物完备"前提——追加型输入（新增能力）加载 spec 是正确先行行为，误列为 spec 负例会制造假缺陷信号；跨 skill 接续断言（spec 之后 implement 是否接续）单发会话测不了，需多轮或真实使用 | 2026-08-05 触发回归发现（spec-n1 误分类当场纠正） | 待验证：是否值得升入 testing-guide
- design 层是否挣得独立（2026-08-05 用户质疑「design 很薄，可否并入 spec」）：结论暂不合并——implement 偏差路由对两层不对称（spec 偏差停下问用户 / BC design 偏差先改文件再实现）、debug 三层路由、不触行为的纯技术决策归宿、ARCHITECTURE.md 范畴归属均依赖分层；薄是设计形态（不产生值得固化的决策即空满足）非病灶 | 待验证：真实项目中 design 长期只空满足、偏差路由从未触发 → 证据到场再裁合并或移出 | 2026-08-05 真实使用：design 偏差路由当日两触发（Feign 命名修订、端口注入策略修订），"长期空满足"反例到场（单案）
- writing 是否管 SKILL.md 等 skill 文件措辞（2026-08-17 用户提出"似乎也可以"）：现负向边界为有意划定——SKILL.md 措辞已有专属且更强的治理（design-principles 选词双闸、审计四问、祈使句/单一信源），writing 的招募顺序与双闸是两套机制，同载将规则打架、重复占上下文 | 待验证：编辑 SKILL.md 时出现 design-principles 管不住的措辞返工（套话、翻译腔、中英混排、em-dash 等纯表达层问题），证据到场再裁是否收窄负向边界（候选形态：仅「语言别条款」对 skill 文件适用）；writing 触发测试集已加 B2 观察用例
- prototype skill 候选（2026-08-19 用户发起评估）… | 已结案：2026-08-19 用户裁定创建（见升格区）
- 社区 engineering/prototype skill 调研（opensource/skills 仓，2026-08-20）：…候选移植机制：①生成侧锚定 ②throwaway capture ③每步操作后展示完整状态 | 部分结案：2026-08-20 用户批准移植①②（见升格区）；③LOGIC 支仍待真实逻辑原型场景拉动
- skill-creator 移植纪律「合入后过双关」… | 已结案：2026-08-19 用户批准改「两道检查」（见升格区）
- skill-creator 升格标准双表述… | 已结案：2026-08-19 用户批准统一为「一字批准」（见升格区）
- skill-creator 迭代工作流步骤未写完成物… | 已结案：2026-08-19 用户批准四步各补完成物一句（见升格区）
- skill-creator 诊断表仅列「错误修法」、无「正确方向」列 | 2026-08-19 审查 | 待验证：agent 把错误修法列当操作指南执行
- skill-creator 冒烟 S2–S5 与触发测试集结果区仍空；统一 description 过/欠触发、分路是否被跳过仍属 2026-08-19 升格待验证 | 2026-08-19 审查 | 待验证：独立会话跑通 S2–S5 与 P/N/B 全表
- prototype 锚点信息框「保存」在术语名/锚点值缺一时静默 return、无任何反馈，现象同为「无法保存」| 2026-08-24 审查附带发现 | 已结案：2026-08-24 真实用户卡住证据到场（见同日返工事件「保存按钮没有作用」），anchor-view.js 两处保存处理器加空值提示 toast + 聚焦缺失字段，jsdom 冒烟 8/8 通过；~/.agents 与 skills/ 双副本同步
- 项目 AGENTS.md「禁止提交 git commit」类条款与 spec/design/implement/domains 的落盘即提交约定正面冲突时，skill 无显化机制（静默遵守一边）| 2026-08-24 ey-timp-etl 会话诊断附带发现：当日冲突未实际开火（skill 零加载，冲突没机会发生），candidate 修法=skill git 行加「项目文档禁止提交时报告冲突请用户裁定」| 2026-08-26 补注：用户级 AGENTS.md「冲突裁决」段已落地 | 已结案：2026-09-07 真实开火（宿主「未经要求不提交」vs skill-creator 落盘即提交，agent 静默遵守宿主不提交也不问）；修法=skill-creator 钉死事先授权仍提交 + 冲突裁决例外（见升格区）
- spec 迁移能力（2026-08-24 创建：布局+结构改写、并入 spec skill、检测到就提议，见升格区）：无返工证据，经设计讨论由用户批准落地 | 待验证：真实项目迁移首用（ey-timp-etl 的 docs/<上下文>/ 直挂为首个候选现场）、检测提议的过/欠触发、「项目自成一体布局不擅动仅报告」的边界误判
- 触发测试环境保真两条（2026-08-25 P7 取证发现）：①跑时预置项目真实 AGENTS.md——常备层竞争模板（计划优先等）是真实会话触发失败的复现必要条件，无竞争干净目录会假通过（P6 旧版测试通过而真实会话失败）；②类全名形态输入须预置对应源码桩且为改前版本（git show 取历史版），否则模型离目录找真实仓库、触发窗口被污染；桩为改后版本时零加载是正确行为而非触发失败 | 已落地：spec 触发测试集「环境保真约定」节 | 待验证：是否值得升入 testing-guide 语料采集/覆盖结构节（其他 skill 测试集复用）
- 「所有/任何」式全称断言在 description 触发判别中无分类力：模型在触发时刻靠具体类别锚点（加/改/删接口、字段、校验…）做请求分类，「所有代码变更请求」字面版 P7 0/2 退化（另叠加「变更」词汇缺口：新增类请求不自归类为变更）；v2.1 例示挤掉「校验」即丢 P6——例示清单不是修辞是触发面本体，增删每一项都改变覆盖 | 2026-08-26 v2/v2.1 回归失败证据（见 spec 触发测试集结果区）| 待验证：是否值得升入 design-principles 触发节，与「不枚举用户说法」并读（类别例示≠枚举说法）
  - 2026-08-26 reasoning 取证补强（opencode.db reasoning 部件）：失败会话（ses_fc6c1074、trigger-P7-v2b）全部 reasoning 零 skill 适用性评估——「判断失误」「觉得没必要」两假设均不成立，根因是**判断事件不发生**：请求开场被项目 AGENTS.md「计划优先」模板单遍分类接管（reasoning 原文「This is a multi-step feature request」「per AGENTS.md（计划优先）」）；成功会话（trigger-P6-v3）reasoning 显示具体锚点（字段/校验）直接命中，且仍闪现比例性豁免本能（「But this is a small change…」）——锚点是 skill 层唯一的打断杠杆（概率性），常备层指针是结构干预
- 单一写者/双事实源原则：同一资产出现两个写者或两个事实源必出污染或倒挂（2026-08-20 spec 追原型倒挂、08-23 cake 双文档污染、08-24 术语表程序写入三案，个案均已升格机制，通用原则未提炼）| 2026-08-26 两周任务审阅归纳 | 拉动条件：第四起双写者事故 → 提炼 DOMAINS.md 词条或 design-principles 原则
- 安装副本/仓库副本漂移：2026-08-26 spec 收口时发现仓库停 v3、安装副本停实验版（三副本 md5 对齐才收口）；副本同步目前靠人记 | 2026-08-26 两周任务审阅归纳 | 拉动条件：第二次漂移 → 立同步机制（install.sh 兜底或落盘步加同步校验）
- bootstrap 冷启动「最小集 3–10 条」与 domains 冷启动「提取 3–5 个最关键概念」数字不一致：DOMAINS.md 冷启动规范双信源（呼应 #44 单一写者原则） | 2026-08-26 全量 review 发现 | 待验证：真实冷启动现场因数字不一致产生迟疑或返工；修法候选=数字归 domains 单一信源，bootstrap 引用
- prototype 触发/冒烟测试集欠账、语料已成熟：08-19 创建留 4 轮原话种子，08-20/23/24 多轮真实使用与六连返工已到场，testing-guide 冷启动条件已满足未偿还 | 2026-08-26 全量 review 发现 | 拉动条件：下次 prototype 改动或真实触发翻车时顺带建集（种子语料见 08-19 创建条与 08-23~24 返工事件）
- writing 冒烟集仍为创建期拟真语料（T01–T05 文内自述待真实语料逐条替换），冒烟与触发两结果记录区全空 | 2026-08-26 全量 review 发现 | 拉动条件：真实写作任务语料到场逐条替换；触发集 B2 用例与「writing 是否管 SKILL.md 措辞」观察项联动取证
- bootstrap 冒烟集 S1–S5 结果记录区全空（2026-08-05 建成至今零记录） | 2026-08-26 全量 review 发现 | 拉动条件：bootstrap 下次改动或真实初始化任务时首跑补记
- DOMAINS.md 任务文件词条「只承载四件事」与 implement 任务文件模板六节表述缝隙：模板含「当前位置」（续写机制核心，入口判断续写分支依赖）与「变更历史」，词条列举未覆盖 | 2026-08-26 全量 review 发现 | 待验证：接手者按词条理解任务文件产生困惑的实例；修法候选=「当前位置」归入验证状态或词条列举对齐模板 | 2026-09-14 补注：缝隙实为三处三个数——implement 意图锚点自称 3 件（目标/验收清单/验证状态）、词条 4 件、模板 5 节（目标/验收清单/当前位置/变更历史/范围外问题）
- squash 同一 spec 本地任务提交的安全协议（2026-08-28 ey-timp-lab ses_fb91594feffe，用户手动实践成功）：patch 式 squash——生成 patch、原分支改名 -back 保留、从远程重新检出、应用 patch、新旧分支等价比对为空才提交并删 back | 复发性结构性保证（implement 每验收标准一提交，本案 56 碎提交），但无返工证据（用户手动执行成功，agent 仅做等价比对与删除判断），不满足证据闸 | 待验证：第二次 squash 现场 agent 默认行为是否偏离协议（如直接 rebase -i 原地改写不留 back、无等价比对即删原分支）；证据到场 → 落入 implement git 节 | 已结案：2026-09-01 第二次现场达成（ses_fa38c8f88ffe 用户再次手动 squash 9+7 两批，见同日返工事件），patch 式五步钉死升格 implement 完成与归档（见升格区）
- implement 第 6 步（完成与归档）补丁堆叠：09-01~09-07 连续把 squash、前端对接、声称核对、死参数、删任务分支、Minor、合入前完整评审压进同一步；skill-creator「每 5–10 轮迭代整体重构」窗口已过。09-07 四条返工是规则缺失不是漏读已有句 | 2026-09-07 全量 review | 已结案：2026-09-07 用户指令 fix 1，拆成第 6 步合入前核对 + 第 7 步归档（见升格区）
- prototype-anchor-sync 无测试集：08-24 创建至今无冒烟/触发集。诞生现场语料即 08-24 写入权收归 | 2026-09-07 全量 review | 拉动条件：下次改动或真实同步翻车时顺带建集 | 已结案：2026-09-12 冷启动冒烟 S1–S3（user-invoked 无触发集）
- debug 冒烟集仍未建：08-05 创建升格条「冒烟集欠账」未迁观察名单、目录无 tests/。此后有真实使用（08-18 依赖源码定位已升格正文） | 2026-09-07 全量 review | 已结案：2026-09-12 本地接口实测翻车拉动，冷启动 S1–S5（09-08 五场原话；08-18 源码定位正文未改未单列）
- 2026-09-07 触发划界三处（domains 去一词多义/interview 负向对偶/tdd 加载前不自判）的干净会话回归至今未跑：domains 触发集 7 列待跑、interview P1/N1 待跑、tdd 边界对「tdd vs 小重构」未跑，升格区自述「干净会话未跑」但当时未迁观察名单 | 2026-09-11 全量 review 发现 | 拉动条件：下次任一相关 description 改动时强制清偿，或用户指令补跑 | 已结案：2026-09-12 干净会话补跑（见升格区）
- tdd「加载前不自判」与请求级不抢入口对冲：2026-09-12 干净会话 spec N4 原话 tdd 先于 spec 加载（与 08-26 T1 目标相反），随后 B 条款截回 spec、未跳过 tdd。拉动条件：真实小重构会话 tdd 抢在 spec 前写码
- 「无未处理发现」一语两读：DOMAINS.md 归档/合入词条与 implement 意图锚点按字面读 = 零发现放行；code-review 2026-09-11 起裁决门槛 = 无未处理的 P4 及以上（P5 经报告不阻断）。09-11 升格条自述按 code-review 处置语义解读、词条未改——单读前两处者得到更严的完成定义 | 2026-09-11 全量 review 发现 | 待验证：P5 放行与字面完成定义冲突引发的真实争议（如归档/合入时对 P5 处置的分歧）

- eli5 缺「没讲懂」修复路径：用户反馈没看懂时缺省行为只是原样重讲一遍，无降档/换类比/借用项目统一语言（DOMAINS.md）的机制 | 来源：2026-09-14 移植分析 matt-skills wait-what（re-pitch 机制：补上下文 + 简化语言 + CONTEXT.md 统一语言）| 拉动条件：真实使用出现一次「没看懂 / 讲的什么」类反馈返工
- eli5 四档完成判据无检验机制：「能复述/能跑通/能指出动哪里」是 aspiration，产物本身不含检索练习或反馈环——teach 的 fluency vs storage strength 机制：知识类降难度、技能类靠 effortful retrieval（quiz + 即时反馈）| 来源：2026-09-14 移植分析 matt-skills teach | 拉动条件：出现「当时讲懂了、之后又不会」类返工
- eli5 判档不锚定学习目的：同一主题对「明天要做技术选型」与「纯好奇」应产出不同讲解；会用档场景未锚定用户真实目的，易滑向教科书式泛化场景（teach 的 MISSION.md 机制）| 来源：同上 | 拉动条件：出现「讲的场景跟我没关系」类返工
- eli5 高档产物（会用/所以然/改造）在对话内、会话结束即不可复习：teach 区分 lesson（一次性）与 reference（压缩参考卡，供日后查阅，术语表一旦建立各课遵守）| 来源：同上 | 拉动条件：出现「把刚才讲的整理成可查的东西」类返工
- eli5 概念类主题讲解无溯源要求：teach 明确 never trust parametric knowledge + RESOURCES.md 登记高信源 + 课内引用背书 | 来源：同上 | 拉动条件：出现讲解事实性错误返工

## 返工事件

- 2026-09-14 | agents-md | 创建当日用户即指出覆盖缺口：存量 AGENTS.md 与已安装 skill 冲突时无裁决纪律。现场=ey-timp-etl/AGENTS.md（395 行前 skill 时代 monolith，项目已具 DOMAINS/ARCHITECTURE/docs-contexts 全套）——「禁止提交 git commit」正面对抗 implement 提交区间机制与 skill-creator 落盘即提交；「计划优先·确认后再输出代码」+ Execution Plan Template 与 SDD 任务文件构成平行仪式，模板第 3 步 tests-last 措辞（同日 debug+tdd 条目已记其反向引导实锤）；Architecture Guidelines/Quick Reference 常量枚举/缓存 API 文档誊写 design 与代码（缓存节甚至誊写另一仓库 spring-cloud-ey）；另有 08-24 条目记其「无 skill 指针」 | 疑似病灶层：审计标准缺冲突检查维度——尺子只问「会过时吗」，不问「与已安装 skill 打架吗」；改造分类缺「显式覆盖」档

- 2026-09-14 | debug+tdd | ey-timp-etl Codex 线程 01a09a63-73ad（「验证并修复 ETL 智能体闭环场景」，rollout jsonl 取证）：入口「自动使用本地环境验证,自动修复」验证修复命令形态，全程唯一 skill 加载=local-env（10:52，立通道合规），debug（description 自称管「本地验证接口/服务」）与 tdd 均未加载；修复三批次均「测试先写、实现后写、中间不跑」——①15:15:09 写 AgentReadGuardFilesystemTest+AgentModelFactoryTest（timeout wiring），15:16:22 起连写 4 个生产文件（ReadGuard/Properties/Executor/GatewayFactory），15:22:18 首次跑测 BUILD FAILURE→Errors: 6（测试自身问题），15:23:17 改测试转绿；②15:45:03 写 AgentFileToolsTest，15:45:47 新建 LocalAgentWorkspace+改 AgentFileReadTool/WriteTool，15:47:44 全量 install 绿，中间零运行；③16:10:52/16:11:21 写 3 个测试，16:12:42 改 2 个生产类（截断标记），16:14:01 断言从 assertEquals(MAX) 放宽为 ≤MAX+marker（实现后适配），16:14:29 绿——RED 全程未以「功能缺失」形态被观察；reasoning 15:14:48 原文 "Plans TDD: new tests, extend agent factory test, create AgentReadGuardFilesystem…"——测试先行判断事件在场，缺席的是 skill 正文纪律（RED 实跑观察/一次一个垂直切片/断言不随实现改）；11:11 把 AgentNodePropertiesTest 断言 20→40 未向用户确认（上一会话遗留失同步，实质合理但确认程序缺席）；项目 AGENTS.md「计划优先」模板第 3 步「补充单元测试」=tests-last 框架反向引导。GREEN 侧无剧院：mvn install 441+ 全绿、E2E 六场景 curl 实测、产物逐一下载验证 | 疑似病灶层：①debug description 未认领「验证+自动修复」命令形态；②tdd 动作级触发在「修复动作发生」时刻未开火（请求面无编码信号，与 08-25 ses_fc6c1074 新能力命令同构、入口形态不同，09-07 description 修复未覆盖本形态）；③执行层缺正文纪律约束——自绘 "TDD" 只有「先写测试」没有「观察失败」；④项目常备层 tests-last 措辞（08-24「无 skill 指针」条目之外新增反向引导证据）

- 2026-09-13 | handoff | 用户纠正：不同 harness 有不同临时文件夹，不应固定 opencode 临时目录。现场=刚把路径钉成 `{tmpdir}/opencode/handoff/` | 疑似病灶层：完成物错锚——把某一 harness 的临时目录名写进契约

- 2026-09-13 | handoff | 用户指令创建会话交接 skill：写临时目录交接文档、生成提示词；能拉起可交互新会话则启动。避免新会话再查源会话。现场=ses_f66842526ffe 按会话 id 查库才拿到修法 | 疑似病灶层：无会话接力产物——任务文件只管 SDD 执行状态，会话间只有口头「按 ses_xxx 执行」

- 2026-09-13 | debug+implement | 用户审阅当日任务：fixbug 移交给新会话后不自动本地测试、不找根因。现场=ses_f66b32d4dffe 已定位推荐失败根因并给出修法；ses_f66842526ffe 首条「按照任务ses_f66b32d4dffepvqllLJ0aR6Wto的建议修改结论执行」加载 implement/spec/tdd，零 debug/local-env，0 次打本地接口，单测绿后合入。同日 ses_f697be333ffe「在本地环境中验证…testrun接口」亦零加载 debug | 疑似病灶层：description 判别逻辑 + 授权缺失——本地测试锚点写成「给出访问地址」，无地址则判断事件不发生；「按结论执行」无 bug 锚点被 implement 吸收；正文无「前会话根因只当线索」

- 2026-09-13 | debug→local-env | 用户补裁定：目录改为 `.local-env`；缺目录或 start/url 不可用应自动引导创建；独立成 skill。现场=刚把契约内嵌 debug 且缺入口只停下要两件 | 疑似病灶层：完成物错锚 + 拆分——立通道/引导创建与 bug 诊断不同触发面，内嵌 debug 则缺入口不会引导、建立目录请求被 debug 吞噬

- 2026-09-13 | debug | 用户审阅近一周任务：本地调试要自己启动程序或重复说明。现场=09-12 刚把「通道不立→停下要服务起来」钉死，启动仍留给用户。用户裁定：项目根 `local-env/`（不进 git，本机软链挡机器差），最低契约可执行 start + url 文件，agent 自己跑 | 疑似病灶层：授权与默认值缺失 + 完成物错锚——立通道的完成物是「用户把服务拉起来」，跨机器差异没有项目内锚点

- 2026-09-12 | debug+tdd | 用户补诉：进行本地测试时也不会自动刷新本地依赖仓库。现场=同日本地接口实测闭环刚落盘；09-08 ses_f804a55e 改完结语「宿主需重新安装本仓库后再打试运行」，把刷新留给用户。tdd「不经共享仓库的中间态」把单测信道与已启动服务加载混成禁止写入 | 疑似病灶层：授权与默认值缺失 + 完成物错锚——debug 第 5 步只写重启再打，未写「经本地依赖仓库加载则先刷新」；tdd 信道条款无「供已启动服务加载而刷新」除外

- 2026-09-12 | debug | 用户审视本周本地接口测试任务：工作总不太理想、不会自动修复。现场=09-08 五场 ey-timp-etl localhost:10003（ses_f811722e 服务已死仍 Arthas；ses_f810fcf6 十一轮催工厂/重启/「为什么不两条一起」；ses_f804a55e 用户补「你来自动调试」；ses_f8018e20 627 原文要追问、建目录未修；ses_f7f18c4c 追问原始输出/根因/如何修复×2）。用户裁定改 debug（do it） | 疑似病灶层：授权与默认值缺失 + 完成物错锚 + 判层误升——「调试/测试本地接口」未写成含修复的本流程；实现层（配置已写代码偏离）被标设计层/拿去问人；验证停在单测绿、等用户说继续；通道不立仍用运行时诊断；失败响应被摘要

- 2026-09-10 | spec+implement+code-review | ey-timp-etl 智能体节点评审修复循环（git 取证）：09-09 19:05 主任务归档（验收绿）→ 完整评审发现问题，21:09 建「评审修复」任务文件（采纳 6 条点名项）→ 09-09 21:19 至 09-10 20:28 全天 36 提交约 10 批修复仍未归档；任务文件 28 提交零更新，「当前位置」滞留 09-09「待 scoped 重审」（落盘纪律失守）。同类分批实锤：失败分支写过程日志三批（13:28 输出校验失败 → 14:36 启动前失败 → 14:56 取消与配置校验失败，提交信息两连「同样」）；缺口报告接线三批（19:31 超时与中断分支「同样携带」→ 19:42 中断接线 → 19:51 收敛 failureWithGaps）；测试专用物两批（14:36 重载、16:38 构造器）；探测健壮性两批（13:39 不依赖 --version、16:38 按存在判定）；spec 断言评审循环中补写 5 个 docs 提交（超时关 MCP、快照失败不改判、循环隔离、模型连接契约、缺口双来源）——评审在替需求阶段逐条补需求。用户问：为什么很多问题只在 full review 才发现，不能需求阶段或执行时处理掉吗 | 疑似病灶层：四层——①spec 无失败面枚举步，边界语义留给评审补写（需求阶段本可处理）；②验收清单按条验证，全称断言（「无论成功或失败」）一个实例测试即标绿，分支覆盖无闸（执行阶段本可处理）；③修复循环按实例修，缺 debug 同款「修这一类」同类归并（循环长度本可压缩）；④执行期无卫生闸，测试专用物/死代码/矛盾注释留给评审兜底（09-04 升格只进评审维度未进执行步）。并发/生命周期类（收集器线程安全、stateStore 按次隔离）属真·评审域，需求阶段无法断言

- 2026-09-09 | implement | 用户裁定完整评审可合入后必须询问：合入基线 / 保留当前 worktree / 丢弃该任务。现场=同日刚把合入口径改成「用户发起」，第 7 步默认不合入等于静默选了保留 worktree | 疑似病灶层：授权与默认值缺失——合入/保留/丢弃未写成必须先确认的决策点

- 2026-09-09 | implement+code-review | 近两天任务无 archive：lab 验收绿未归档；etl Review scoped 可合入后停（`智能体节点-评审修复` 滞留 `docs/tasks/`）；实现会话合入 feature 后停。用户裁定：归档≠合入；任务完成后自动完整评审、发现问题自动修、没问题则归档；病灶是 scoped 无问题后后续流程没有自动继续；合入前若无完整评审也要做 | 疑似病灶层：授权与默认值缺失——修复循环出口只写「回第 4 步」和「合入前须完整评审」，scoped 清零后没有「立刻再派完整评审→无发现则归档」；第 7 步把归档与合入熔成同一完成物，scoped 可合入被当成流程终点

- 2026-09-07 | implement | 本会话审查：归档合并有时不建议处理遗留、有时又问范围外。用户裁定：遗留询问只留归档合并、只问仍成立的未完成/待办；已消化进备查不问；不扩到第 7 步；问句去掉范围外，仍成立的范围外留汇总不问 | 疑似病灶层：授权错绑——S9 把范围外与未完成捆成同一问，并把单任务归档范围外未问当归档合并证据

- 2026-09-07 | skill-creator | 本会话审查报告用户看不懂（「我看不懂你的结论,review的结果不是应该说明哪些地方有问题吗」）；随后改 skill 不自动提交（「修改skill后不会自动提交」） | 疑似病灶层：完成物错锚——分路「产出分级」把证据闸状态当给用户的结论；落盘即提交已写，与宿主「未经要求不提交」冲突时静默遵守宿主、不提交也不问

- 2026-09-07 | implement | 用户审视上周任务后投诉：手动要求合并 task archive 后有时不自动提交，也不就遗留问题询问是否修复/完成。现场=上周 ey-timp-lab 归档（runtime-deps 09-02 汇总、agent-skills 09-03/04 再合并；09-04/06 单任务归档的范围外问题未询问） | 疑似病灶层：授权与默认值缺失——归档合并节无提交句、无遗留裁定节点；第 6 步「并提交」只管单任务归档

- 2026-09-07 | implement+code-review | 用户裁定：合入/提交到基线前必须对工作分支相对基线做完整 review；scoped review 看不到完整问题。现场=同日 ses_f863478c 多轮 scoped 重审后合入 | 疑似病灶层：完成判据错锚——修复循环把 scoped 重审清零当成合入依据，合入前没有「最近一次必须是完整评审」

- 2026-09-07 | implement+code-review | ey-timp-etl ses_f863478c：评审仅 Minor、裁决可合入后归档任务文件并报告「尚未合入」；用户问遗留、要求修复后再独立评审，后又修第二轮 Minor。用户裁定：Minor 自动修→scoped 重审→无未处理发现才算完成，不能因可合入视为任务完成。上周任务同样多次 Minor 后用户要求修 | 疑似病灶层：授权与默认值缺失——Minor 写成「记录待办」，implement 归档门只拦 Critical/Important，「可合入」被当成完成

- 2026-09-07 | implement | 用户核对上周任务后裁定：任务结束后合并 commit 总是批准，squash 收口不再要求审批、改为自动合并。上周 ey-timp-lab reflog：09-06 连续 patch 式 squash（skill-code-format / skill-code-add-validate / skill-mcp-whitelist / skill-deps-query 等均 -back 后压单提交），用户均批是 | 疑似病灶层：授权与默认值缺失——「是否压缩」写成必须先确认，决策点已有稳定默认「压」

- 2026-09-06 | implement | ey-timp-lab ses_f8b4637e3ffe：两次任务归档（skill-mcp-whitelist / skill-deps-query）均 merge 后只 `git worktree remove`，reasoning 写 "Branch … still exists locally after worktree remove - that's OK"；squash 只删 `-back`。用户报「两个本地的 skill-deps-query skill-mcp-whitelist 分支似乎没有删除」，随后才 `git branch -D`。用户裁定：移除 worktree 的含义也包括移除本地分支 | 疑似病灶层：归档条「移除 worktree」未把删除本地任务分支写成同一完成物；模型按 git 默认（worktree remove 不删分支）自判残留 OK

- 2026-09-06 | interview+spec | ey-timp-lab ses_f8b781b8fffe：用户「生成的skillCode应该是30位以内且以字母开头,只包含数字字母下划线」。开场即钉「这是对 skillCode 生成规则的约束」；读现状后写出「超长、数字开头、登记是否同样校验还没定，先访谈」；reasoning 已写 add 应对非法编码拒绝；随即「访谈跳过：生成结果的格式已明确…走默认」，spec 只落生成器。用户后问「add接口有增加这个限制吗」只报告没有。用户裁定：原请求也应影响 add 校验，这是 agent 应提出的疑问 | 疑似病灶层：interview 判别把「用户点了一条路径」当成一种解读已明；未指定走默认把另一条写入路径从多义降成默认不改。spec 第 3 步「决策都已有明确答案 → 跳过」整批跳过，清单里未决的「登记是否同样校验」被生成格式已明一起吞掉

- 2026-09-04 | interview | skills 仓：用户说今日开了几个任务修代码事实与文档不一致、应在开发流程或 review 避免。agent 已加载 interview（护栏含「事实自己查，决策才问」），仍把文档类别当成多义列 1/2/3/4 问人，再要用户丢各单一句症状；用户纠正「你应该搜寻今天的任务来判断」后才在 ey-timp-lab 搜到 c30e601 / script-scan-log-cause / 7e20f55。用户一字批准把「先查事实再问决策」钉进 interview 开场 | 疑似病灶层：空钩子位置错——原则在护栏，判别首条是「≥2 解读 → 列项」；一次一问的操作信息比护栏强，本可查的事实分类被当成多义

- 2026-09-04 | implement+code-review | ey-timp-lab：用户开任务修代码事实与文档不一致。c30e601 无任务文件修三处——spec 只写缺 name、代码已拒缺 description；DTO javadoc 称失败进 results、代码走聚合异常；类注释写 WARN、代码打 ERROR。script-scan-log-cause：spec/design 把 shfmt 非零退出算工具链 ERROR，改判脚本错 WARN。7e20f55：解压写失败补 ERROR、shell 无效 AST 复用 fileFailure。用户裁定应在开发流程或 review 避免，一字批准双落 | 疑似病灶层：完成定义与评审范围均只盯本任务验收清单；code-review「只读代码」不读已有断言；所及类型上的注释不在核对面

- 2026-09-04 | tdd+implement+code-review | ey-timp-lab ses_f9446b0a7ffe：用户要求修复 ses_f97eac052ffe 评审标为 Minor 的死参数（ShellCommandRules.record 的 embeddedScripts 方法体未引用）与测试专用构造器（ScriptDependencyReport 双参仅测试消费）。开发会话 ses_fa896dbe3 已加载 tdd/implement/code-review 仍引入；模块评审找到但标 Minor 放行 | 疑似病灶层：空钩子+分级默认——「死代码」不含签名面未引用参数；发现后无分级钉死，默认 Minor 待办

- 2026-09-03 | domains | my-harness ses_f99b92013ffedz2wDNdXgzCebM 用 dogfood 当下一步选项；溯源 ses_f99b73513ffe：用户从未用过该词。最早写入产品语言=ses_fa3ba5c7（2026-09-01）自拟「每阶段都能 dogfood」并落入 `docs/00-总体架构与技术选型.md`；ses_f9fc993d8 起草内核-spec 时造「作为 dogfood 使用者」。用户裁定「这不是一个业务相关的词汇」，并一字批准「禁止招募俚语」 | 疑似病灶层：选词优先级只有来源顺序、没有负向边界，圈内习惯说法被当成社区共识招入术语表与文档

- 2026-09-02 | 常备层（无属主） | ey-timp-lab ses_f9dff28fbfferh37TojOJTEyK5：用户「现在skill的配置项有哪些」三解并存（SKILL.md frontmatter / SkillPackageParser / 本项目 skill.scriptdeps），reasoning 检出 "This could mean" 仍自选 frontmatter 作答；用户两轮纠正（「代码里解析的」→ Parser，「本项目中的skill 扫描配置」才命中）全程零 question；随后改 prefix 才加载 spec 并跳过澄清访谈。domains 未加载（观察名单理解偏移项待验证当日达成：未到场，且到场也对不上「问当前这句」） | 疑似病灶层：多义动作缺失——「同一描述多种合理解读」无属主。未指定→默认值（禁止问）、信息不足→spec 澄清访谈（仅能力变更路径）、规则冲突→冲突裁决，三套均不覆盖问答轮自选解读

- 2026-09-01 | spec | opensource/harness ses_fa3ba5c7dffegxKznNY0MXaDbz：愿景级输入「我想要自建一个harness，拥有dsh的架构可塑性及可插拔性，并拥有cindy和multica的多harness/agent编排沟通」被 spec 依职责吸收（reasoning 取证：模型援引「加载前不自判」条款加载 spec，行为合规），用户 92 秒后纠正「我觉得可以先不从spec开始，而是先从讨论开始」；纠正后开放探讨正常开展，但探讨产物去向无约定（agent 即兴提出「落成一页纸意图文档」）| 疑似病灶层：spec 无「探讨出口」——「能力目标未落纸面同样归管辖」把目标轮廓未成形的输入结构性吸收，正文从豁免判断直达分解/澄清访谈/落盘，无「先探讨、不落文档」合法分支；系统/架构级「说不清要什么」现场在 skill 体系无属主（prototype 只覆盖界面/交互同款现场）

- 2026-09-01 | implement | ey-timp-lab ses_fa38c8f88ffeYGh9g1fJ1GAC6X 用户再次手动 squash（reflog 取证：IntelliJ interactive rebase 两批——runtime-deps 任务 9 提交、skill-crud 任务 7 提交各压为单提交，范围含 spec/design docs 提交+feat+归档 chore，对象均为未推送本地提交，origin 停 squash 前 5c223cb）；观察名单 #53「第二次 squash 现场」条件当日达成 | 疑似病灶层：workflow 决策点缺失——提交纪律结构性产生碎提交序列（每验收标准一提交+docs 落盘即提交），归档步无 squash 收口节点与默认协议，压缩只能用户事后手动做

- 2026-09-01 | implement | ey-timp-lab skill-crud 归档刷新前端对接：错误码列填内部枚举名（SKILL_*）；出参写成散文摘要（「result 为 id」）；用户两轮返工后裁定写入模板，且「码」列不钉死字段名为 msg（他项目可能暴露 code） | 疑似病灶层：模板未约束「码」列取值来源、出参须完整响应 JSON

- 2026-09-01 | domains+spec/design 链路 | ey-timp-lab 两周 35 会话（2026-08-26~09-01，opencode.db 取证）domains 零加载（spec×14/code-review×14/tdd×10/design×8/implement×6/debug×1/customize-opencode×1），DOMAINS.md 与项目 AGENTS.md 始终未建（git 全历史零触及）；同项目 spec 08-26 升触发必达后正常触发 14 次，对照证明 description 触发必达架构不依赖项目常备层。会话原文两处实锤绕行：ses_fa896dbe3「没有 DOMAINS.md/AGENTS.md，上下文命名跟随现有 agent-skills」（spec 正文「没有 → 按直觉命名」被执行）；design 8 次归属路由均判单上下文（「无跨上下文决策，不动 ARCHITECTURE.md」，ARCHITECTURE.md 未建属正确输出非遗漏）。唯一理解偏移信号（08-28 ses_fb91594feffe 用户纠正「我的意思是整合本地的task archive到一个文件」）指向工作流概念且单次，未达沉淀门槛 | 疑似病灶层：触发架构代差——domains 仍停在 08-04 架构（被动信号 description + 项目 AGENTS.md 常备指针双腿，升格区 08-04 条），ey-timp-lab 无项目 AGENTS.md 缺常备腿，命名又被 spec 流程静默消化、description 信号无可抓；「无 DOMAINS.md 现场」全链路无属主：spec 正文把该现场写成可绕行分支（「没有 → 按直觉命名」），design 读现状静默跳过。同模式第三起（08-04 #91 spec-skill 会话未触发、08-24 ey-timp-etl 常备层无指针零加载）

- 2026-08-27 | prototype-anchor | 用户报「添加锚点/添加组按钮提出和锚点清单同一层级不生效」；考证：当前生效客户端两按钮始终在清单面板内部（h3+rows+按钮同属 am-panel），git 全历史（759f6ad 插件化起 5 提交）无移出记录，工作区干净无 stash，仓库与 ~/.agents 副本 md5 一致，8420 服务分发即此版——非被改回，而是该改动从未进入生效资产，最可能丢失于 8-23 插件化（cake 内嵌 250 行拷贝换一行接入，会话内改动随内嵌块消失，cake 非 git 不可考），证据日志亦无此需求记录；用户裁定落点=A/B/C 切换工具条，anchor-view.js 三处改动（CSS am-act/grp、面板移除两按钮+note 补指引、工具条加按钮+绑定选择器收窄 button[data-v] 防误绑模式切换），jsdom 冒烟 24/24（旧 16 回归+新 8：工具条在场、面板无残留、A 视图无面板可直接添加、点选/分组流程通），双副本同步 md5 一致，8420 stop/start 后 curl /__am_client.js 实测新版本在场 | 疑似病灶层：改动不落生效资产即不可追溯——插件化抽取时内嵌拷贝的会话内改动未带入新信源，且需求未沉淀（无证据日志/无测试断言），失联后无人察觉

- 2026-08-26 | spec+用户级门禁 | ey-timp-lab ses_fc27145adffeI5ngT22EDuJns2（18:12，"我觉得现在需要增加一个接口,该接口接受skill-zip-file-code…"）SDD 零执行（opencode.db 取证）：全程唯一 skill 加载为 4 分钟后的 tdd，直出 8 write+25 edit 零提交；reasoning 原文显示模型**评估了门禁**（"According to the AGENTS.md instructions, I should check for docs/ folder with *-spec.md files first"→"No docs/ folder with *-spec.md files"）后放行——项目无 docs/ 无 spec 无项目 AGENTS.md，门禁存在性条件为假 | 疑似病灶层：用户级门禁的条件逻辑（非 description、非判断事件缺失）——「docs/ 下存在 *-spec.md 时」把最需判管辖的无 spec 现场结构性放行；常备层指针的注意力强制**有效**（判断事件发生了），失效在内容条件。距 12:08 门禁部署仅 6 小时
- 2026-08-26 | spec | 门禁删除后三轮受控实测（ey-timp-lab 改前副本 P8=上述会话原话）：**无门禁轮** t1 ✗ 零加载直写 4 生产文件（reasoning 零管辖判断——判断事件缺失复发）/t2 ✓ spec+澄清访谈——同环境同输入一翻一過，description 层概率性实锤；**「任意变更都要使用本skill」轮**（用户原句实测裁决）：P8 2/2 ✓、P7 ✓、N3 ✗（debug 直路由吸收）、N4 ✗（"Simple refactor"直写码）；**「任意变更都要先使用本skill（含 bug 修复与重构，加载前不自判）」+正文豁免判断轮**（用户裁定机制=触发必达+豁免挪正文）：P8 ✓ 且豁免判断可见执行（"本次是新增能力，spec 不豁免"）、P7 ✓、N3 1/2（r3b 达成 spec→debug 组合）、N4 0/2 | 疑似病灶层：bug/重构形态被一阶直路由（debug 锚点）与「小重构豁免」本能吸收，description 层（含显式点名）打不断；spec 入口是**元动作**（判要不要 spec），无一阶请求表面特征可抓，快路径分类天然漏它

- 2026-08-25 | spec+tdd+implement | ey-timp-etl ses_fc6c1074（智能体节点提示词预览接口，22:07–22:19）零 skill 加载绕开 SDD（opencode.db 取证）：用户命令形态新能力请求（"智能体节点需要增加一个用于预览所有提示词的接口…"）被直接承接——探索→question→todowrite→写 3 生产类+1 测试类→mvn 测试，全程零 skill 加载、零 git 提交、spec 未更新；距 B+C 条款部署回归通过（20:08）仅 2 小时 | 疑似病灶层：入口无闸门当日复发——B（tdd 入口路由）C（spec→implement 接续）均在 skill 体内，零加载时无机会开火，description 触发面对短命令形态再次失效；升格区 2026-08-25 条所记 A（常备层指针）「再发即重提」条件达成
- 2026-08-25 | spec+implement+tdd+design | ey-timp-etl 三个功能会话同日全部绕开 SDD 闭环（opencode.db 取证）：①ses_fccb721c（上游结果配置+重试，15:40 提交 5c94dd19）全程仅加载 writing，SDD 零加载，spec/design 于 17:37 docs 提交补票；②ses_fc7d3316（skillCodeList 必填）tdd 17:08 先加载承接入口、spec 17:21 才补中，implement 全程缺席——无任务文件、无验收清单闭环，docs(17:37)/code(17:38) 背靠背提交；③ses_fc7afd03（试运行接口重构：param 改 requestParam+formdata、output 结构化、新增 resultFileCode，未提交）零 skill 加载，spec 试运行章节断言已失真 | 疑似病灶层：入口无闸门——命令形态编码请求被 tdd description/项目 AGENTS.md「计划优先」模板直接承接，spec「管辖」判别需先查 docs/ 鸡生蛋；spec→implement 接续无机制（spec 落盘步结束语是「告知用户」非路由 agent，观察名单 #19 证据到场）；08-24 升格标注「常备层指针未落地，残留风险存续」当日复发；当日 16:31 干净会话触发回归（trigger-P5/N4/N3/P5r/impl-P1）全过而真实会话三发——观察名单 #12（干净会话回归覆盖不了累积上下文退化）同步佐证
- 2026-08-24 | spec+design+implement+tdd | ey-timp-etl 会话（ses_fcd2f114cffexa70IBK19koUlp）三轮改动（删除 inputContract、outputContract 迁移+字段类型校验、OutputField 提升顶层类）全程零 skill 加载（工具面仅 bash/read/edit/grep/write/todowrite/question/codegraph），agent 按项目 AGENTS.md「计划优先」模板直接编码，把 spec/design/domains 三文档当实现子任务就地同步；全程零提交（当日 git log 空、工作区 16 文件含三文档未提交）| 疑似病灶层：①常备层无 SDD 指针——影响面扫描已列出三文档，agent 仍不知其归 skill 管辖（description 触发面覆盖不了「删除X」简短命令形态，且「改动是否触及 spec 断言」需先读 spec，鸡生蛋）；②规则冲突——项目 AGENTS.md 第 239 行「禁止提交 git commit」与 skill 落盘即提交约定直接打架，skill 未加载时项目条款独自生效
- 2026-08-24 | prototype-anchor | 用户报「点击这个原型中的锚点保存按钮没有作用」（cake prototype.html，#am-c-ok）；读码定位：anchor-view.js 保存处理器对术语名/锚点值为空静默 return，是唯一零可观测反馈路径（其余路径均有 toast）；jsdom 复现空字段点击无反馈 | 疑似病灶层：校验失败反馈缺失（观察名单同日行已预判，证据到场触发修复）
- 2026-08-24 | prototype-anchor | 用户反馈锚点清单（B）与覆盖标签（A）交互问题：点清单条目只定位不显示标签，「A 覆盖标签」含义应收束为「显示所有锚点的标签视图」；裁定：清单点击=单独显示该锚点标签（开关式、再点取消）、单独标签可点弹信息卡；anchor-view.js 加 amSolo + 行点击改开关，面板提示语同步；jsdom 冒烟旧 8/8 + 新 11/11 通过，~/.agents 与 skills/ 双副本同步 | 疑似病灶层：单锚标签视图无入口（A 全部 / B 零个之间缺「单个」中间态，清单点击只承担定位）
- 2026-08-24 | prototype-anchor | 上条改动当场对齐返工：用户指出点锚点 B 时锚点 A 的标签不应自动关闭——排他单选改为逐锚点独立开关、多标签并存（AM.solo 改 Set，amSolo 按锚点值增删）；冒烟新集改写 12 断言全过 | 疑似病灶层：交互模型误判（把「单独显示」实现为排他单选，用户预期是各锚点独立开关的并集）
- 2026-08-24 | prototype-anchor | 同主题第二轮对齐：用户指出 A→B 切换不应清除标签视图——「A 覆盖标签=显示所有」与锚点清单应共存；A/B 从互斥变体改为正交（A=显示全集命令、B=清单面板+标签集并存），A→B 把全集物化为 solo 集合并可逐个关闭，C 层级条保持独占替换，Esc/关闭才清集；面板提示语同步；冒烟旧 8/8 + 新 14/14 通过 | 疑似病灶层：模式互斥假设过强（变体切换=清空重建，把「显示范围」与「面板可见性」两个正交状态绑死在一个 variant 上）
- 2026-08-24 | prototype-anchor | 同主题第三轮对齐：用户指出点击无锚点子元素会显示其父元素锚点，裁定无反应——全局点击处理器去掉 closest 就近归属回退，只有带锚点元素本身响应点击开信息卡；给未锚定元素加锚统一走面板「添加锚点」点选模式；页面交互冻结不变；冒烟旧 8/8（开卡路径改经点选模式）+ 新 16/16 通过 | 疑似病灶层：隐式就近归属（closest 回退把子元素点击归因到父锚点，与「锚点=精确指认」语义相悖）
- 2026-08-24 | prototype-anchor | 用户提出「锚点必须，术语名及描述可在沉淀时自动总结」（与 domains 的 agent 总结+用户审核同构）；查实 prototype-anchor-sync 意图锚点本就裁定「命名裁定发生在同步时，不在锚点创建时」，卡片必填术语名是相矛盾的遗留 → anchor-view.js 五处对齐：术语名/定义降可选、仅锚点值必填、导出片段带（待命名）占位、清单面板回退显锚点值；jsdom 冒烟 8/8；无正文改动（工具行为向既有裁定收敛）

- 2026-08-24 | prototype-anchor | 用户指出两点：锚点编辑期变更频繁，保存自动同步术语表会把中间态术语写进长期资产；且其他领域语言均由 agent 编写，程序写术语表是第二写者 | 疑似病灶层：写入权错配（高频工作内存的保存默认同步低频长期资产；程序硬编码术语行格式成第二信源，命名/定义/归属裁定绕过 domains 纪律）
- 2026-08-24 | prototype-anchor | 用户报「原型设计时锚点无法保存」；curl 复现：cake prototype.html 的 settings-panel（内联 JS 运行时渲染，源码仅空占位 div）内 DOM 路径保存报 path not found，静态区保存正常 | 疑似病灶层：锚点写盘机制——「DOM 路径⇄源码区间」映射假定页面全静态，JS 渲染子树与磁盘变动后的陈旧 DOM 在源码中无对应区间；服务端英文技术报错（path not found: html:0/…）用户无法自救
- 2026-08-24 | prototype-anchor | 附随发现：8420 端口跑 10:40 启动的旧进程，内存仍含当日已删除的 syncDomains（服务逻辑在启动时加载，不会随磁盘代码更新）——改动服务端代码后必须重启服务才生效，pid 时代无提示 | 疑似病灶层：观测缺口（运行版本与磁盘版本漂移不可见）

- 2026-08-23 | prototype-anchor | cake prototype.html 出现双文档拼接污染（1484→2836 行，第二份完整文档尾随第一份；agent 多轮改写文件期间浏览器持有旧版页面，疑似随后从锚点视图保存/包盒，服务端按旧 DOM 路径解析新文件树错位写入）| 疑似病灶层：写盘无陈旧页面防护（客户端不发版本指纹，服务端无法识别页面已过期；agent 改文件与浏览器页面寿命天然交错）

- 2026-08-23 | prototype | 分组标记首版落地为成员各自携带 `data-anchor-member` 属性，用户当场反馈预期是「ABC 包进一个更大的盒、盒打锚点」——分组 = 标记锚点 + 多一步包盒 | 疑似病灶层：机制选择错误（把「组」建模为成员属性而非容器；首轮答复本就给出容器方案，实现时选了成员属性岔路——多选任意元素批量打属性的实现便宜性压过了语义贴合）

- 2026-08-20 | spec+prototype+domains | cake 项目「为原型添加统一锚点」会话（ses_fe1c90e95ffeeJ4sjPTxierW1f）：spec 三条 UI 拓扑断言（侧栏分项目会话/对话会话两组、对话区下方 status bar、顶栏运行状态）在原型中无对应 DOM，锚定任务暴露差异，用户裁定「先按照现有原型来补充 spec」；spec 改写 + domains 4 个未落地术语移除、3 个随原型新增，双文档追原型返工 | 疑似病灶层：spec 边界自查只排除技术方案、不排除 UI 拓扑（抽象判据「悄悄变了有人受损吗」拦不住貌似可验证的拓扑断言），spec 与原型在断言级强关联，原型探索一落定就倒挂为 spec 追原型

- 2026-08-19 | skill-creator（invocation-markers 规范）| 用户观察：有些模型调用 skill 自带脚本时以项目目录（会话 cwd）解析路径，而非 skill 目录 → 找不到脚本或跑错同名脚本 | 疑似病灶层：路径锚点缺失（规范未规定 scripts 调用的锚定基准；模型不知道 base directory 的存在与用法）

- 2026-08-19 | skill-creator | 用户指出：创建 skill 时不应建测试集，因为不是基于真实场景 | 疑似病灶层：创建工作流把测试集建成当作交付物，与 testing-guide「真实语料」冲突，诱发假用例
- 2026-08-04 | implement | 用户二轮审查再指出 git 约定两缺陷：worktree 目录未写死默认约定（用户未被确认）、基线漂移下的同步无防修复循环机制 | 疑似病灶层：默认值缺失 + 机制未闭环（需结构性无冲突不变量而非冲突解决策略）
- 2026-08-04 | implement（含 spec/design git 行） | 用户审查指出当日落地的 git 约定三处缺陷：branch 切换与"docs 落主干、代码在任务分支"的双层结构冲突、合并目标（基线分支）无处记录、异分支上 spec 偏差无路径 | 疑似病灶层：机制选型（隔离载体应为 worktree；基线分支应锚定在任务文件这一交接介质上）

- 2026-08-04 | skill-iteration-review | 用户反馈：AI 输出与文档充斥造词型黑话，解码成本高 | 疑似病灶层：措辞（旧「主导词」原则只问招募先验，不问联想是否干净）
- 2026-08-05 | tdd | 用户报告：模型倾向写容易通过的测试（contains 替代 equals 等弱断言），甚至删除测试断言伪造通过 | 疑似病灶层：目标错位（"让测试变绿"取代"验证行为"成为目标，测试退化为待通过的关卡；原变异自检只事后查弱测试，拦不住主动弱化方向）
- 2026-08-05 | tdd | 用户报告：模型会为测试在生产代码里添加仅供测试的方法/字段/类型，测试走的是伪造路径而非生产真实路径 | 疑似病灶层：同目标错位——绿即可过，不追问测试覆盖的路径是否真实发生
- 2026-08-05 | tdd | 用户报告：模型经常不验证注册到容器的行为（如 Spring bean 能否加载进容器），只测单元逻辑，装配错误留到运行时 | 疑似病灶层：覆盖盲区（注册/装配面是生产真实路径但不在模型的默认验证范围内）
- 2026-08-04 | domains | 用户审查指出 description 枚举场景说法，违反「不枚举用户说法」原则 | 疑似病灶层：description 判别逻辑（已修：收缩为本质能力+锚点 DOMAINS.md+负向边界；DOMAINS.md 属类别级锚点，判定为不违规）
- 2026-08-04 | domains | spec-skill 设计会话（ses_034155）全程命名裁定/领域建模，domains 未触发、术语未沉淀，用户事后发现补沉淀 | 疑似病灶层：description 判别逻辑（"命名裁定/产出关键概念即沉淀"类别锚点缺失；skill-creator 已占用会话，domains 未被判断可并行；待诊断）
- 2026-08-04 | domains+spec | domains 与 spec 存放路径各自为政（docs/domains/ vs specs/），同一上下文的术语与断言分处两棵目录树，且 DOMAINS.md 中"spec 归属单元=特性"词条被本会话"按 BC 组织"决策超越未同步 | 疑似病灶层：跨 skill 共用约定缺失（存放路径与术语表各自独立发明）
- 2026-08-04 | domains | 用户审计指出 DOMAINS.md 部分词条无真实消费者：design 就绪裁定未落 implement 入口，任务契约/特性词条零消费者，特性与 skill 实际用词"能力"脱节 | 疑似病灶层：沉淀未回环到消费者端（加词条而未落/未改对应消费端）
- 2026-08-05 | debug 创建证据 | 用户报告：修 bug 时模型倾向修复表面症状，不追根因、不判断设计问题还是实现问题 | 疑似病灶层：诊断纪律缺失（此前涉 bug 修复的只有 tdd 一句"修 bug 先写复现测试"，只管编码阶段，诊断阶段无人守护）
- 2026-08-05 | implement + code-review | 用户报告：AI 生成的代码往往保留死代码、注释、未使用的导入、无用的测试 | 疑似病灶层：完成卫生门禁缺失（验收标准级验证不覆盖项目级整洁；观察名单第 17 行"项目级全量门禁"证据到场）
- 2026-08-05 | implement + code-review | 用户补充：整洁需要反向边界——不能顺带修复历史遗留代码、不能删除非本任务编写的代码 | 疑似病灶层：代码变更范围纪律缺失（implement 无范围护栏；code-review"无未请求的额外功能"只覆盖加法面）
- 2026-08-05 | tdd + code-review | 用户报告：AI 经常写很多层代码、设计很多插入的入口（过度分层、过度扩展点） | 疑似病灶层：最小性纪律缺结构面（tdd「最小实现」被执行为让测试变绿的最少代码，分层/扩展点不违规；code-review「复杂度」无判据）
- 2026-08-05 | spec | 真实项目（ey-timp-etl）给出完整详细需求文档（新限界上下文），spec 未触发，模型直接探索并出执行计划（全程未加载 skill），用户问"为什么不先建立spec?"后才加载 spec skill 补落 | 疑似病灶层：description 判别逻辑（会话内的详细需求输入被误判为"目标与验收标准已落纸面"；"完整可执行需求"形态不在"启动新功能/新需求"的判别面内）
- 2026-08-05 | implement | 真实项目追加功能（智能体节点加"试运行"能力，触及 spec 断言、新增 spec 能力章节）未触发 implement：仅加载 spec/tdd/code-review，无任务文件、无按验收标准提交，用户问"为什么没有git提交"才补两个提交 | 疑似病灶层：触发边界（模型对追加型中等变更施加了 description 未写的比例性豁免；"spec 就绪进入实现"未覆盖"已有能力上追加新能力"类别锚点）
- 2026-08-06 | code-review | 同一会话（智能体节点试运行）两轮评审裁决"可合入"后，用户仍发现两个约定一致性缺陷：EtlNodeAgentFormData 字段无层级（违反其他 *FormData 的 Properties*Config 结构约定）、异常抛魔法值（违反 EtlErrorEnum 枚举约定）；评审已查断言质量/死代码/范围面，惯例一致性无人查 | 疑似病灶层：code-review 评审维度盲区（缺「与代码库既有结构/命名约定的一致性」检查项；同类文件惯例需评审者主动 grep 同类，规格无此提示）
- 2026-08-18 | spec+design+implement+domains | 用户反馈：docs/ 根层混置限界上下文目录与其他文档（tasks/ 等），同层混淆 | 疑似病灶层：存放约定缺父目录收编（docs/ 一级目录语义不单一）
- 2026-08-18 | tdd | 会话 ses_fed5a3031（CE脚本过程日志下载排查）修复阶段：构建环境事实边撞边发现（运行时 JDK 与项目要求不符、版本管理工具环境变量干扰、父 pom relativePath 解析到错误分支），约 15 次失败排列组合后才发问用户；分支基线本身编译不过，TDD 中途才发现被迫排除文件；同一错误（构建命令忘设工作目录、跑在真实仓库而非隔离副本）连犯 3 次才写包装脚本机制化；`install` 写入共享本地依赖仓库，下游模块测到旧 jar 被迫重装重跑 | 疑似病灶层：tdd 只保"实际运行并读输出"，未保信道前提——跑的是对的代码/对的目录/未污染的依赖，RED/GREEN 的证据资格无人守护；观察名单第 17 行"熔断仍待证据"证据到场（同类失败第二次未触发机制化）
- 2026-08-18 | debug | 同会话定位根因阶段：依赖（内部 jar）中的实现需要追读，先走 javap 反编译约 20 次工具调用、5 轮查找实现类，才发现本机就有该依赖的源码仓库可直接读；用户补充观察：自动反编译倾向取最新版本而非项目实际依赖的版本 | 疑似病灶层：私有知识缺失（依赖代码定位顺序：问用户/本机源码仓库 → 依赖自带 sources → 按项目实际解析版本反编译）——模型不知道，非知道而忘

- 2026-09-14 | tdd + code-review | ey-timp-etl 会话（thread 01a09bb2，试运行模拟前置节点结果）：实现把 AgentUpstreamReader 生产调用点从 4 参切到 5 参，4 参构造遗留为仅测试消费（同模式：10 参 AgentNodeExecution 构造在本变更后亦退化为仅测试消费）；一轮评审将「4 参构造仅测试消费」评 P5——派遣提示含「本任务引入的测试专用物不低于 P2」原文仍被低评，理由=「与既有 10 参构造同类容忍」先例合理化；二轮评审才发现 executor→reader 装配缝无测试钉住（功能可整体静默失效，P4）——测试全绿但未走生产真实入口；三轮评审再以「标准适用不一致」追出 10 参构造；共四轮评审闭环 | 疑似病灶层：①tdd「不加测试专用物」只覆盖「为测试添加」方向，不覆盖「生产消费者迁移后遗留」方向，「测试走真实入口」无强制函数（旧入口仍在、测试全绿，入口漂移无失败信号）；②「装配面」例证全是容器/框架注册形态，手工构造装配缝（executor→reader）不被模式匹配（一轮评审以「无新增 bean」判装配面已覆盖）；③code-review 分级底线被「基线已有同类容忍」击穿，「测试专用物」被重新归类为「便捷 API」

- 2026-09-14 | eli5 | 用户投诉：「虽然 eli5 本身能让我简单理解某样东西的原理，但无法让我深入」——SKILL.md 只有「了解」档产物（大图少字图解），SKILL2.md 仅在受众维度加自由度，深度维度（了解/会用/所以然/改造）在 description 与正文均不存在 | 疑似病灶层：能力定义层——缺深度判档与各档产物定义，非措辞不足

## 升格 / 移出

- 2026-09-14 | DOMAINS 9 词条补消费者落点（限界上下文/spec/spec 就绪/design/design 就绪/ARCHITECTURE.md/能力/任务文件/SDD），消费者行格式与存量 6 词条对齐；观察名单任务文件枚举条补注第三处枚举（implement 意图锚点自称 3 件） | 依据：2026-09-14 全量 review P5（消费者标注 6/15 不统一）+ 用户指令「全部 fix」；各消费者落点均按 skill 正文实读核位 | 回归：纯增补，词条定义与反例一字未动

- 2026-09-14 | implement 触发测试集补 P4（合并同一 spec 的任务归档）：语料=2026-08-28 ey-timp-lab ses_fb91594feffe 用户纠正原话「我的意思是整合本地的task archive到一个文件」；结果表加 P4 列，存量两行标 — | 依据：2026-09-14 全量 review P4（08-28 description 加归档合并锚点后触发集未补未跑）+ 用户指令「全部 fix」| 待验证：干净会话实跑 P4

- 2026-09-14 | 9 个测试文件跑法去 opencode 硬编码：code-review/tdd smoke、debug/domains/implement/interview/spec/writing 触发集与 spec 冒烟集的跑法行改 harness 中立（机制=干净会话发输入+查该 harness 加载记录；opencode 命令与 Codex 子代理探针并列为例示）；历史结果行内的 opencode 字样为事实记录不动 | 依据：2026-09-14 全量 review P4 + 用户指令「全部 fix」；实践已先行（debug 触发集 2026-09-14 结果行自述 Codex 子代理 rollout 取证）| 回归：仅跑法说明，用例与断言零改动

- 2026-09-14 | handoff 第 4 步去 harness 专有引用：「`opencode run` 无 TUI 不算」改「无交互界面的拉起方式不算（一次性批处理执行拿不到后续输入）」——09-13 去 harness 化（path.py 删写死 opencode、DOMAINS 词条不写死 harness）在正文的遗漏句 | 依据：2026-09-14 全量 review P3 + 用户指令「全部 fix」，挂 2026-09-13 用户纠正（harness 不写死）同向 | 回归：冒烟 S1–S3 不触第 4 步，对照改后正文断言仍成立 | 减法审查：同义替换，无可删

- 2026-09-14 | debug description 用于段补连体命令锚点：「调试、测试或本地验证接口/服务（不要求给出访问地址），含『验证并修复/自动修复』连体命令」 | 依据：2026-09-14 返工事件（Codex 线程 01a09a63「自动使用本地环境验证,自动修复」全程仅加载 local-env，debug 零加载→第 4 步修复节 tdd 路由链断）；用户裁定 A | 双向对照：连体命令=本地验证入口形态非新类别，不吞 local-env（立通道仍路由）、不动 tdd 负向边界 | 回归：触发测试集补 P6（该线程首条原话）；P6/N2 干净会话探针结果见测试集结果记录 | 待验证：下一次「验证+自动修复」连体命令真实现场

- 2026-09-14 | tdd description 用于段封闭「自认已会」豁免：「无论从哪个流程环节进入；自认已会测试先行同样加载，凭记忆自绘纪律不算数」 | 依据：2026-09-14 返工事件 reasoning 取证（"Plans TDD: new tests, extend agent factory test…" 后零加载，三批修复 RED 全程未观察——豁免路径非「不适用」（09-07 已封闭）而是「自认已会」）；用户裁定 B | 减法审查：「无论从哪个流程环节进入」管进入路径、新句管豁免路径，不重叠，无可删 | 回归：动作级时刻单发触发测试不可达，smoke.md 边界对补「tdd vs 自绘 TDD」对照 | 待验证：下一次流程中途转修复且模型自绘 TDD 的真实现场

- 2026-09-13 | handoff 路径改本会话临时目录：交接文档 `{本会话临时目录}/handoff/<yyyyMMdd>-<topic>.md`；`path.py` 接收临时目录参数，删写死的 `opencode`。DOMAINS 词条同步。冷启动冒烟 S1–S3 | 依据：2026-09-13 用户原话 | 回归：对照旧稿 `{tmpdir}/opencode/handoff`；新稿不拼接 harness 名。S2/S3 未触。description 未改，触发集不跑 | 减法审查：删 path.py 内 `opencode` 段 | 待验证：下一场交接是否写到当前 harness 临时目录

- 2026-09-13 | 创建 handoff skill（model-invoked）：交接文档 `{tmpdir}/opencode/handoff/<yyyyMMdd>-<topic>.md`；写完生成可粘贴提示词；可交互新会话 API 才启动（`opencode run` 不算）。接手只读文档、不查源会话。debug/implement description 负向补会话间交接。创建期不建测试集 | 依据：2026-09-13 用户指令 + 同日 ses_f66842526ffe 查源会话返工 | 回归：对照无此产物时新会话必查库；新稿路径由 scripts/path.py 钉死。debug/implement 触发集对照负向成立、干净会话未跑 | 减法审查：不改 implement 任务文件机制，只划界 | 待验证：被同日「临时目录改本会话环境」条超越。下一场「移交给新会话」是否加载 handoff 且新会话不查库

- 2026-09-13 | 创建 local-env skill（model-invoked）：契约项目根 `.local-env/start`+`url`（可软链）；缺则引导创建（一次一问，url 本轮已给则写入不问，补 gitignore）；有则执行 start 立通道。debug 第 1/5 步立通道改为 `<use-skill>local-env</use-skill>`，删内嵌路径与「缺则要两件」；description 负向补建立/补齐 `.local-env`。debug 冒烟 S1/S3/S7 改路由；触发 N3 新。创建期不建 local-env 测试集 | 依据：2026-09-13 用户指令（`.local-env` + 引导创建 + 独立 skill） | 回归：对照 debug 旧稿内嵌 `local-env/` 且缺则停下；新稿只路由。S2/S4/S5/S6 未触；description 已改，触发集对照成立、干净会话待跑 | 减法审查：debug 立通道操作信息整段迁出，无双写 | 待验证：下一场通道不立是否加载 local-env 并引导/启动；建立 `.local-env` 请求是否不触发 debug（N3）

- 2026-09-13 | debug 第 1 步：通道不立 → 有 `local-env/start` 与 `local-env/url` 则执行 start（工作目录=项目根）再按 url 探活（本轮用户给出的地址优先）；缺则停下要这两件（目录不进 git，本机软链）。第 5 步需要重启时同样：有 start 则执行再探活，不留给用户。冒烟 S1/S3 预期改写、S7 新。09-12 升格条待验证「通道不立是否停下要服务」被本条超越 | 依据：2026-09-13 用户原话 + 三轮裁定（agent 起进程 / 软链目录挡机器差 / start+url） | 回归：对照改后第 1/5 步——旧稿停下要人起、重启告知重启点；新稿项目内入口可执行则 agent 立通道。S2/S4/S5/S6 未触；description 未改，触发集不跑 | 减法审查：删「停下要服务起来」「需要重启则告知重启点」两句，立通道/重启并入同一完成物（有入口就执行） | 待验证：被同日抽出 local-env 条超越

- 2026-09-12 | 09-07 划界干净会话补跑：domains P1/P2/P3 ✗（直触仍被 spec/interview/skill-creator 吸收，与 08-04/09-01 同构）、N1/N2 ✓、N3 经 spec R1 冷启动加载非直触、N4 ✓（interview 到场 domains 未触）；interview P1 ✓、N1 ✗（无前序「该配置」，语境饥渴过触发）；tdd vs 小重构：请求开场 tdd 先于 spec 加载（不抢入口 ✗）随后 B 条款截回 spec（不自判 ✓）。观察名单 #60 结案 | 依据：观察名单 #60 + 2026-09-12 用户指令补跑 | 无正文改动

- 2026-09-12 | prototype-anchor-sync 冷启动冒烟 S1–S3（写入权/命名在同步时/格式信源自指）；user-invoked 无触发集。观察名单无测试集欠账结案 | 依据：观察名单 #58 拉动条件（09-11 已改动未建集）+ 2026-09-12 用户指令 | 回归：对照改后正文三硬性均在场；S1–S3 对照 ✓。无 description | 减法审查：只建集不改正文

- 2026-09-12 | design-principles §2/§3/§4：空钩子须出现在决策步；默认须是互斥选项里显式选中的一项且稳定后降档；下游状态名不得当本步完成判据 | 依据：2026-09-12 两周审查（09-04 护栏赢不了判别步、09-07/09-09 可合入当完成、09-07 squash 降档、09-09 跳过合入静默保留、09-12 单测绿当修完）+ 用户指令 fix 1 2 3 | 回归：对照改后三节——旧稿空钩子只禁无操作信息、默认值只写死未指定、完成判据只说可观察产物；新稿并入位置/显式选项/降档/下游状态名。无可删。无 description | 待验证：下一轮审查/创建是否按新句审计

- 2026-09-12 | debug 第 5 步：本变更被该地址进程经本地依赖仓库加载 → 先刷新该仓库再重启再打，刷新不留给用户。tdd 共享本地状态条补除外：已启动服务从该仓库加载本变更时刷新不是测试信道。冒烟 S6；T08 未触 | 依据：2026-09-12 用户补诉 + 09-08 ses_f804a55e 把安装留给用户 | 回归：对照改后第 5 步——旧稿只重启再打，新稿刷新是再打前置；tdd 旧稿单测不经共享仓库可被读成永不 install，新稿除外仅已启动服务加载；S1–S5/T08 未触；description 未改，触发集不跑 | 减法审查：重启再打句保留，刷新并入同一完成物；tdd「记账 + reactor」保留 | 待验证：下一场本地接口改库后是否先刷新再打、单测是否仍走 reactor

- 2026-09-12 | debug description 用于段加「调试或测试给出本地访问地址的接口/服务」；护栏加调试入口含修复；第 1 步补本地接口复现（通道不立则停下要服务、禁止运行时诊断代替复现；失败出示原始响应）；第 3 步钉死实现层默认自动修、不问修哪一层、方案不互斥都做、已有配置/spec/design 写 X 代码做 Y 是实现层、用户已给目标或 spec 已断言则非期望层；第 5 步复现是 HTTP 时同一请求再打到符合承诺，重启后立刻再打不等继续。冷启动冒烟 S1–S5、触发 P1–P3/N1–N2。观察名单 debug 冒烟集欠账结案 | 依据：2026-09-12 返工（本周五场）+ 用户指令 do it | 回归：对照改后正文——旧稿入口只有「报告 bug」、实现层无自动修默认、验证无 HTTP 再打；新稿本地地址入口走完修复与再打；S1–S5 对照通过；description 已改，触发集对照 description 成立、干净会话待跑 | 减法审查：旧「不可复现→要条件」保留，通道不立是其操作信息；旧实现层箭头保留，加默认自动修与误升判据；意图锚点「未定位根因不动手」未改 | 待验证：下一场本地接口实测是否自动修且出示原文、通道不立是否停下要服务；P1–P3/N1–N2 干净会话

- 2026-09-11 | prototype-anchor-sync 第 6 步信源错指修正：删「按 domains 表格格式追加（通用格式信源在 domains skill）」——domains 正文写入规范仅四要素、无表格格式定义，本仓 DOMAINS.md 亦为定义式非表格，声称的信源不存在；锚点行格式改由本 skill 拥有（唯一约束=与锚点服务只读解析兼容），示例行承载不变 | 依据：2026-09-11 全量 review 发现正文失实 + 用户裁定信源改指自身（候选方向二：domains 补表格格式定义，被否——会与本仓 DOMAINS.md 定义式冲突且钉死所有项目术语表形态） | 回归：对照改后第 6 步——旧稿指向不存在的信源，新稿信源自指、约束句保留；位置归属仍由第 5 步承载，无残留双写；无 description，无触发集 | 减法审查：错指删除即全部改动，无可另删 | 待验证：下次真实同步按新措辞执行度

- 2026-09-10 | code-review scoped 重审归当前会话自检：意图锚点「生成者不评审自己的工作」收窄为「生成者不裁决自己的工作」；第 4 步 scoped 重审明确当前会话做、不派独立评审、不作裁决；护栏独立评审要求限定完整评审。冒烟 R01 硬性限定完整评审、R10 新 | 依据：2026-09-10 用户裁定「scoped评审可以在当前会话进行,但完全评审一定要派出独立评审,这样可以平衡效率问题」+ 同日返工（评审循环约 10 批，批批派独立评审成本高） | 回归：对照改后正文——旧稿护栏要求评审者与被评审实现不同会话（scoped 重审同样须派发），新稿完整评审独立不变、scoped 自检不派不作裁决；R02/R07/R08（scoped 不作归档/合入依据、清零后再派完整评审）未触仍成立；implement 各 scoped 条款未触；description 未改，触发集不跑 | 减法审查：意图锚点第二句与护栏原句合并收窄（裁决权归独立评审，单一信源），无可删 | 待验证：下一真实修复循环 scoped 是否当前会话自检且不作裁决；完整评审是否仍派新鲜上下文

- 2026-09-10 | implement 第 4 步卫生闸前移：每条验收落盘提交前自查本切片 diff——「范围与整洁」列举逐项过 + 本切片所及注释与代码矛盾，有则本切片内清除；护栏列举补测试专用物。冒烟 S13 | 依据：2026-09-10 返工（修复循环两轮引入测试专用物，死导入/矛盾 javadoc 评审才发现；09-04 升格只进评审维度未进执行步）+ 用户裁定四条全落 | 回归：对照改后第 4 步——旧稿验证通过即提交（卫生留给第 6 步评审兜底），新稿提交前切片自查；S1–S12 未触；description 未改，触发集不跑 | 减法审查：护栏「范围与整洁」保留作原则与列举信源，第 4 步加操作闸（同 09-04 interview 护栏/判别步分工先例），无可删 | 待验证：下一真实任务切片提交前是否自查；测试专用物进评审是否绝迹

- 2026-09-10 | code-review 发现与裁决加同模式归并：同模式缺陷归并为一条发现、全部实例位置逐个 file:line，修复针对这一类，不一处一条分批报。机制移植自 debug「修这一类，不只修这一处」——诊断缺口一句话：评审发现按目击点逐条，同类实例靠多轮完整评审分批捞（09-10 失败分支写过程日志三批、测试专用物两批）。冒烟 R09 | 依据：2026-09-10 返工 + 用户裁定四条全落 | 回归：对照改后第 3 步——旧稿同模式可一处一条分批报，新稿归并一条列全实例；R01–R08 未触；description 未改，触发集不跑 | 减法审查：无可删（分级与裁决句不变；第 4 步 scoped 重审句保留，管修复差异验证） | 待验证：下一真实评审同模式缺陷是否一条归并列全实例；同类问题的评审循环轮数是否下降

- 2026-09-10 | implement 第 2 步全称断言拆分支：「无论/任意/所有/每个」类断言按分支全集拆成逐项验证，一个实例测试不得给全称断言标绿；列不出分支全集回 spec 补失败面枚举。冒烟 S12 | 依据：2026-09-10 返工（「无论成功或失败都写过程日志」一条清单一个测试标绿，三失败分支未接线、评审三轮捞完）+ 用户裁定四条全落 | 回归：对照改后第 2 步——旧稿逐条抄录按条验证（全称断言一条一项），新稿全称拆分支逐项；S1–S11 未触；description 未改，触发集不跑 | 减法审查：无可删（「写不出验证手段回 spec」管可执行性，新条管全称覆盖，不重叠） | 待验证：下一真实任务含全称断言时是否拆分支；一个实例测试给全称断言标绿是否复发

- 2026-09-10 | spec 第 5 步加失败面枚举：写验收标准前列出失败入口全集（校验点、外部调用、异步/超时/中断边界）逐个给出断言；语义未定的失败入口 = 未决决策回第 3 步访谈，不留白。冒烟 S3 | 依据：2026-09-10 返工（智能体节点评审循环替需求阶段补写 5 批失败语义断言）+ 用户裁定四条全落 | 回归：对照改后第 5 步——旧稿只要求每条用户故事至少一条可验证标准（采样式，失败面合法留白），新稿失败入口全集逐个断言或进访谈；S1 讨论出口、S2 禁整批跳过未触；description 未改，触发集不跑 | 减法审查：无可删（第 3 步「边缘情形」保留为访谈路由目标；第 6 步边界自查是减法，不重叠） | 待验证：下一真实多失败入口能力的 spec 是否先枚举失败面；未定语义是否进访谈而非留白

- 2026-09-09 | implement 完整评审可合入后必须问三选一（合入基线 / 保留当前 worktree / 丢弃该任务），不自选。合入或保留 → 第 7 步归档（合入再走合入节）；丢弃 → 不归档不改基线、卸树删分支、删任务文件。非 git 不问直接归档。冒烟 S11、S10 补问句 | 依据：2026-09-09 用户裁定（本会话） | 回归：对照改后第 6 步出口——旧稿无发现→第 7 步静默保留 worktree；新稿无发现→问三选一；S1–S9 未触；description 未改，触发集不跑 | 减法审查：合入节「用户发起」改为「选合入或事后要合入」，与问句不双写默认；第 7 步仍不合入 | 待验证：下一真实完整评审可合入是否问这三项、丢弃是否不进 archive

- 2026-09-09 | implement 归档与合入拆开；scoped 清零后必须再完整评审才归档。第 6 步改名归档前核对：验收后自动完整评审；scoped 无发现→立刻再派完整评审，不得停。第 7 步归档只移任务文件，不合入/不拆树/不 squash。新增「合入」节（用户发起；合入前无完整评审则先评）。code-review 修复循环：scoped 清零不是终点，调用方须再完整评审。DOMAINS 新增归档/合入词条。冒烟 S6 改合入、S7/S10/R02/R08 | 依据：2026-09-09 返工（近两天无 archive）+ 用户裁定（归档≠合入；scoped 清零后须自动继续；合入前无完整评审也要评） | 回归：对照改后正文——旧稿第 7 步熔归档+合入、scoped 可合入可停；新稿归档≠合入、scoped 清零→再完整评审→归档；S1–S5/S8/S9 硬性仍成立；description 未改，触发集不跑 | 减法审查：第 7 步删合入/拆树/squash（迁合入节，非删规则）；第 6 步删「合入基线前」主语（改归档前）；合入前完整评审仍在合入节+S8 | 待验证：下一真实任务 scoped 清零后是否自动再完整评审并归档；用户发起合入时无完整评审是否先评

- 2026-09-07 | implement 归档合并遗留询问收窄为仍成立的未完成/待办；范围外仍成立留汇总不问、已消化进备查。冒烟 S9 硬性同步，来源去掉 09-04/06 单任务范围外未问 | 依据：2026-09-07 本会话审查问题 2/3 + 用户指令 fix | 回归：对照改后归档合并节——旧稿问范围外+未完成/待办，新稿只问未完成/待办、范围外留汇总不问；提交默认未动；S1–S8 未触；description 未改，触发集不跑 | 减法审查：问句删范围外；S9 来源删单任务范围外未问；「已消化安全删除」与备查保留 | 待验证：下一次归档合并是否只问未完成/待办、范围外是否不问

- 2026-09-07 | skill-creator：审查报告先列问题（哪里、为何）再写是否动手，结论禁止用证据闸状态；落盘即提交钉死事先授权，与宿主「未经要求不提交」冲突时仍提交不问。用户级 AGENTS.md 冲突裁决加例外。冒烟 S3 改硬性、补 S6/S7 | 依据：2026-09-07 本会话返工（看不懂结论 + 改完不提交） | 减法审查：分路「产出分级」被问题清单取代（分级仍作是否动手的内部闸，不删证据闸本身） | 待验证：下次仓库审查是否先列问题；下次 skill 改动是否自动提交

- 2026-09-07 | 触发划界三处：①tdd description 加「是否免新失败测试由正文判定，加载前不自判」，正文无行为变化补「仍加载本 skill」（依据：08-26 N4-final reasoning 自判豁免）；②domains description 去掉「歧义信号（一词多义、理解偏移）」、加加载前不自判、负向指向 interview；沉淀时机「理解偏移」改为「同一术语被用成不同含义」（依据：09-01 P3 不到场 + 09-02 配置项现场要的是点选不是沉淀）；③interview 负向「术语表维护」改为「概念定义、命名选择与词条沉淀」与 domains 用于段对偶。触发集：tdd 边界对补小重构；domains 加 N4（interview P1 原话）；interview 结果区补对照。干净会话未跑 | 依据：2026-09-07 用户指令 fix 3 | 减法审查：删 domains 用于段与沉淀时机里和 interview 重叠的「一词多义/理解偏移」；tdd 无行为变化旧句被「仍加载」吸收非另加规则 | 待验证：P3 概念讨论 domains 是否到场且不立无信号词条；N4 配置项是否只触 interview；小重构写码是否不再自判跳过 tdd；T1 能力变更 tdd 是否仍不抢入口

- 2026-09-07 | implement 完成步整体重构：旧「第 6 步完成与归档」拆成第 6 步合入前核对（merge/lint/声称/注释/完整评审）与第 7 步归档（前端对接/落盘/squash）；熔成一句的声称+注释+评审拆开。冒烟 S1–S9 对照硬性仍成立。任务文件模板共置第 2 步；DOMAINS 消费者改为「归档」步 | 依据：2026-09-07 用户指令 fix 1（全量 review 结构项一字批准） | 回归：对照改后正文——旧稿一步七类完成物且声称/注释/评审熔句，新稿两类完成物分步、熔句拆开；规则集合不变；description 未改，触发集不跑；减法审查：删与护栏「验证状态实时落盘」重复的「中断无需额外操作」 | 待验证：下一真实任务是否按第 6 步逐项核对后再走第 7 步归档

- 2026-09-07 | implement 归档合并补提交与遗留询问：汇总写入后提交（执行后告知）；核对后仍成立的范围外问题与未完成/待办列出并问是否现在修复或完成，未批准不修。冒烟 S9 | 依据：2026-09-07 用户投诉（审视上周任务：合并不提交、遗留不问）+ 上周 ey-timp-lab 现场（09-02/03/04 汇总无遗留询问；09-04/06 单任务范围外问题未问） | 回归：对照改后归档合并节——旧稿只核对汇总备查，新稿提交默认 + 遗留裁定节点；S1–S8 未触；description 未改，触发集不跑；减法审查：无可删（第 6 步「并提交」管单任务归档，本条管多份合并；执行中「不顺带修」与合并时询问不重叠） | 待验证：下一次用户发起归档合并是否提交且先问遗留

- 2026-09-07 | implement 第 6 步：合入基线前最近一次评审必须是工作分支相对基线的完整评审且无未处理发现，scoped 重审清零不够。code-review 钉死完整评审 vs scoped 重审的范围。冒烟 S8/R07 | 依据：2026-09-07 用户裁定「合并/提交到基线前应对工作分支/差异做完整 review」+ 同日 ses_f863478c 多轮 scoped 后合入 | 回归：对照改后第 6 步——旧稿 scoped 清零可归档，新稿合入前最近一次须为完整评审；S1–S7/R01/R03–R06 未触；description 未改，触发集不跑；减法审查：删「裁决可合入 → 归档」不区分评审种类的箭头，改为完整评审且无未处理发现 → 归档；scoped 重审仍保留给修复循环 | 待验证：下一真实任务修完 Minor 后合入前是否再派完整评审

- 2026-09-07 | implement 完成定义与归档门补「无未处理评审发现」；第 6 步「有 Critical/Important → 修复」改为「有发现（含 Minor）→ 按 code-review 修复循环」。code-review：可合入仅当无未处理发现；Minor 默认自动修、不询问、不记待办。冒烟 S7/R06 | 依据：2026-09-07 返工 ses_f863478c + 用户裁定「Minor 自动修→scoped 重审→无未处理发现才算完成」 | 回归：对照改后正文——旧稿可合入即归档、Minor 待办；新稿仅 Minor 也修复后重审、无未处理发现才归档；S1–S6/R01/R03–R05 未触；description 未改，触发集不跑；减法审查：删「Minor 记录待办」与 implement「有 Critical/Important」子集列举，分级仍三档、修复循环仍 scoped 重审 | 待验证：下一真实任务仅 Minor 时是否自动修且不归档

- 2026-09-07 | implement squash 收口「询问是否压缩」改为默认压缩、执行后告知该序列；已推送仍须明示。冒烟 S3 | 依据：2026-09-07 用户裁定（核对上周任务，总是批准结束后合并 commit）+ 同日返工（ey-timp-lab 09-06 连续 squash 均批是） | 回归：对照改后 squash 条——旧稿列出序列询问、确认后五步，新稿默认五步、执行后告知；S1/S2/S4–S6 未触；description 未改，触发集不跑；减法审查：删「询问是否」「确认后」，授权从必须先确认降为执行后告知，五步与已推送明示保留 | 待验证：下一真实归档是否不问直接压、已推送分支是否仍停下

- 2026-09-06 | implement 归档「移除 worktree」钉死含删除本地任务分支。冒烟 S6 | 依据：2026-09-06 返工 ses_f8b4637e3ffe + 用户一字批准「移除worktree的含义也包括移除本地分支」 | 回归：对照改后归档条——旧稿只卸工作树可留任务分支，新稿同一完成物含删本地任务分支；S1–S5 未触；description 未改，触发集不跑；减法审查：无可删（squash 只删 -back，归档条管任务分支，不重叠）；~/.agents 与仓库双副本 md5 一致 | 待验证：下一真实归档是否删任务分支

- 2026-09-06 | interview 判别补路径多义：同一字段多写入路径，用户只点一条、其他路径是否同样约束未说 → 多义，问；不因字面只点一条就当未指定。补「路径多义」示例。spec 第 3 步补：任一条未决 → 不得因其余已明整批跳过。冒烟 interview S4、spec S2 | 依据：2026-09-06 返工 ses_f8b781b8fffe（写出登记未定后访谈跳过，用户裁定原请求也应影响 add、这是 agent 应提出的疑问）+ 用户一字批准双落 | 回归：对照改后正文——旧稿「生成的」可走未指定默认只改生成器、部分已明可整批跳过；新稿多写入路径→问、未决未清空不得整批跳过；S1 多义仍问、S2 一种解读仍跳过、S3 先查事实仍成立、spec S1 讨论出口未触；description 未改，触发集不跑；减法审查：未指定走默认与「决策都已有明确答案 → 跳过」保留，新条收窄其范围，无可删 | 待验证：下一「只点生成/展示、同字段另有写入路径」现场是否先问

- 2026-09-04 | interview 判别步首条钉死：能自己查的事实先查完再判别（任务文件、git log、代码、仓库）；把本可查到的事实分类当成多义问人 → 不准。补「事实当多义」反例。冒烟 S3 | 依据：2026-09-04 返工（先问 1/2/3/4，用户纠正应搜今日任务）+ 用户一字批准钉进开场 | 回归：对照改后正文——旧稿护栏有「事实自己查」仍先问人，新稿判别先查；S1 多义仍问（对照仍成立）；description 未改，触发集不跑；减法审查：护栏「事实自己查，决策才问」保留作原则，操作信息进判别步，无可删 | 待验证：下一「今天的任务/提交里有答案」现场是否先搜再问

- 2026-09-04 | implement 完成定义改为验收通过且未让已有声称失真；第 6 步归档前读所涉 spec/design（不在 diff 里也要读），失真走第 5 步；所及类型/方法 javadoc 与类注释行为事实与代码不一致则改注释或改代码；不把 design 写成代码镜像、不补写新注释。code-review 派发补所涉 spec/design 路径，评审者读代码与断言；规格符合度补断言仍为真；所及注释与代码矛盾 = Important。冒烟 S5/R05 | 依据：2026-09-04 返工（c30e601 / script-scan-log-cause / 7e20f55）+ 用户一字批准双落 | 回归：对照改后正文——旧稿完成=验收通过、评审只读代码只对任务清单，新稿失真走偏差路由、注释矛盾 Important；description 未改，触发集不跑；减法审查：「只读代码」「只给三样」被替换为读断言/四项输入，完成定义「只有验收标准通过」被放宽，无可另删 | 待验证：下一真实归档/评审是否读所涉章节、所及注释失真是否仍被标 Minor 放行

- 2026-09-04 | tdd 最小实现补签名面（方法参数须被方法体引用，未引用则不加）；implement 整洁「死代码」列举补方法体未引用的参数；code-review「死代码」同补，发现与裁决钉死本任务引入的死代码、测试专用物 = Important。冒烟 T09/S4/R04 | 依据：2026-09-04 返工 ses_f9446b0a（用户：应在开发与 review 中避免）+ ses_f97eac 评审实锤找到后标 Minor | 回归：对照改后正文——旧稿死参数可被标 Minor 放行、开发无签名面唤醒；新稿开发不加未引用参数、评审 Important 修复后重审；description 未改，触发集不跑；减法审查：无可删（旧「死代码」空钩子保留作类名，补操作信息） | 待验证：下一真实编码/评审是否拦未引用参数、测试专用物是否仍被标 Minor

- 2026-09-03 | domains 选词优先级补负向边界：圈内习惯说法先译成中性领域说法再走本表，不立俚语词条；社区共识是稳定指称，不是俚语。冒烟集冷启动 S1（同会话原话） | 依据：2026-09-03 返工事件（dogfood 从会话俚语进入 00 文档再升格成 spec 角色）+ 用户一字批准「禁止招募俚语」 | 回归：对照改后正文——旧稿社区常见习惯说法可走第 3 档招入，新稿先译不立条；description 未改，触发集不跑；减法审查：无可删（旧选词表无负向边界，不重叠） | 待验证：下一真实命名现场是否拒招俚语、中性译名是否被过度立条

- 2026-09-02 | 创建 interview skill（model-invoked）+ 常备层歧义裁决：守护「多种合理解读或未决决策当前，列出候选停下问，不静默自选」；description 锚点=多义（导向不同产物/答案）+ 可达性（spec 澄清访谈路由）；负向=落 spec 断言 / 讨论出口 / 未指定走默认值 / domains 术语表。spec 第 3 步访谈协议抽出改 <use-skill>interview</use-skill>（单一信源），护栏「信息不足」改指第 3 步。用户级 AGENTS.md 紧挨冲突裁决加「歧义裁决」段（先加载 interview，不静默自选）。冒烟/触发集冷启动摘同会话原话 S1/S2、P1/N1 | 依据：2026-09-02 返工事件 ses_f9dff28fbfferh37（reasoning 检出 This could mean 仍自选，两轮纠正零 question）+ 用户裁定建访谈 skill、范围=抽取 spec 澄清访谈成通用协议、调用=model-invoked+常备层指针 | 回归：触发集未跑（待独立会话）；减法审查：spec 澄清访谈协议正文删除（迁出，无双写）；用户级 AGENTS.md 不在 git。待验证：下一多义开场是否先问；description 与 spec/skill-creator 触发竞争；N1 是否欠触发 interview 过触发

- 2026-09-01 | spec 豁免判断新增「讨论出口」（第三路判断）：判不豁免但目标轮廓未成形（用户明确要先讨论，或输入只有方向与参照、划不出能力边界）→ 告知「目标轮廓未成形：先讨论，不落文档」；讨论期不落任何断言文档（spec/domains/design 均不动笔）、未决决策整批列出每条附推荐、产物=会话内决策清单、用户逐条拍板收敛后回第 2 步 | 依据：2026-09-01 返工事件（ses_fa3ba5c7 用户纠正「我觉得可以先不从spec开始，而是先从讨论开始」）+ 用户裁定不建独立讨论 skill（观察名单保留拉动条件）、条款一字批准 | 回归：冒烟集新建 S1（同会话原话），两轮预置跑法 ✓——spec 加载、告知语逐字发出、10 条决策清单整批附推荐、零断言文档写入；单发跑法 ×2 未到达出口（语境饥渴致开场被事实追问接管，观察名单「干净会话覆盖不了累积上下文退化」新实例）；description 未动（触发必达不变），触发集无需重跑 | 减法审查：无可删（无旧规则覆盖此分支）；SKILL.md 与冒烟集双副本 md5 一致 | 待验证：下一愿景级输入真实会话是否主动走出口；「目标轮廓未成形」判别的过/欠判；单发形态愿景输入 spec 不触发是否复发

- 2026-09-01 | implement 完成与归档新增 squash 收口条：任务分支合入基线后，本需求 spec→归档的本地提交多于一个 → 列出序列询问是否压为单个提交；确认后按 patch 式钉死五步（生成区间 patch 含 --binary → 基线分支改名 -back 保留 → 压缩起点重新检出 → 应用 patch 提交单提交 → 新旧分支 diff 等价比对为空才删 back）；含已推送提交时改写与强推须用户明示；冒烟集补 S3 | 依据：2026-09-01 返工事件 + 观察名单 #53 条件达成（08-28 patch 式协议为钉死步骤来源）；用户裁定协议钉死 patch 式、其余按草案一字批准 | 回归：玩具仓库实跑五步——4 提交（含二进制）压为单提交、比对为空删 back、二进制完好；S1/S2 模板节未触不受影响；减法审查：无可删（归档条管合入动作，新条管合入后的历史收口，不重叠）；~/.agents 与仓库双副本 md5 一致 | 待验证：下一任务归档是否主动询问、patch 式五步真实任务执行度

- 2026-09-01 | implement 前端对接模板补两句：出参 JSON 用完整响应代码块、禁止散文摘要；错误码「码」列写接口对外暴露给前端的错误取值，不写内部枚举名或常量名（不钉死字段名为 msg） | 依据：2026-09-01 返工（ey-timp-lab 前端对接两轮），用户一字批准 | 冒烟：S1/S2 对照改后模板通过；description 未改，触发集未跑；`frontend-api-doc` 不复制（单一信源仍为 implement 模板节） | 待验证：下一真实接口类任务收尾是否按新约束产出

- 2026-09-01 | 创建 frontend-api-doc skill（model-invoked，创建工作流四步）：存量场景从已有代码（与 spec，如有）全量生成/重建前端对接文档——定范围（用户指定 > 全项目扫描含前端可见接口的上下文）→ 读现状（spec 缺失 → 从代码生成+文档声明信源+提示 spec 补建；spec 与代码不一致 → 停下报告，断言修订走 spec 后再生成，保派生方向）→ 生成（旧版全量替换，派生视图无增量维护）→ 落盘即提交；模板单一信源=implement「前端对接文档模板」节（遵循不复制）；implement description 负向补划界、冷启动节补路由、DOMAINS 词条消费者同步 | 依据：2026-09-01 用户指令「这也值得作为一个单独的 skill，用来给已有代码生成前端对接文档」——增量刷新已落 implement（cf48495/c141374），存量生成无属主；无返工证据按冷启动先例（spec 迁移能力 08-24） | 冒烟：玩具项目删除已生成文档模拟存量，按新 skill 流程重生成，与原产物 diff 为空（两 skill 遵循同一模板信源殊途同归验证通过）；~/.agents 与仓库双副本 md5 一致 | 待验证：真实存量项目首用（触发、范围扫描、spec 缺失分支表现）；description 过/欠触发；创建期不建测试集（无真实语料，待首用语料到场）

- 2026-09-01 | 对接文档收窄为给前端的接口文档并统一改名「前端对接文档」：删联调步骤节；入参/出参按媒体类型分档（formData→表格 | 字段|类型|必填|说明|，JSON/XML→对应代码块真实示例）；错误码始终表格（| 码|含义|对接方处置 |）；「对接文档」改名「前端对接文档」——DOMAINS 词条、文件名 `<BC>-前端对接.md`、implement 条款「前端对接面刷新」与判定收窄为前端可见接口（服务间接口不再触发）；worktree 护栏措辞同步 | 依据：2026-09-01 用户三条修订指令+命名提议（冒烟产物评审反馈） | 冒烟：玩具项目 订单-前端对接.md 按新模板重产出——JSON 档代码块、formData 档表格、错误码表格、无联调步骤四项通过；批量导入接口先补 spec 断言再派生（派生方向正确）；~/.agents 与仓库双副本 md5 一致 | 待验证：首个真实接口类任务收尾执行

- 2026-09-01 | implement「完成与归档」新增对接面刷新产物 + DOMAINS.md 新增「对接文档」词条：触及前后端/服务间对接面（对外接口、字段、错误码增删改）的任务收尾时从 spec 断言与代码刷新 `docs/contexts/<限界上下文>/<限界上下文>-对接.md`（接口契约+联调步骤，读者=前端/下游/调用方），随归档一并提交，未触及→跳过；worktree 护栏同步微调（worktree 内可改清单+对接文档）；对接文档=派生视图，断言信源仍在 spec、不新立断言 | 依据：2026-09-01 用户审视两周任务缺口报告——ey-timp-lab 35 会话与 ey-timp-etl 多会话接口类任务零对接文档产出，SDD 文档集（spec/design/ARCHITECTURE/任务文件）中该产物无属主；用户两项裁定（持续一份收尾刷新、派生视图），提案一字批准 | 冒烟：玩具项目（订单上下文 POST /api/orders 新增）走归档步产出 订单-对接.md，落点/模板结构/派生性三项通过；~/.agents 与仓库双副本 md5 一致 | 待验证：首个真实接口类任务收尾执行刷新与模板可用性；「触及对接面」判定过/欠触发

- 2026-09-01 | domains 冷启动接入 SDD 主链路（四项，双副本 md5 一致）：①spec 正文「分解与读现状」无 DOMAINS.md 分支由「按直觉命名」改为「先走 domains 冷启动判管辖（建不建由 domains 正文判定），再回来命名」；②design 正文「读现状」对称补同款路由行；③domains 冷启动补豁免出口与常备层落地——「每次需求讨论都有领域语言可落地或对齐，『提不出关键概念』不是豁免理由；仅用户明确不需要 → 不建，告知后返回原流程」（用户原句定稿豁免收窄）、AGENTS.md 引用行「不存在则创建」；④domains description 补可达性条款「spec/design 判到项目无 DOMAINS.md 时路由过来冷启动」 | 依据：2026-09-01 返工事件（ey-timp-lab 两周 35 会话 domains 零加载、DOMAINS.md/AGENTS.md 未建，opencode.db 取证）——触发架构代差诊断：spec 08-26 升触发必达后同项目正常触发 14 次（对照组），domains 停 08-04 架构（被动信号 description+项目 AGENTS.md 常备指针）缺常备腿失声，同模式第三起；用户裁定「下一次需求到来时 DOMAINS.md 也应该被自动建立」并四项全批，③豁免措辞按用户断言「每次需求讨论中应该都有领域语言可以落地或对齐」收窄 | 回归：P8（ey-timp-lab 改前副本 141b80a，08-26 原话）✓——spec→domains 依序加载，建 DOMAINS.md（5 关键概念含技能编码/技能文件编码反例辨析）+AGENTS.md 引用行，零生产代码写入；domains 过触发防线 N1/N2/N3 ✓ 均未误触；P3 行为变化经对照组归因竞争场漂移（入观察名单），非本次条款；spec/domains 触发测试集同步维护（P8 预期扩展+R1 路由触发区） | 待验证：下一真实需求会话中冷启动端到端（含落盘即提交）执行度；豁免出口「用户明确不需要」形态出现频次；第二次冷启动后 AGENTS.md 常备指针在 meta 讨论中的防御效果

- 2026-08-27 | prototype-anchor 视图按钮文案改名：「A 覆盖标签」→「显示所有锚点」、「B 锚点清单」→「查看锚点清单」（data-v 内部标识不变），清单面板 note 联动说明同步 | 依据：2026-08-27 用户指令 | 回归：jsdom 冒烟 25/25（新增按钮文案断言）；双副本 md5 一致，服务重启 curl 实测新文案在场 | 待验证：真实页面观感

- 2026-08-27 | prototype-anchor 工具条按钮顺序调整：＋锚点/＋分组 移至 A/B 切换按钮之前，CSS 分隔边距同步从 am-act 挪至 button[data-v="a"] | 依据：2026-08-27 用户指令「添加锚点/分组按钮应该在A/B画布前」| 回归：jsdom 冒烟 24/24（新增按钮顺序断言）；双副本 md5 一致，服务重启 curl 实测顺序在场 | 待验证：真实页面观感

- 2026-08-27 | prototype-anchor 移除 C 层级条视图：工具条 C 按钮、amBuild C 渲染分支、.am-bar CSS、amSync am-bar 宽度特判、AM.colors 五项全删（amChain 保留——信息卡「嵌套于」仍消费）；A/B 两视图行为不变 | 依据：2026-08-27 用户指令「删除C 层级条」；观察名单 2026-08-23 条「变体 A/B/C 用户挑选后收编」部分闭环 | 回归：jsdom 冒烟 23/23（删 C 断言 2 条、加「工具条无 C 按钮」1 条）；减法审查：五处删除 grep 零残留，SKILL.md 无 C 视图断言无需同步；双副本 md5 一致，8420 stop/start 后 curl /__am_client.js 实测 C 痕迹零匹配、A/B 按钮在场 | 待验证：真实页面使用效果

- 2026-08-26 | spec description 收窄稿定稿 + 正文豁免判断收口进仓库：①description 用于段定稿为「任意系统能力变更（加/改/删功能、接口、字段、配置、校验、行为）都要先使用本 skill——能力目标与验收标准未落纸面（会话中给出完整需求不等于已落纸面）同样归管辖；是否豁免由正文判定，加载前不自判」；负向定稿为「不用于 bug 报告与诊断（debug 的入口，诊断到期望层由 debug 路由过来）、原型界面与交互（prototype 的事）、技术方案设计（design 的事）、spec 就绪后的实现与验证（implement/tdd 的事）」——bug/重构声称移除（架构裁定：bug 入口归 debug、重构属豁免面）、「任意」圈定「系统能力」防字面吞噬 prototype/domains/writing/skill-creator 域、负向补 prototype（08-20 UI 拓扑倒挂前科）；②正文「入口判断」重构为「豁免判断」（显式豁免类别 + 路由：bug→debug、其余直接干活归 tdd）、护栏首条删除（被吸收，单一信源）、完整示例引用同步——②为当日 round-3 实测版正文，随本次收口落入仓库副本（此前仓库停在 v3，安装副本停在实验版，三副本现已 md5 一致）；「任意变更」实验版与 v3 双弃 | 依据：2026-08-26 description 竞争场会诊（冲突清单四条：spec×tdd 全编码面重叠、spec×debug 抢 bug、任意变更字面吞噬、负向缺 prototype），用户批准 #3；回归：trigger-P8-final ✓（无 docs 能力变更，spec 加载）、trigger-P7-final ✓、trigger-N3-final2 ✓（debug 单独加载，spec 未过触发）、trigger-N4-final ✓（零加载，重构豁免面正确）；P8 作为无 docs 象限唯一防线入触发测试集（P1 因 datamart 工程不在本机无法保真复跑）| 待验证：真实会话防御效果
- 2026-08-26 | DOMAINS.md 新增「判管辖」「豁免」两词条（SDD 上下文）：判定逻辑单一信源=spec 正文「豁免判断」，入口处只负责先加载不预先自判；豁免=无需 spec 变更的结论之一，豁免的是文档义务不豁免其他纪律 | 依据：2026-08-26 架构讨论中两词跨三载体使用（spec 正文/description、tdd B 条款、拟议门禁），用户批准 #4；消费者闭环=spec 正文豁免判断步、tdd 入口路由条款（均已在用）；按 domains 纪律单独提交
- 2026-08-26 | tdd description 收窄为动作级全称 + 正文 B 条款去存在性条件：①description 用于段「实现功能、修复 bug、重构行为等写生产代码之前」→「所有生产代码变更——写或改生产代码的动作发生时（实现新功能、修复 bug、重构等，无论从哪个流程环节进入）」，负向加「编码请求的入口判管辖（spec 的事；bug 请求入口归 debug）」与「测试代码本身不触发」——tdd 退出请求级入口竞争（治冲突 1：与 spec 全编码面重叠声称，08-25 fc7d3316 tdd 17:08 抢跑现场）；②正文入口路由删「docs/ 下存在对应 *-spec.md 且本次改动改变其能力行为」存在性条件（与 18:12 门禁假阴性同款盲区），standalone 承接编码请求一律先走 spec 判管辖、豁免由 spec 正文判定，并加防回环（自 implement 流程或 spec 判管辖后进入的空满足——spec 判豁免路由回 tdd 时不再弹回 spec）| 依据：2026-08-26 description 竞争场会诊（冲突清单四条），用户声明「所有的生产代码变更都需要走 tdd 流程」并裁定批准 tdd 两项；触发测试集同步维护（T02 与重构用例从「应触发」移出——请求级预期在新架构下反转，断言改按动作级设计）；回归：trigger-T1（spec 集 P8 原话，ey-timp-lab 改前副本）✓ spec 加载且零写码、tdd 不抢跑、豁免判断可见执行后进澄清访谈，trigger-T2（N3 原话）✓ debug 单独加载、tdd 请求级未触发；~/.claude、~/.agents、仓库三副本已同步 | 待验证：P6b 全链路（spec→implement→tdd→code-review）未重跑；standalone 玩具/练手请求被 B 条款一律路由 spec 的仪式成本（无 docs 目录下新建 spec）在真实使用中的接受度
- 2026-08-26 | tdd 正文加无行为变化分支：「无行为变化的改动（注释、格式、纯重命名）：不要求新失败测试，但既有测试必须全绿方可动手、动手后必须仍全绿」——紧随铁律单列一条，**不入例外条款**（tdd 仍适用，仅免新失败测试要求）| 依据：2026-08-26 用户声明「所有的生产代码变更都需要走 tdd 流程」引发的 tdd 架构讨论，发现铁律（没有先失败的测试就没有生产代码）与「所有」字面冲突——无行为变化改动写不出失败测试，不加分支则改注释/typo 被卡死；采用 TDD 经典解法（无行为变化改动在既有绿测试保护下进行）；用户裁定关 2 先行落地，description 收窄（动作级全称）与 B 条款存在性条件修复按用户指示留待后续讨论、不在本批；~/.claude、~/.agents、仓库三副本已同步 | 待验证：分支在真实会话被引用的首次现场；「拿不准是否无行为变化」与例外条款末条「拿不准 → 问人」的协作面
- 2026-08-26 | A（常备层指针）落地为**用户级** `~/.config/opencode/AGENTS.md` 两段：「SDD 门禁——项目 docs/ 下存在 *-spec.md 时：所有代码改动动手前，先加载 spec skill 判管辖；bug 修复走 debug skill」+「冲突裁决——skill 规则与项目文档约束（AGENTS.md/CLAUDE.md 等）冲突时，停下问用户，不静默二选一」| 依据：2026-08-25 否 A 时所约「再发即重提」条件当日达成（22:07 ses_fc6c1074 零加载）；2026-08-26 reasoning 取证（零加载根因=判断事件不发生+请求被项目模板单遍分类接管；误判/觉得没必要两假设证伪；description 层全称断言 P7 0/2 实测无效）；用户裁定两点（多人协作项目的 AGENTS.md 不引用个人 skill → 项目级方案撤回改用户级；逃生口机制采纳为冲突裁决段）；实测：用户级指针+项目竞争模板在场 P7 ✓ spec 先行、N3 ✓ debug 不被吞（trigger-P7-userptr/N3-userptr）；用户级文件不在 git 仓库，按纪律跳过提交 | 待验证：真实会话防御效果（下一个编码形态请求是否零探索直判管辖）；冲突裁决段防的是观察名单 #36 潜在冲突（尚无真实开火案例，属预防性落地）
- 2026-08-26 | spec description 第二版：首条例示扩为「加/改/删功能、接口、字段、配置、校验、行为」（+功能、配置——收编长尾形态，如 8-25 fccb721c「增加上游结果配置字段+重试」类）+ 负向边界加「bug 诊断与修复（debug 的事）」（防「改行为」例示吞 bug 修复）| 依据：用户提出「所有变更都走 spec 评估」方向；v2 全称字面版（「所有代码变更请求」）回归退化 P7 0/2（「变更」词汇缺口：新增类请求不自归类为变更；全称断言在触发时刻无分类力）、v2.1 补例示但挤掉「校验」丢 P6——机制结论：类别锚点是触发面本体，全称表述无效（候选升 design-principles，见观察名单）；v3 文案经用户一字批准；回归：P6-v3 ✓ spec 先行（澄清确认后输出 spec 与代码）、P7 继承 v1 结果（锚点结构未变）、N3-v21 ✓ debug 加载 spec 未过触发、N4-v21 零加载；减法审查：v2/v2.1 全称表述弃用回到 v1 锚点结构，无残留；~/.claude 与 ~/.agents 安装副本已同步 | 待验证：v3 在真实项目会话的防御效果（下一个编码形态请求是否零探索直判管辖）
- 2026-08-25 | spec description 首条触发条件改为编码请求拦截：「改动系统能力的编码请求（加/改/删接口、字段、校验、行为）——先经本 skill 判管辖再编码，docs/ 下存在对应 *-spec.md 即归管辖」（吸收旧第二条「已有能力的增删改」管辖锚点，无残留）；负向边界「不用于技术方案设计、任务拆解、编码实现与验证执行」→「不用于技术方案设计（design 的事）、spec 就绪后的实现与验证（implement/tdd 的事）」——从按请求形态划界改为按生命周期阶段划界，消除负向边界对编码形态请求的排斥（旧版下模型把「加接口」类请求归类为编码实现，借负向边界跳过 spec）| 依据：2026-08-25 返工事件（22:07 ses_fc6c1074 零加载，opencode.db 取证）；用户裁定走 description 修改（A 常备层指针维持未落地，description 版真实防御失败再发即重提 A）；措辞经用户一字批准；回归：测试环境首次预置真实项目 AGENTS.md 竞争模板——P7-old ✗ 零加载（基线复现真实失败）→ P7-new ✓ spec 加载判管辖进澄清访谈、P6（改前源码桩+改前 spec）✓ spec 先行（先改 spec 章节再改代码）、N4r ✓ 零加载无过触发（见 spec 触发测试集结果区）；减法审查：旧第二条被新首条吸收、「任务拆解」删除（implement 负向边界「不用于澄清需求与验收标准」已对侧划界）；~/.claude 与 ~/.agents 安装副本已同步 | 待验证：description 版在真实项目会话的防御效果（下一个编码形态请求是否零探索直判管辖）
- 2026-08-25 | spec+tdd 入口与接续双修：①tdd 纪律首部加「入口路由」——standalone 承接编码请求（非自 implement 流程进入）时先判管辖，`docs/` 下存在对应 `*-spec.md` 且本次改动改变其能力行为 → 先走 spec 再回来，自 implement 进入空满足（治顺序倒置：fc7d3316 中 tdd 17:08 先加载、spec 17:21 补中）；②spec 落盘步末条「完成后告知用户：spec 已就绪，可进入设计与实现」改为分支路由——本会话目标含实现该改动 → 接续 `<use-skill>implement</use-skill>`（design 就绪由其入口自判），纯文档变更 → 告知用户 spec 已就绪（治接续断点：fc7d3316 spec 补完后 implement 全程缺席，无任务文件）| 依据：2026-08-25 返工事件（ey-timp-etl 三会话绕开 SDD，opencode.db 取证）；用户批准 B+C 两项、否 A（常备层指针维持未落地，fc7afd03/fccb721c 零加载路径残留风险存续，再发即重提）；回归：P6（fc7d3316 原话）✓ spec 先行无 tdd 抢跑、N4r ✓ 纯重构零加载无过触发、P6b（含源码桩）✓ 全链路 spec→implement→tdd→code-review 按时序加载 + 任务文件创建（详见 spec 触发测试集结果区）；干净会话复现不了真实会话 tdd-first 路径（观察名单 #12 局限），B 条款真实防御效果待观测；减法审查：两处均为既有句改写/单条新增，无可删；~/.claude 与 ~/.agents 安装副本已同步
- 2026-08-24 | spec 新增迁移能力：description 加锚点「检测到老标准 spec 文档（布局/结构不符现行约定）时提议迁移」；正文入口判断加第三问、新增「迁移」节（老标准签名=docs/requirements/、docs/specs/、docs/<上下文>/ 直挂三个本体系被超越的旧约定 + 结构缺模板四件/混技术方案/UI 拓扑；项目自成一体布局不擅动仅报告；提议出迁移清单含引用同步点、确认后执行；git mv 以上下文目录为单位、design/domains 随同内容不改；结构改写验收写不出回澄清访谈；迁移单独提交 docs-only）+ 第 7 步括注防「跟随项目约定」吞掉迁移候选 | 依据：用户发起需求并裁定三项（范围=布局+结构改写、落点=并入 spec skill、自动化=检测到就提议），措辞经一字批准；无返工证据，按 spec/design 冷启动先例经设计讨论批准落地；回归：P2 ✓ 迁移正确执行（直挂→contexts/、变更历史追加、非 git 不提交）、P3 ✓、P4 ✓ 且主动提议迁移老布局（检测到就提议当场生效）、N4 零加载无过触发；真实使用待验证进观察名单
- 2026-08-24 | spec description 第二触发条件换产物管辖锚点：「改动改变了 spec 已写过的断言」（鸡生蛋——不读 spec 不知是否触及，决策时刻无据可依）→「已有能力的增删改（docs/ 下存在对应 *-spec.md 即受本 skill 管辖）」（文件名模式锚点，任意嵌套深度命中，模型知道「文档存在」即可判）；负向边界「不触及已有 spec 断言」→「不改变能力行为」，消表述打架；讨论中砍掉的元指令半句（"先走本 skill，无需先判断"）以「管辖」蕴含，不入文 | 依据：2026-08-24 返工事件（ey-timp-etl 三轮改动零 skill 加载）；措辞经用户两轮质询（可否挪正文→判别信息决策时刻必须可见故不可挪、路径模式可达性）后一字批准；回归：基线旧版 P3/P4 ✗✗ 复现失败 → 新版 P1/P3/P4 ✓、N2→code-review/N3→debug/N4 零加载无过触发（见 spec 触发测试集结果区）；常备层指针（用户级/项目级 AGENTS.md 一行）为零探索场景的结构性兜底，本次按用户约束未落地，残留风险存续
- 2026-08-24 | 术语表写入权收归 agent：prototype-anchor 服务删 syncDomains 自动同步（saveAnchor 纯写 HTML，术语表只读，`/__am_terms` 读路径不变）；新建 user-invoked skill prototype-anchor-sync（grep 锚点 → 对照已有 → 一轮推荐确认 → 按 domains 格式写入；只增不改、孤儿只报不删）作为锚点进术语表唯一通道；客户端「复制新增片段」升格为交接介质（导出含定义），保存 toast 去除术语表同步提示 | 依据：2026-08-24 返工事件（用户两连指令——先提「手动落盘按钮」方案，讨论后裁定程序退为只读 + 手动 skill；命名 prototype-anchor-sync 用户裁定）；前方案（服务端批量落盘按钮）未落地即被本方案超越；回归：语法检查 + 端到端冒烟（保存仅写 HTML、术语表零改动、只读解析正常）
- 2026-08-24 | 锚定失败可读诊断：prototype-anchor v0.6.1 findByPath 失败返回可读中文诊断（脚本运行时生成/磁盘已变动两因，引导先刷新、再锚静态容器）+ 最近可锚祖先路径；客户端 toast 时长随消息长度伸缩（诊断需可读完）；SKILL.md 锚点服务节补「动态渲染边界」条（JS 渲染 DOM 无源码区间，锚静态容器不锚渲染产物）——修复方向「明确报错+引导，不做 JS 渲染区写盘」经用户裁定 | 依据：2026-08-24 返工事件（用户报障「锚点无法保存」+ curl 复现 settings-panel path not found）；减法审查：无可删（写盘防护条仍独立成立）；回归：settings 路径报新诊断含祖先、静态保存/包盒/删除全过、8420 重启后 v0.6.1 | 待验证：用户对「不确定失败现场」的另一可能因（磁盘变动陈旧 DOM）是否也被新诊断覆盖

- 2026-08-04 | 选词双闸（闸一·招募共识 / 闸二·招来的干净）升入 design-principles.md §5「措辞与组织」 | 依据：2026-08-04 返工事件，用户批准
- 2026-08-04 | 移出：单维「主导词（leading word）」段落（只问招募、不问副作用） | 依据同上
- 2026-08-04 | domains description：加"任何讨论中"前置 + 类别锚点「概念定义、命名选择」；负向边界由「不用于业务功能开发本身」收窄为「不用于执行任务本身」 | 依据：2026-08-04 返工事件（spec-skill 会话未触发）；用户批准方向：domains 应在所有讨论中生效（常备层由 AGENTS.md 一行承担，description 只改触发锚点）
- 2026-08-04 | 统一存放约定升入 domains+spec 正文与 DOMAINS.md：DOMAINS.md 恒在根目录；每上下文一个 `docs/<限界上下文>/`，内藏 `<限界上下文>-domains.md` 与 `<限界上下文>-spec.md`；spec 拆分为 `<限界上下文>-spec-<能力名>.md` | 依据：2026-08-04 返工事件（路径各自为政），用户指定约定
- 2026-08-04 | design 与 ARCHITECTURE.md 词条、spec↔design 1:1 约定、决策归属路由（归属单一上下文 → design；跨上下文/项目级 → ARCHITECTURE.md）升入 DOMAINS.md 与 skills/design/ | 依据：2026-08-04 design skill 设计讨论，用户批准
- 2026-08-04 | git 约定二次补全（implement）：worktree 落点写死默认 `<项目根目录>/.worktrees/<需求名>`（用户指定，原输入 .worktress 纠正为 .worktrees），首次建时把 `.worktrees/` 写入 `.gitignore`（已实测：git 允许仓库内建 worktree，但不 ignore 会污染主检出 status）；升格不变量"worktree 内只改代码与任务文件，spec/design 修正一律回主 checkout"——任务分支永不携带 docs 变更，两处同步（取回修正、归档前）在 docs 上结构无冲突；归档前 merge 基线分支，带入新变更 → 重跑全部验证手段 | 依据：2026-08-04 第二次返工事件；目录约定为用户裁定
- 2026-08-04 | git 约定修订为 worktree 机制（implement 五处）：建任务文件时从基线分支建 worktree（旁系目录，分支名=需求名）；基线分支记入任务文件模板（合并目标唯一依据，不靠猜）；spec/design 偏差修正回主 checkout 提交到基线分支、worktree 同步后继续；归档时合入基线分支并移除 worktree；续写入口补 `git worktree list` 发现路径。spec/design 两行 git 规则不变（主 checkout 恰为其天然现场） | 依据：2026-08-04 返工事件（用户审查指出三缺陷），worktree 方向为用户提议
- 2026-08-04 | git 时机约定升入三个 skill 落盘步骤：spec/design 落盘后单独提交 docs 变更（不开分支，直落主干）；implement 任务文件=隔离单位——建任务文件时开分支（分支名=需求名），每条验收标准验证通过并落盘后提交，归档后合回主干（项目走 PR 流程则改开 PR） | 依据：2026-08-04 用户发起设计讨论并裁定三决策点；无返工证据，经设计讨论批准落地（spec/design 先例）
- 2026-08-04 | 观察名单"Tasks 层 skill"结案：用户发起讨论并裁定创建 implement skill——四机制：验收强制闭环（跨模型一致性靠协议不靠能力）、偏差分路由（spec/ARCHITECTURE.md 问用户，design 先改文件）、拆分不落盘、状态可续写（任务文件为唯一交接介质）；任务文件落 docs/tasks/、完成移入 archive/、skill 命名 implement 均为用户裁定。同步修 DOMAINS.md SDD ④（可追溯性收窄为验收标准级）并新增任务文件词条 | 依据：2026-08-04 implement 设计会话；无返工证据，经设计讨论批准落地（spec/design 先例）；冒烟集欠账，待真实语料到场偿还
- 2026-08-04 | DOMAINS.md 新增「spec 就绪」词条：就绪相对改动判定，双条件（质量——每个用户故事有可验证验收标准，由 spec 边界自查产出时保证；覆盖——本次改动触及的行为被 spec 断言覆盖）；design 入口存在检查改为覆盖检查（无 spec 或未覆盖 → 先走 spec skill 再回来；不触及行为的改动不受此限） | 依据：2026-08-04 用户发起设计讨论并裁定两条版；无返工证据，按 spec/design/git 约定先例经设计讨论批准落地；冒烟集欠账，待真实语料到场偿还
- 2026-08-04 | DOMAINS.md 新增「design 就绪」词条：就绪相对改动判定，双条件（质量——每条固化的决策写有约束/取舍理由，由 design 边界自查产出时保证；覆盖——改动触及的值得固化的 how 问题均已落定 BC design 或 ARCHITECTURE.md）；改动不产生值得固化的决策则 design 不触发、空满足就绪。implement 入口「design 可有可无」改为 spec+design 双就绪主动拦截（有决策未落定 → 先走 design skill 再回来），用户裁定主动拦截而非只靠偏差路由事后兜底 | 依据：2026-08-04 用户发起设计讨论并裁定；无返工证据，按 spec 就绪先例经设计讨论批准落地；冒烟集欠账，待真实语料到场偿还
- 2026-08-04 | implement 入口补齐 design 就绪拦截（第 1 步：改动触及的值得固化的 how 问题未落定 → 先走 design skill 落定再回来；不产生值得固化的决策 → 空满足） | 依据：2026-08-04 术语表消费者审计确认上一条裁定的入口改动未落地，用户裁定补齐；待验证：入口拦截在真实使用中是否过度阻塞（空满足条款为兜底）
- 2026-08-04 | DOMAINS.md 删「任务契约（验收卡）」词条：零消费者，无任何 skill 引用；观察名单对应条目（与任务文件混用风险）随结案 | 依据：2026-08-04 用户发起术语表消费者审计并裁定删除
- 2026-08-04 | DOMAINS.md「特性」词条换为「能力」词条（保留分合判据与跨上下文引用规则，反例记特性已废弃），spec description「特性」改「能力」 | 依据：同上审计并裁定；能力是 spec/design/implement 实际使用的组织词；待验证：description 改词后触发面是否变化（"特性"触发词仍由"启动新功能"侧的用语承接）
- 2026-08-04 | domains 写入规范新增「消费者闭环」条款：新增/修订词条须指明消费端落点并同会话落地验证，指不出消费者不立词条、记观察名单 | 依据：同日两起返工事件（design 就绪落地丢失、任务契约/特性零消费者；前科见路径各自为政条"词条被决策超越未同步"），模式复发，用户批准立规则；不建定期清理脚本（词条表小，grep 即查，预防性建设违反 domains 自身护栏）；待验证：下次沉淀是否真正闭环，条款是否造成过度保守（该立的词条被"指不出消费者"挡住）
- 2026-08-05 | 新建对外 skill 两个：tdd（测试先行纪律：铁律"无失败测试不写生产代码"、垂直切片、RED/GREEN 本轮实跑读输出、期望值手工推导禁自验、变异自检、例外限原型/配置/生成代码）、code-review（独立评审：生成者≠评审者、新鲜上下文评审者只给目标+变更范围、发现分级 Critical/Important/Minor 带 file:line、裁决门禁、修复后 scoped 重审、意见先验证再采纳）。同步改 implement 三处：验证手段优先级"可落成测试 → 该测试"（ATDD 增量并入，不独立成 skill）；执行步骤加 tdd 引用；归档前加 code-review 裁决门禁（可合入才归档）。机制移植自 obra/superpowers（test-driven-development、verification-before-completion、requesting/receiving-code-review），按移植纪律翻译不誊写 | 依据：2026-08-05 用户质疑验证有效性 + 社区调研 + 用户裁定三项（形态=两个 skill 而非三个、调用方式=model-invoked、设计讨论批准落地）；无返工证据，按 spec/design/implement 先例；冒烟集欠账（无真实编码语料，用例为扰动变体），待真实语料到场偿还；待验证：tdd 与 implement 双触发分工是否冲突、code-review 归档门禁是否过度阻塞 | 2026-08-05 真实使用：双触发分工清晰（tdd 管切片纪律、implement 管验收落盘）无冲突；归档门禁 8 Minor→修廉价项→scoped 重审→合入，未过度阻塞
- 2026-08-05 | tdd 新增两条纪律：断言写到最精确（equals 优先 contains、完整值优先片段；断言变弱=保护变弱）、测试失败修实现不改测试（想弱化断言的那一刻=实现未达标信号；怀疑断言写错→问用户，改断言须说明理由）；code-review 测试维度同步加断言精确度与弱化痕迹检查；弱断言场景入冒烟集 T03 | 依据：2026-08-05 返工事件（用户报告弱断言/删断言伪造通过）；改动未跑回归对比，记为待验证 | 2026-08-05 真实使用佐证：code-review 实查捕获 RuntimeException 弱断言并收窄（试运行评审 Minor 1）；正式回归未跑
- 2026-08-05 | tdd 新增一条纪律：测试走生产真实发生的路径（真实入口/依赖/数据流），不为测试在生产代码添加测试专用方法/字段/类型/分支——想开口子=设计不可测信号，改设计不加测试专用物；code-review 代码质量维度同步检查；测试专用物场景入冒烟集 T04 | 依据：2026-08-05 返工事件（用户报告测试专用物/伪造路径）；改动未跑回归对比，记为待验证 | 2026-08-05 真实使用佐证：两次真实评审均查测试专用物/伪造路径维度，未见违规；正式回归未跑
- 2026-08-05 | tdd 真实路径规则扩展：注册/装配面也是真实路径——组件向容器/框架的注册（bean 加载、依赖解析、路由/配置生效）必须有验证覆盖，只测单元逻辑不算验证装配；code-review 测试维度同步加装配面覆盖检查；场景入冒烟集 T05 | 依据：2026-08-05 返工事件（用户报告模型不验证容器注册，如 Spring bean 加载）；改动未跑回归对比，记为待验证 | 2026-08-05 真实使用佐证：试运行实现中主动写 AgentNodeBeanConfigTest 验证 bean 装配；正式回归未跑
- 2026-08-05 | 新建对外 skill：debug（bug 诊断与修复纪律）。主线"未定位根因，不动手修复"；机制五件：先复现（复现不出向用户要条件，不猜测改码）、根因通过判据=解释+预测（防"找到貌似合理的原因即停"）、根因三层路由（期望→spec skill / 设计→design skill / 实现→tdd；无 spec/design 项目退化为"期望行为 vs 实现方式"直接判断）、想加特判/吞异常/默认值绕过=根因未除信号即退回、修这一类不只修这一处；入口排除测试先行/验收循环中的预期失败。修复阶段遵循 tdd（顺序分工，tdd 正文未改）。用户裁定四项：命名 debug、三层路由、与 tdd 顺序分工、冒烟集推迟到有真实语料 | 依据：2026-08-05 返工事件（用户报告模型修表面症状忽略 bug 本质），经设计讨论批准落地；冒烟集欠账，待真实语料到场偿还；待验证：与 tdd 双触发分工是否冲突（同 tdd/implement 先例）、三层路由在无 spec/design 项目上是否误判、"解释+预测"判据是否过度阻塞简单 bug
- 2026-08-05 | implement 新增「范围与整洁」护栏（改动限于验收标准所需，本任务产出不留死代码、注释掉的代码、未使用的导入；顺手发现的范围外问题记入任务文件「范围外问题」节，不顺带修复或删除）+ 完成步骤加 lint/typecheck 门禁（本次改动引入的新失败必修；既有的范围外失败只记录）+ 任务文件模板加「范围外问题」节；code-review 规格符合度扩展至删改面（无未请求的历史代码删除/修复）；DOMAINS.md 任务文件词条同步「三件事→四件事」| 依据：2026-08-05 两条返工事件（保留死代码 / 越界过度清理），用户批准落地；独立 refactor skill 不成立——差值小（lint/typecheck 为工具兜底）、触发面与 tdd「重构行为」重叠（同 BDD 先例）；观察名单第 17 行「项目级全量门禁」部分结案（见该行补注）；冒烟集欠账（无真实编码语料），待真实语料到场偿还；改动未跑回归对比，记为待验证（范围护栏是否过度阻塞验收所需的连带改动） | 2026-08-05 真实使用：两次真实评审查删改面未见违规，无过度阻塞迹象（单案）；正式回归未跑
- 2026-08-05 | tdd 新增一条纪律：最小实现含结构面——每一层、抽象、扩展点（接口/钩子/配置项/预留位）须能指出当前消费者，指不出=过度设计不加（铁律「最小实现」被执行为让测试变绿的最少代码，分层/扩展点不违规，缺结构面判据）；code-review 代码质量维度同步加过度设计检查；场景入冒烟集 T06 | 依据：2026-08-05 返工事件（用户报告：AI 经常写很多层代码、设计很多插入的入口），用户批准双落点；改动未跑回归对比，记为待验证（消费者判据是否误伤项目约定必需的分层） | 2026-08-05 真实使用：约 25 新类多层结构（port/runtime/tool/output/log）过消费者判据无误伤（单案）；正式回归未跑
- 2026-08-05 | spec 新增「澄清访谈」步骤（第 3 步，原 3–6 步顺延为 4–7 步）：写 what/why 前枚举未决决策（为什么做、给谁用、做到什么边界、边缘情形）逐条走，一次一问、每问附推荐答案，事实自己查（现有 spec/DOMAINS.md/代码）决策才问用户，决策皆明 → 跳过；验收标准写不出 → 回访谈继续问；完整示例同步补澄清环节。机制移植自 obra/superpowers brainstorming 与 mattpocock/skills grilling，按移植纪律翻译不誊写（HARD-GATE 不移植——spec 就绪入口已承担；2-3 方案对比不移植——属 design 层 how，不进 spec） | 依据：2026-08-05 用户指出缺口 + 社区源机制对照 + 用户裁定并入 spec；无返工证据，按 tdd/code-review 先例；回归未跑（spec 无冒烟集，此前按用户决定推迟），记为待验证；待验证：简单需求过度提问、更新型需求是否被跳过条款正确豁免、模型是否先枚举后问 | 2026-08-05 真实使用：落纸面前一次一问单个行为决策、无过度提问；更新型豁免场景未演练

- 2026-08-05 | spec description 加判别澄清"（会话中给出完整需求不等于已落纸面）" | 依据：2026-08-05 返工事件（真实项目完整需求输入欠触发），用户批准；回归：失败输入 P1 新版触发 ✓、N2/N3 负例 ✓（spec 触发测试集首轮建成并跑，见结果记录）
- 2026-08-05 | implement description 加类别锚点"（含在已有能力上追加新能力）"，用户裁定不设比例性豁免（触及 spec 断言一律走 implement） | 依据：2026-08-05 返工事件（追加功能欠触发），用户批准；回归：P3 ✓、N1 ✓，P1 部分验证（流程正确以 spec 先行，implement 接续待真实使用观测，见结果记录）
- 2026-08-05 | 结案：升格区 6 条"待验证"（tdd 弱断言/真实路径/装配面/结构面、implement 范围整洁、spec 澄清访谈）| 依据：2026-08-05 智能体节点全流程真实使用佐证（见各条补注），用户裁定接受佐证结案，不再追正式回归；冒烟集工作流用例欠账维持
- 2026-08-05 | 一致性修缮（用户批准直接修）：domains 触发测试集移入 skills/domains/tests/；tdd/code-review 冒烟集补跑法说明与结果记录区；spec/implement 触发测试集首轮建成（真实语料：两条失败输入原话 + 迁移/续写/评审会话原话）

- 2026-08-05 | 新建对外 skill：bootstrap（项目初始化：多级 AGENTS.md 生成/增量更新走标记区机制、重复段落收编+确认删除、按事实生成/同步 DOMAINS.md 与 ARCHITECTURE.md（已有文件改动先出清单确认）、codegraph 只装不更新含 CLI 未安装分路；名称、标记区机制、冒烟集同期建成均为用户逐项裁定）| 依据：2026-08-05 用户发起需求会话并裁定，无返工证据，按 spec/design/implement 先例；冒烟集欠账按用户决定待真实案例到场偿还
- 2026-08-05 | bootstrap 转 user-invoked：移出 description（人做索引，零常驻成本）；触发测试集随测试对象消失移出，真实语料两行并入冒烟集 S1/S3 | 依据：2026-08-05 用户指令"bootstrap 只能手动触发才对"，属创建期调用方式裁定的当日修订 | 待验证：手动唤起是否顺畅（"bootstrap"名称是否足以让人想起它）
- 2026-08-06 | tdd 新增一条纪律：写与既有同类平行的代码（新 FormData、新枚举值、新组件）前先读 2–3 个同类，对齐其结构/命名/错误处理模式；≥2 个一致实例才算惯例，个例不强制对齐。code-review 代码质量维度同步加同类惯例一致性检查（实际读同类文件对比，不接受实现者自述）；场景入 tdd 冒烟集 T07、code-review 冒烟集 R03 | 依据：2026-08-06 返工事件（试运行评审两轮可合入后，用户仍发现 EtlNodeAgentFormData 无层级、异常魔法值两处同类惯例不一致），用户批准双落点；改动未跑回归对比，记为待验证（同类判定过宽误伤、过度对齐陈旧代码坏习惯）

- 2026-08-06 | 观测基础设施落地（harness 落地顺序①②）：scripts/skill-observe.py + scripts/skill-observe.hooks.json（安装至用户级 ~/.codex/hooks.json，/hooks 信任后全面生效），捕获 SessionStart/SessionEnd/UserPromptSubmit/PostToolUse（matcher: Bash、apply_patch）→ skill-事件日志.jsonl（只捕获不判断）；session.start 行带 model slug，model.change 通道接通；周心跳 automation「skill-观测心跳」同批建；「事件日志」词条入 DOMAINS.md（新开限界上下文 skill 治理）；端到端冒烟通过（codex exec --dangerously-bypass-hook-trust 实跑，session.start/prompt.submit/session.end 三行到齐）| 依据：三次欠触发返工事件（2026-08-04 domains、2026-08-05 spec、2026-08-05 implement）暴露取证无现场记录；观察名单第 1 条证据通道名存实亡；用户批准四项裁定（用户级配置 / prompt 截断 4000 落盘 / 心跳同批 / 命名沉淀）| 待验证：桌面 app 会话的 hook 触发面（CLI exec 已验，app 未验，心跳兜底）；Bash 相对路径 scope 判定依赖会话 cwd；system/plugin skill 加载不在匹配面

- 2026-08-18 | 限界上下文目录收编为 `docs/contexts/<限界上下文>/`（与 `docs/tasks/` 并列，docs/ 一级目录语义单一）；DOMAINS.md 限界上下文/design 词条及反例、spec/design/implement/domains 四 skill 正文与 spec/implement 触发测试集预置路径同步，`~/.agents/skills/` 安装副本已同步；升格区 2026-08-04「统一存放约定」条的路径表述被本条超越 | 依据：2026-08-18 返工事件（层级混淆），父目录命名 contexts 经讨论裁定（否决 domains——与术语表撞词且 domain≠bounded context；否决 specs——目录内藏 domains/spec/design 三种文档，以偏概全）；纯路径改名不涉触发与行为，回归以 rg 全量核查代替（无残留）

- 2026-08-18 | tdd 新增验证信道三条纪律（跑测试前先立信道：环境版本/未改动基线/工作目录，任一不成立测试输出不算证据；同一命令第二次因环境/路径失败即机制化；写入共享本地状态即记账、模块间依赖优先同一构建进程内解析）+ 冒烟集 T08；debug 定位根因加依赖源码定位序（搜本机工作区 → 问用户 → 依赖自带 sources → 按构建工具依赖树确认实际解析版本后反编译，不取默认最新版）| 依据：2026-08-18 两条返工事件（CE脚本会话 ses_fed5a3031），用户批准并修订 debug 条款（问用户优先、反编译版本须锁定项目实际依赖版本）；环境具体技术（JDK/版本管理工具/父 pom/本地仓库）按用户裁定不进正文，仅作案例留证据日志；改动未跑正式回归，本次会话作真实使用佐证（同 T03–T06 先例），记为待验证

- 2026-08-19 | 统一对外 skill-creator：合并原 `.agents/skills/` 私有对（skill-creator 创建 + skill-iteration-review 迭代）为 `skills/skill-creator/` 单 skill（创建/迭代两条分路）；references 收编 design-principles、testing-guide、harness；AGENTS.md 改指对外版；`.agents/skills/` 私有对删除改 README 指针；writing 负向边界与 DOMAINS/scripts 消费者路径同步；`scripts/install.sh` 已装至 `~/.agents/skills/skill-creator/` | 依据：2026-08-19 用户发起需求并裁定三项（形态=统一对外版接管、调用=model-invoked、范围=创建+迭代+原理+测试+移植+harness）；无返工证据，按 bootstrap/tdd 先例经设计讨论批准落地；S1 本会话自举；待验证：统一 description 是否过触发 writing/业务任务、创建与迭代分路是否被跳过、本项目 agent 发现是否只见新路径
- 2026-08-19 | skill-creator 创建工作流：删「建创建期冒烟集」步骤（五步→四步）；意图锚点/交付判据改为「不造无真实语料的测试集」；testing-guide 文首加时机条款（使用期/返工再建，0 条真实不生成凑数）；skill-creator 自身冒烟集 S1 断言同步 | 依据：2026-08-19 返工事件（用户指出创建期无真实场景不应建测试集）；与既有证据日志多条「冒烟集欠账待真实语料」先例一致
- 2026-08-19 | skill-creator 三处表述修缮：（1）移植纪律「双关」→「两道检查」；（2）升格门槛统一为「须用户一字批准」（删「一次本地确认」）；（3）迭代四步各补完成物一句（病灶层+机制句 / 模式+证据+破坏面或观察名单条目 / diff或无改动 / 对比结论+冒烟结果+加减对应）| 依据：2026-08-19 审查观察名单三项，用户指令「先执行2」一字批准；减法：模式句段与完成物合并，避免双写；未动诊断表「正确方向」、未跑 S2–S5（仍观察）
- 2026-08-19 | 调用标记规范 v0.2 落成 references/invocation-markers.md：四种载体（<use-skill> / <mcp server tool> / <tool> / bash 首行 `# skill-run` 块）；经一轮方案审查修订四处硬问题——XML 区禁裸 <（占位符 {x}）、info string 改首行注释保 GitHub 高亮、<skill> 避 harness 同形异义改 <use-skill>、每载体单一写法；<function> 对齐宿主词表改 <tool> | 依据：2026-08-19 用户发起规范需求（要从正文代码解析依赖），经设计讨论与方案审查由用户批准整理成文；无返工证据，按 design/spec 先例；待验证：解析脚本落地、现有 10 skill 改写后抽取完整性、lint 防静默缺项；结构性张力记入观察（与 scripts/ 分工、标记膨胀）
- 2026-08-19 | parse-skill-invocations.py 落地（scripts/，Python3 标准库，parse+lint 双模式）| 依据：用户指令「编写对应的解析脚本」；夹具验证通过（sudo -u 带参 flag、command -v 特判、管道右侧忽略、续行合并、旧式/畸形标记 lint error、inline code 不抽取）；`--all` 首跑 10 skill 全空（未迁移，符合预期），lint 即捕获 bootstrap×2 + implement×1 真实漏标内联命令——「防静默缺项」设计当场生效；规范文档 §5 同步由「待建」改实例 | 待验证：现有 skill 迁移标记后抽取完整性复跑
- 2026-08-19 | bootstrap 试点迁移完成：§5 三条命令合一 skill-run optional 块；抽取结果精确命中预期（shell-optional=[codegraph, npm]，skills/tools/mcp/shell 全空）；全仓 lint 警告 3→1（余 implement 的 git 内联）。试点发现三条澄清升入 invocation-markers.md 规则 9–11：提及≠调用（归属提及不标）、<tool> 只标非默认工具、同步多命令可共块（条件放行内注释）| 依据：用户指令「进行试点」；首个真实迁移用例，验证规范可执行性与抽取正确性；规范 §5 例块顺带成为自证样本 | 待验证：implement 等其余 skill 迁移；规范文档自身被解析时的示例块噪音（warn 级，可接受）
- 2026-08-19 | shell 块标记 `# skill-run` 改名 `# invocation`：用户指出 skill-run 易误读为「执行 skill」；候选过双闸（invocation=规范术语「调用标记」互证、invoke 偏祈使、executable 夹带 chmod 联想），用户裁定 invocation。同步三处：规范文档、解析器（MARKER_RE + 旧标记/旧 info string 双形态 lint error，修掉 FORBIDDEN 扫 cleaned 文本漏检 fence 内旧标记的缺陷）、bootstrap 试点块；回归通过 | 依据：2026-08-19 用户选词裁定（闸二：招来的不干净）
- 2026-08-19 | implement 试点迁移完成：4 个 skill 调用打 `<use-skill>`（design×2/spec×2/tdd/code-review），`git worktree list` 转列表项内嵌 invocation optional 块；抽取精确命中（skills=[code-review, design, spec, tdd]、shell-optional=[git]）；**全仓 lint 首次清零**。两处有意不迁：§2 `git worktree add` 原文只写意图未写字面命令（迁移不新造命令）、完整示例节 skill 提及为场景叙述。试点发现升入规范规则 12–14（叙述不打标、不新造命令、列表项嵌 fence）| 依据：用户指令「进行下一个skill试点」；迁移原则「只转换、不新增」经本次验证可守住
- 2026-08-19 | invocation-markers 补规则 15–16（路径锚点）：scripts 调用以 `<skill目录>` 占位锚定 harness base directory，禁 cwd 相对路径；scripts 自身 cwd 无关（`__file__` 推导）。实证：本仓库两脚本（skill-observe.py、parse-skill-invocations.py）天然满足规则 16，从 /tmp 实跑 `--all` 验证通过 | 依据：同日返工事件（用户观察模型用项目目录调脚本）；参考文件可逆采纳层直接落地；SKILL.md 诊断表行「脚本找不到/跑错同名 → 路径锚点缺失 → 硬编码绝对路径」同日经用户一字批准补入
- 2026-08-19 | 脚本职责拆分：parse-skill-invocations.py 只抽取（静默），校验独立为 lint-skill.py；共享解析原语抽成 scripts/skill_md.py（正则/切块唯一信源，防双写漂移）。用户裁定「绝对路径不进正文规则（模型已知，写了是稀释）而进 lint（问 4：字节级检查归 scripts）」→ lint 新增可移植性检查（`/Users/`、`/home/` → error，`~/` 允许）。回归：两试点抽取结果不变、全仓 lint 清零、绝对路径与旧标记夹具命中 | 依据：2026-08-19 用户两裁定（职责单一；绝对路径归 lint 不归正文）
- 2026-08-19 | 移出：lint-skill.py 删除，skill_md.py 折叠回 parse-skill-invocations.py（单消费者不保留共享层）。用户裁定理由链：lint 实证价值是迁移期的（首跑 3 处漏标已兑现），持续校验面随迁移完成消亡；「校验」承诺超出启发式所能兑现（绝对路径检查不可完备），挂 error 级制造虚假确定感——预防性门禁无事故拉动，犯 harness「猜出来的门禁最坏」之忌。残余缺口（静默漏标、绝对路径）回 review 层；持续校验待真实事故拉回再建。规范 §5 与 §4 兜底条同步改写 | 依据：2026-08-19 用户指令「我觉得应该删除lint-skill」；回归：两试点抽取结果不变、--all 通过
- 2026-08-19 | 新建对外 skill：prototype（原型与界面效果对齐纪律）。主线「差异出自空白，先锚定再动手」；机制五件：差异分类路由（行为→spec / 效果→可观测断言）、锚定强度序（截图>DOM指认>数值>场景脚本>草图）、方位词消歧三选（区域命名/截图红框/复述指认）、术语→DOM 可 grep（类名即标签，样式导向才 data-region）、一次一区域当场对齐。语料与痛点来自 ses_fe5046c6dffejwDJNWf2iJ4tK3（4 轮原话留作种子，测试集待真实任务语料到场按冷启动建）。抽取验证：skills=[spec]，符合设计 | 依据：2026-08-19 评估（见观察名单结案条）后用户裁定创建；真实痛点+真实语料，证据等级高于既往纯讨论先例；待验证：触发边界（与 spec/domains 划界）、薄度保持、「类名即标签」在真实原型任务中的表现
- 2026-08-20 | prototype 术语→DOM 映射改写：删「类名即标签」（原型持续演进 → 样式类名随重构漂移，零工作换来锚点静默失效，用户裁定一律专用属性）；属性名 data-region 词不达意（撞 ARIA role=region 与云 region 联想），用户裁定自造 `data-term-anchor`（term+anchor 双招募，定义已显式写入正文）；授权分档同步：补锚点属性降执行后告知，新增结构属性条款删 | 依据：2026-08-20 用户裁定两条（一律专用属性；命名 term-anchor）；同日升格条中「类名即标签」表述被本条超越
- 2026-08-20 | prototype 移植社区版两机制（按移植纪律翻译不誊写）：①变体锚定——用户给不出锚点时同页造 3 个结构迥异变体对比挑选（含「结构级差异/非壁纸」「嵌真实骨架数据」两护栏），入第 2 步分支；②归宿节——结论沉淀 spec/任务文件、原型重写合入不照搬、落选变体与脚手架移除。不移植：LOGIC 支与 surface-state（无场景证据）、?variant=/浮动条等实现血肉。description 用户裁定放宽至创建+编辑（原型 HTML 创建修改、界面方向探索）；标题改「原型创建与对齐」；主线补「实物即锚」；分路步覆盖新建场景 | 依据：2026-08-20 用户批准移植（观察名单 ①② 拉动条件视为到场：社区调研+用户一字批准）；待验证：变体锚定在真实建造期任务的表现、description 放宽后是否过触发

- 2026-08-23 | spec 边界自查新增排除项「不含 UI 拓扑（分组/方位/包含）；区域名作词汇引用，存在性可断言、结构关系不断言——拓扑是原型的探索面」；prototype 归宿分化为「行为结论沉淀 spec/任务文件，视觉/拓扑结论留原型（可观测断言）不进 spec」（顺带修掉与第 1 步差异路由的内部不一致）| 依据：2026-08-20 返工事件（cake 锚定会话 ses_fe1c90e95ffeeJ4sjPTxierW1f：spec 三条拓扑断言随原型落定全部改写/删除，domains 四删三增），用户一字批准；新旧对比：旧文本下「侧栏分两组」过自查（非技术方案即放行），新文本下被拓扑排除项拦截，骨架存在性断言（「主界面含侧栏」）按存在性条款保留；减法审查：无可删（spec 为并列新增，prototype 新句与旧笼统句同体替换）；安装副本已同步 | 待验证：UI 类 spec 是否过度变薄（存在性/拓扑边界误判）；cake 桌面-spec 现存拓扑残留（「项目选择与会话历史两组」「模型选择位于输入区」）按新规则属待清理，下次触及该 spec 时验证

- 2026-08-23 | prototype 新增「锚点服务（自举）」节 + 授权分档补自举项：包从 packages/ 移入 `skills/prototype/scripts/prototype-anchor`（随 install.sh 分发，无第二信源）；自举三步 = npm link `<skill目录>` 安装 / 注册 MCP（command `["npx","-y","prototype-anchor"]`）/ 起 HTTP 面；npm 全局链接改指安装副本并实测 npx 链路、8420 服务经链接 bin 重启；不 publish（用户裁定，观察名单对应条已收窄）| 依据：2026-08-23 用户指令「先在 skill 里自动安装到本地 npm 并让 agent 自动注册该 mcp，目前先不 publish」；抽取验证：skills=[spec]、shell=[npm] 符合预期 | 待验证：他机/新项目首次自举是否顺畅（npm link 权限、opencode 重启提示）、cake 旧 serve.py 去留

- 2026-08-23 | prototype-anchor 移出 MCP 面 + 落地生命周期机制：bin v0.2.0，serve 成唯一入口（anchor_list/anchor_save 工具随 MCP 一并移出，agent 侧有 Edit/grep 无差值）；单实例=启动前探活 /__am_ping，已存活退出 0、外部占用报错退出 1（端口即锁，无 pidfile）；自动消亡=页面 60s 心跳保活 + 无请求 AM_IDLE_MIN 分钟（默认 10）自动退出（服务无状态，消亡零损失）；skill 自举节删注册 MCP 步、改写为两步（npm link + nohup serve）并注明幂等/消亡；opencode 全局 mcp 条目撤出 | 依据：2026-08-23 用户复盘「功能本身似乎和 mcp 没什么关系」（确认 MCP 面零差值）+ 用户提问单实例与自动消亡；六项生命周期实测全过（单实例幂等/外部占用拒绝/空闲消亡/心跳保活后准时消亡/裸命令用法/保存回归）；8420 已跑 v0.2.0 且二次启动幂等验证 | 待验证：真实页面打开场景的心跳保活节奏；睡眠唤醒后服务已退的回退提示体验 | 2026-08-23 补注：默认空闲消亡 10 分钟改 2 分钟（用户裁定，v0.2.1），心跳 60s 间隔与 2 分钟窗口容一次丢拍

- 2026-08-23 | prototype-anchor 起停收编为脚本：`skills/prototype/scripts/anchor-serve.sh start|stop|status`（幂等；人手与 skill 共用，AM_PORT 改端口）；服务端 v0.3.0 新增 POST /__am_shutdown 安全停机端点（stop 免 lsof、跨平台；仅监听 127.0.0.1）；skill 自举第 2 步改调脚本并注明手动用法；试点捕获一处真实 bug：bash 变量紧邻全角括号被吞进变量名（set -u 报 unbound），改 ${BASE} 花括号形态 | 依据：2026-08-23 用户指令「提供手动启动/停止的脚本，skill 里也可以直接调用」；六项脚本生命周期实测全过（start/幂等 start/status/stop/复停/未知参数），8420 经脚本重启于 v0.3.0；抽取验证 shell=[bash, npm]

- 2026-08-23 | skill-creator 迭代第 4 步新增「改动落盘即提交」（一个逻辑改动一个提交、证据日志追加与正文改动同提交、信息一句话带依据、非 git 仓库跳过且不提议 init），创建第 4 步补指针，domains 写入规范补同义条 | 依据：2026-08-23 用户发起审计（最近三天改动×触发 skill×时机）：08-21/22 零改动，08-23 改动全部经 skill-creator 迭代/domains 流程而其工作流无提交步，被 18:26 兜底提交卷走（node_modules 误入库为症状）；用户裁定 skill 流程节点级提交而非仓库级立规、非 git 仓库不提交不升级，措辞已批准 | 同步修正 5 条升格条目与 2 条补注的误标日期（08-20→08-23；返工事件记事件发生日不变）；本次提交即新规首个自觉遵守案例

- 2026-08-23 | prototype 组锚点语义定为容器盒：术语→DOM 映射改写（盒打 `data-term-anchor`、成员归属由 DOM 包含表达、成员须同父相邻、锚点视图「添加分组」自动包盒）；prototype-anchor v0.4.0 新增 wrap 包盒模式（parseTree 记 fullEnd 元素完整区间、同父相邻两级校验、拒绝即不落盘），客户端包盒前本地预检同父相邻；当日首版的 `data-anchor-member` 成员属性机制撤销（零消费者，不留坏死面）| 依据：2026-08-23 返工事件（首版分组标记语义与用户预期不符，用户原话「分组相较于直接标记锚点，只是多一步为这些元素增加一个更大的box元素」）；新旧对比：成员属性需维护第二套锚语义与渲染/清单分支，容器盒复用区域锚点全链路（覆盖标签/清单定位/术语表同步零新增概念），grep 组成员 = 盒的 DOM 后代；fixture 实测包盒（含嵌套子树 fullEnd）/非兄弟拒绝/不相邻拒绝/旧单锚点兼容四项通过

- 2026-08-23 | prototype 锚点删除落地：锚点清单行内 ✕ 与信息框「删除锚点」两入口，两次点击确认免弹窗；prototype-anchor v0.5.0 新增 POST /__am_delete（移除属性；unwrap 时连盒拆除、内容保留，parseTree 补记 closeStart）；分组纯包装盒（div 且仅有锚点属性）删除即拆盒——用户裁定，不留无标记 wrapper 坏死面；授权分档「补」改「补/删」| 依据：2026-08-23 用户指令「在锚点清单每一条后提供删除按钮，同时在锚点信息框中提供删除按钮」+ 拆盒行为裁定；fixture 实测删属性/无锚点拒绝/拆盒内容保留/void 拆盒拒绝/保存回归六项通过

- 2026-08-23 | prototype 锚点视图插件化：客户端抽为单文件 `anchor-view.js`（CSS 运行时注入、var() 带兜底色、window.__AM_VIEW__ 防重、DOMContentLoaded 兜底），收进 prototype-anchor 包由服务 `GET /__am_client.js` 分发（v0.6.0）；接入两档——零侵入经 `/proto?abs=` 打开（响应自动注入、幂等跳过已接入页、源码零改动），一行接入删行即净；术语三级回退 window.AM_TERMS → `GET /__am_terms`（解析项目术语表行，兼容定义跨选择器前后段的真实行格式）→ DOM 现查；syncDomains 与 listTerms 共用 domainsFileOf 消重；SKILL.md 自举节改写接入方式并补「写盘防护」提示 | 依据：2026-08-23 用户指令「锚点视图的设计是不是应该形成一个插件，允许应用于任何一个 html，并且能够无副作用的删除」+ 确认；同日三轮迭代均改内嵌拷贝为直接动机；实测：客户端分发/术语 8 条解析（含反引号边界修复）/注入幂等/无接入页注入一次/包盒与拆盒回归全过；cake prototype.html 换一行接入（250 行内嵌块移除）| 同会话附带事件：cake 文件双文档污染已修复（见返工事件），指纹防护入观察名单

- 2026-08-26 | prototype 第 5 步新增「交互语义分岔不取默认」：实现交互（开关/模式切换/分组/删除/点击响应/空值校验）遇多种合理语义的维度（联动排他或并集、模式互斥或正交、点击精确指认或就近归父、失败提示或静默），先列维度与候选取值请用户一句话点选再动手 | 依据：2026-08-23~24 六连返工（组锚点成员属性 vs 容器盒、单锚标签缺中间态、排他开关、A/B 互斥、closest 就近归属、保存静默 return）归纳同一病灶「实现便宜性压过语义贴合」；用户一字批准；新旧对比：旧文本实施期无语义分岔护栏，新文本把先锚定主线延伸到交互语义；减法审查：无可删（与第 2 步场景脚本不重叠） | 待验证：下一次含语义分岔的交互实现是否先点选
- 2026-08-26 | skill-creator description 划界双向对照：创建第 2 步自查句扩写（吞噬/重叠/负向指向真实性三查）+ 迭代第 3 步加「改动 description 时同步过双向对照」 | 依据：2026-08-26 description 竞争场会诊一次发现 4 条存量 skill 间冲突（spec×tdd 重叠有 fc7d3316 真实翻车、负向缺 prototype 有 08-20 拓扑倒挂前科），单向自查捕获不到存量冲突；用户一字批准；减法审查：无新增步骤，两处均为既有句扩写 | 待验证：下一次创建/修改 description 时对照是否实际执行
- 2026-08-26 | prototype 锚点服务节加「改动源码后必须重启生效」：start 幂等不替换已存活实例，须 stop 再 start 并行为实测确认新版本 | 依据：2026-08-24 返工事件（8420 跑 10:40 旧进程，内存含当日已删 syncDomains，磁盘改动不生效且无提示）；用户一字批准；新旧对比：旧文本「改代码→start→以为生效」路径无拦截；减法审查：无可删 | 待验证：下一次改动服务端源码后是否停旧再起新

- 2026-08-26 | 移出：hook 观测能力全拆——scripts/skill-observe.py、skill-observe.hooks.json、skill-事件日志.jsonl 删除；harness.md 三节（hook 迁移边界/落地顺序/落地实例）移除，改写为机制层现状与重建判据；skill-creator SKILL.md 冷启动/证据体系/Resources 三处同步；DOMAINS.md 事件日志词条与 skill 治理上下文移除；invocation-markers 规则 16 示例同步 | 依据：部署 20 天零真实捕获（事件日志仅 2026-08-06 冒烟 3 行），~/.codex/hooks.json 已不存在（安装静默丢失，harness 自预言的心跳防御同失、无人察觉）——机制死于静默失效，用户裁定「并没有实际用处」；同日两周任务审阅顺带发现 | 观察名单 #14（差值漂移）的 model.change 取证通道随本次删除关闭，已补注；外部面无残留（codex config 无心跳 automation 痕迹）
- 2026-08-26 | prototype-anchor package.json 元数据对齐：version 0.6.0→0.6.1（对齐 bin 内 VERSION 常量），description「写盘与术语表同步」改「写盘，术语表只读解析」 | 依据：同日全量 review 取证——v0.6.1（2026-08-24 findByPath 可读诊断升格）后 package.json 未 bump，同日术语表写入权收归升格（服务删 syncDomains 转只读）后描述未同步，元数据与已裁定事实双重失实；~/.agents 副本已同步，md5 一致
- 2026-08-26 | debug×3、design×1 处 skill 路由补打 `<use-skill>` 标记（debug 判层路由 design/spec、修复节 tdd；design 入口路由 spec） | 依据：同日全量 review 跑 parse-skill-invocations.py 实测——debug/design 抽取结果 clean 与正文路由指令矛盾，invocation-markers v0.2 规则 9（真实加载才打标）在 spec/tdd/implement/prototype/prototype-anchor-sync 已执行、此两 skill 欠账；2026-08-19「现有 10 skill 改写后抽取完整性」待验证项随本次清偿（全仓抽取现已与正文一致）；纯元数据无行为变化（规范 §4：标记是元数据不是行为），~/.agents 与 ~/.claude 三副本已同步，md5 一致

- 2026-08-28 | implement 新增「归档合并」节（同 spec 多份归档用户发起合并时：逐条与 spec/design/现有代码核对、过期已完成与被替代待办安全删除、按 spec 当前断言组织当前状态视图而非历史流水、删除留备查节原始文件靠 git 历史）+ description 用于段加锚点「合并同一 spec 的任务归档」 | 依据：2026-08-28 ey-timp-lab ses_fb91594feffe 真实使用（6 份归档合并为 archive/agent-skills.md）——第一轮理解被用户纠正（"我的意思是整合本地的task archive到一个文件"），核心纪律由用户口述补充（"应该与spec和design和现有代码进行核对…不应该是直接按照任务历史直接合并的汇总文档，而是一个能够反映已完成/未完成的汇总"），模型默认合并行为与期望差值实锤；措辞以用户原话为底，用户批准落点=并入 implement 而非独立 skill（触发面=显式命令无自动触发判别需求，同 BDD/refactor 先例）| 减法审查：无可删（第 6 步管单任务归档动作，新节管多份归档合并维护，不重叠）；N=1 单案有意不固化汇总文件完整模板（历程/已知边界等节留个案判断），防过拟合 | 待验证：第二次合并现场（spec 大改/删除后的归档合并、其他项目目录约定）

- 2026-08-28 | implement 任务文件新增「提交区间」：模板目标节一行（起点=建任务时基线顶端、终点=归档时任务分支顶端，终点归档时补记）+ 第 2 步起点记入 + 第 6 步终点补记与 code-review 派遣以区间为变更范围 + 归档合并节「按区间定位改动、悬空以现存代码为据」句；DOMAINS.md 任务文件词条同步「另锚定执行位置」句（部分收窄观察名单 #50 缝隙：基线分支原不在「四件事」列举内）| 依据：2026-08-28 ey-timp-lab ses_fb91594feffe 两次手工考古——归档合并时 6 任务↔57 提交映射靠分析重建、squash 批次由用户手动挑选；区间记录使两步变机械操作，且 squash 删 back 后区间是唯一事后凭证；消费者=code-review 范围圈定/归档合并定位/squash 批次；措辞经用户一字批准 | 边界：squash 删 back 后区间悬空（价值在 squash 前已兑现，可接受）；worktree merge 基线后区间混入基线提交（消费时 --first-parent 过滤）| 待验证：下一任务从建文件到归档按新点记录的执行度；区间在第二次归档合并/squash 现场的实消费

- 2026-09-11 | code-review 发现分级三级改六级 P0（致命）/P1（严重）/P2（一般）/P3（轻微）/P4（建议）/P5（低）；修复授权按场景分路——单纯 review 任务（用户直接发起）仅报告发现与修复建议并询问，不自动修；非单纯 review 任务（implement 等流程派发）仅 P5 可仅报告不修复，含 P4 及以上自动修不询问；裁决门槛「无未处理发现」改「无未处理的 P4 及以上发现」；死代码/测试专用物/矛盾注释钉死由 =Important 改不低于 P2；第 4 步改名「处置与修复循环」。implement 第 6 步同步（含 P4 及以上走修复循环，仅 P5 报告后继续）；冒烟 R01/R02/R04/R05/R06 与 implement S7 同步 | 依据：2026-09-11 用户指令（分级不够细致 + 两场景处置规则）；2026-09-07「最低档也自动修」裁定在 P5 档上被本指令放宽（P4 及以上仍自动修，该裁定主体保留）；DOMAINS.md 归档/合入词条「无未处理发现」按 code-review 处置语义解读，未改 | 回归：对照改后正文——旧稿三级、任何场景有发现即修复循环，新稿六级分场景处置；description 未改，触发集不跑；待真实使用验证 P5 档判级一致性与单纯 review 场景不自动修的执行度 | 减法审查：删「Critical 立即修；Important 与 Minor 归档前与合入前必须修」时限分档，被场景规则吸收

- 2026-09-14 | tdd 正文「测试走真实路径」条补两款：①装配面例证补手工构造接线（A new B 并传入关键依赖的构造缝），与容器注册并列须有验证覆盖；②切走生产消费者后重审旧入口剩余消费者，仅剩测试消费则删除入口或把测试迁到生产入口（全绿不算覆盖） | 依据：同日返工事件（ey-timp-etl thread 01a09bb2）；用户一字批准；冒烟补 T10

- 2026-09-14 | code-review 第 3 步分级地板补两款：①本任务导致既有生产代码退化为仅测试消费的，视同本任务引入的测试专用物（不低于 P2）；②不以基线存在同类缺陷降权，基线同类实例同案列为发现 | 依据：同日返工事件（ey-timp-etl thread 01a09bb2）；用户一字批准；冒烟补 R11

- 2026-09-14 | eli5 四档重设计：SKILL.md 重写为分层讲解——判档表（信号→档）+ 四档产物表（了解=HTML 图解 / 会用=场景指南 / 所以然=原理讲解 / 改造=旋钮与迁移），未指明默认了解档、信号跨档问一句、高档对前序档各一句带过、讲解止于「怎么改」动手改路由 spec；SKILL2.md 删除——受众校准四张表过不了审计四问第 1 问（模型本来就会按受众调措辞），压成第 4 步一句，「读懂来源材料」被新第 2 步吸收，三个示例段教科书化删除 | 依据：同日返工事件（用户投诉无法深入）+ 用户裁定形态=单 skill 四档（候选：四独立 skill / 每次全层输出，未选）| 减法审查：加判档表与产物表，对应删两份旧文件整体 | 冒烟补 S1/S2（S1=旧触发回归、S2=高档判档），语料不足处待真实使用按 testing-guide 扩充

## 冻结状态

（暂无）